[package]
name = "nsql-search"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Nexa SQL 파일 검색 엔진 — 병렬 폴더 걷기(std 스레드 풀) · .gitignore 부분집합 · 이진 제외 · SWAR 리터럴/단어/정규식 매칭 · mpsc 스트리밍 · 취소 · 열린 탭 버퍼 검색 (GUI 파일 검색 패널 · CLI `nsql grep` 공용 · docs/36 §2 · T-81b)"

[lints]
workspace = true

[dependencies]
# D-76 예외 — 선형 시간 정규식 코어(순수 Rust). 그 외는 std만(+ 자모열 검색 = nsql-core `hangul` · 의존 0).
regex.workspace = true
nsql-core = { path = "../nsql-core" }
