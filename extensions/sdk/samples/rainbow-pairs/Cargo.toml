[package]
name = "rainbow-pairs-ext"
version = "1.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Rainbow Pairs — 앱의 crates/nexa-sql/src/extensions/rainbow_pairs.rs(정책 층)를 SDK 위로 옮긴 WASM 확장. 설치하면 내장판을 대체한다"

[lib]
crate-type = ["cdylib"]

[dependencies]
nexa-ext-sdk.workspace = true
