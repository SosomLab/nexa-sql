# CLAUDE.md — Nexa SQL 프로젝트 컨텍스트 (이식용 메모리)

> **먼저 읽기:** [docs/00-foundation-report.md](docs/00-foundation-report.md)(보고서) → [docs/STATUS.md](docs/STATUS.md) → [docs/10-decision-record.md](docs/10-decision-record.md).
> ★ **다른 PC(맥 ↔ 윈도우 ↔ 리눅스)에서 이어 갈 때**: 세션의 로컬 메모리 폴더는 따라오지 않는다 — 규칙은 **이 파일과 [docs/61](docs/61-core-design-and-working-rules.md)** 이 전부다(핵심 설계의 불변식 · 작업 규칙 · OS별 차이 · 맥에서 처음 할 일).

## 1. 이 프로젝트는

**Nexa SQL** = **크로스플랫폼 경량 SQL 클라이언트(IDE) + CLI `nsql`**(Windows · macOS · Linux). DBeaver의 커버리지 × TablePlus/Golden의 가벼움 × SQL*Plus/PL/SQL Developer의 DBA 워크플로.
**올 러스트 · 단일 바이너리 · `../nexa-ui`(자체 CPU 래스터) 위에 그린다** — Qt·WebView·Electron 없음.

- 조직: **SosomLab** · 개발자: Sangyong Bae · kiros33@gmail.com · 저장소 <https://github.com/SosomLab/nexa-sql> · 라이선스 **PolyForm NC 1.0.0**
- 현 단계: **M1·M2 병행 진행(2026-09-22 · Windows 92차(최신화·분석 회귀 없음 · 🔧 nexa-ui 53차 Windows CI 실패 원인 = nexa-font 시험의 `set_text_gdi` 경주 → 가드 `GdiOn` + 글꼴 걷기 캐시 3-OS 복귀 · [journal 09-22 §14](docs/journal/2026-09-22.md)) · Mac 90차 · **Linux 91차**(★ 첫 Linux 세션 · Ubuntu 26.04 VM — Oracle Instant Client 셸 환경(`scripts/install-instantclient-linux.sh`) · 성능 전수([26 §7-7](docs/26-performance-architecture.md) · `scripts/linux-*.sh`) · Linux 기동 병목 둘 수정(nexa-font 글꼴 트리 걷기 캐시 · 아이콘 래스터 `memo`+찾기 막대 지연) · `[startup]` 구간 계측(`NSQL_TRACE_FRAMES`) · 전체 테스트 자동화 `scripts/linux-all-tests.sh`(357 · 414 · 통합 13/13) · OS별 차이 = [61 §3](docs/61-core-design-and-working-rules.md) Linux 열 · Linux 앱 아이콘(`app_id`+`.desktop` · `scripts/install-desktop-linux.sh`) · ★ **Linux 창 백엔드 기본 X11**(`gfx.linux_backend` · 모달 = `WM_TRANSIENT_FOR`+MODAL · x11rb — winit 0.30 Wayland는 부모·올리기 미지원) · 실기 = T-163) — **Mac 90차(09-21~22) = 89차 win 반영 · 프로필 BISCM·M4PLAN·Repository · 🔧 시작 직후 Details 팝업 · 🔧 탐색기 키보드 서버 간 이동(`cross_pane` · PgUp/PgDn 페이지) · 🔧 맥 탭 메뉴 배율 · 🔧 Σ 초기 활성 · ★ 비밀번호 창 최상위 모달 · 🔧 `EXEC` 블록 현재 문 실행(원문 `span`) · 🔧 `PRINT`/`VARIABLE` 로그 표현(값 · 이름+타입 · `Begin.server`) · 흠 `nsql conn add` 비밀번호 stdin/env 미배선(T-132 ③)** · 89차(win) = 종합 점검 회귀 없음 · T-150 PG refcursor ✅ · T-152 `Caps` 포트 ✅(엔진·러너는 방언이 아니라 능력표를 묻는다 · [61 §1-6](docs/61-core-design-and-working-rules.md)) · T-151 ✅(실서버 Oracle·SQL Server — DATE 진짜 바인드 · `OUTPUT` 보충) · T-153 대부분 ✅(`ACCEPT` · `COLUMN NEW_VALUE` · 시스템 변수 · `${v:형식}` · 값 상한) · T-155 · **89차 후반 = 설정 ▸ DBMS(Oracle 클라이언트 · [64](docs/64-dbms-clients-and-driver-packaging.md)) · 🔧 탐색기 우클릭(실제 입력으로 닿지 않던 결함 둘) · ★ 일회성 비밀번호 + 세션 자격 금고([21 §7](docs/21-connection-profiles.md) · T-132 ② ✅) · 같은 서버 `CONNECT` 규칙(자격 변경 = 재접속 · 프로필 이름 = 최종 접속 정보로 비교) · 자체 시험 `ui.*`·`NSQL_NO_ACTIVATE`([61 §4](docs/61-core-design-and-working-rules.md)) · 바인드만 있는 SELECT 항목 = 변수 이름이 열 이름(SQL Server·PG) · CLI도 비밀번호 없으면 서버에 안 감 · 저장하지 않은 탭 닫기 = 묻기(`editor.close_unsaved`) · 추가 24 = 성능 재점검 회귀 없음([26 §7-6](docs/26-performance-architecture.md)) · CLI `--password-stdin` · T-158·T-161·T-162 ②③ ✅** · 남음 = T-153 hover 값·입력 창 타입 열 · T-132 ① · T-156·157·159·160 · T-162 ①④⑤⑥ · **사용자 확인표 U-1~U-14([TODO](docs/TODO.md) T-154)** — ★ 윈도우에서 이어 갈 때 = [docs/61 §3·§6](docs/61-core-design-and-working-rules.md) "윈도우에서 처음 할 일" · 이어 받을 일 순서표)** — 드라이버 4종(sqlite·oracle·mssql·pg) · CLI · GUI(접속 창·탐색기·편집기·그리드·결과 다중 탭·찾기/파일 검색·트랜잭션 UX·툴바 그룹 도크·확장 패널·세션 창) · DR-1~34 · **글자 = Windows GDI ClearType 글리프(D-77)** · **결과 데이터 = 한 세트+뷰 투영(DR-33)** · **세션 컨텍스트(DR-34)** · **편집 버퍼 = nexa-ctl `TextBuf`(UTF-8 갭 버퍼 + 줄 표 · T-142) · 되돌리기 = 연산 기록 + 기록 파일** · 배포 = 설치본 + 포터블 모드 검토(D-78). **결정 D-100~132 확정. 다음 = 🔧 T-146(CI `integration` PostgreSQL 실패 원인 — T-137 L1 결함 여부) → T-145(줄 변경 표시·괄호 표의 줄 단위 갱신) · Mac 86차 = ✅ T-139 한글 입력(앱 조합 · `input.hangul_compose` · [62](docs/62-macos-input-and-present.md)) · 🔧 분할기 방언 결함(`split_script_in` — SQLite 트리거·`BEGIN;` 뒤 문장 미실행) · 전 객체 유형 DDL 자동 시험 · **Mac 87차 = 86차 보완 전부(T-148 ✅ · `SHOW ERRORS` 등) · 🚧 T-147 맥 화면 내보내기(`present.rs` + nexa-sys `layer_present` · 설정 `gfx.mac_present` · present 36 → 2.9 ms · 기본 softbuffer · D-133 = 기본 전환) · ✅ T-146(수동 커밋이 PG·SQLite·MSSQL·MySQL에서 실제로는 자동 커밋이던 결함 → `manual_begin_sql`) · 🚧 ★ **변수 관리 T-149([63](docs/63-variable-management.md) · V0·V1 ✅ · D-135~138 ✅ 계층형 변수 표·문장마다 결과 탭·실행당 한 번 입력 창·파일별 보존 · ✅ 변수 창·`SELECT … INTO` 행 수 정책(SQL Server `@@ROWCOUNT`)·기준 계측 · CI 전부 초록 · 다음 = PG refcursor(Codespaces) → `Caps` 포트 → MacroStore·`ACCEPT`)**([journal 09-21](docs/journal/2026-09-21.md))** · ⏳ 실기 = journal 09-19~20(큰 파일 한글 조합 · 거대 선택 위 타이핑 안내 · 재시작 뒤 되돌리기) · 대기 = D-87~90 → T-103 위키 잔여 → T-102 · T-105 · T-106 · T-136.**
- **09-16 Windows 세션(11~37차 · [STATUS](docs/STATUS.md) · [journal](docs/journal/2026-09-16.md))**: 로그 창(스크롤·스위치 4·형식 어댑터 8·파일 싱크·필터·선택·F10) · 토스트+DBMS 오류 정규화 [42](docs/42-db-error-normalization.md) · 결과→SQL 키 규칙 [41](docs/41-sql-copy-key-rules.md) · CLI 폭/형식/도움말(기본값은 설정에서) · **페치 모델 [43](docs/43-fetch-model-and-result-tabs.md)(결과 도구줄 5그룹 · 보기 모드 7종 · 스크롤 끝 자동 페치 OFFSET 폴백 · 지연 텍스트 변환 · 다중 탭 검토 D-73~75)** · 상태줄(선택 3종 · 줄끝 3종 · 인코딩 CJK `encoding_rs` · git) · 편집기(거터 띠·여백·선택어 외곽선·구분선 설정·정규식 **D-76 fancy-regex**·실행 뒤 캐럿·Alt+↓/↑) · 단축키 프리셋 `key.preset`(Sublime 정렬) · 글자 선명도(Segoe UI·정수 스냅·대비 감마) · 설정 창(종속 잠금·콤보) · 탭 메뉴 · 자원 거버넌스 설계 [39](docs/39-resource-governance.md). **다음 = T-93 결과 다중 탭(확인 뒤) · T-48a 커서 유지 · T-94 데이터 편집기 · T-96 탭 검색 · T-97 미니맵 · T-98 Sublime 편집 명령.**
- **09-20 Windows 세션(84차 · [journal](docs/journal/2026-09-20.md))**: ★ **편집 버퍼 = nexa-ctl `TextBuf`**([59 §6](docs/59-large-file-handling.md) · UTF-8 갭 버퍼 + 줄 표 + 줄 변경 기록 `LineChange` · **좌표는 글자 인덱스 그대로**(바이트는 버퍼 안에서만) · `TextBox::chars()` 폐지 → `buf()` · 그리기 = 보이는 줄만 `Rows` + 줄별 캐시를 변경 기록으로 · 65 MB 상주 348 → 87 MB · 입력 190 → 3 ms) · **되돌리기 후속**([60 §7](docs/60-undo-redo-redesign.md) · `editor.undo_group_ms` · `merge3::line_edits` 재로드 · 거대 편집 = 지우는 동작 되풀이 `editor.undo_giant_mb` · 기록 파일 `undofile.rs` `editor.undo_persist`) · 찾기 = 줄 단위(`find_in_chars`). **본문 전체를 글자 배열·문자열로 뜨는 코드를 새로 넣지 않는다**(`buf()`의 줄·구간 조회를 쓴다). **다음 = T-145 · Mac T-139.**
- **09-20 Windows 세션(83차 · [journal](docs/journal/2026-09-19.md))**: ★ **되돌리기 = 연산 기록**([60](docs/60-undo-redo-redesign.md) · nexa-ctl `Op`/`Txn` · 저장 = 지운 글자만 · 본문 변경 단일 통로 `splice_rec`/`replace_many_inner` · 저장 지점 `mark_saved`/`is_saved` O(1) · 바이트 예산 `editor.undo_budget_mb` · `read_only`) · ★ **대용량 파일**([59 §5](docs/59-large-file-handling.md) · 큰 파일 모드 L1/L2 `Editors.large` · 열기 선택 넷 `bigfile.*` · `file.run_file` · **탭 격리 적재** `fileload.rs` = 자리 탭 `begin_load_tab` → 스레드(읽기·풀이·`PreparedText`) → `fill_loaded`(활성 탭 안 빼앗음) · 진행 막은 그 탭에만 · Esc/탭 닫기 = 취소 · **적재 중에도 다른 탭은 그대로**(사용자 09-20 — 전역 차단 철회)) · 확장 상세 = 뷰 탭 `ext_view.rs` · 탐색기 우클릭 ▸ 계층형 Refresh · 로그인 표식 `mark_dot`. (T-142 · D-129~132는 84차에 완료.)
- **09-19 Windows 세션(67~75차 · [journal](docs/journal/2026-09-19.md))**: 캐럿 깜빡임 위상 = 입력 기준(↑ 1열 이동 "느림" 실측 [55 §6](docs/55-editor-input-latency.md) · `tmark` 계측 · `scripts/win-latency-probe.ps1`) · Disconnect 배지 즉시 갱신 · 결과 도구줄 연결 전/불필요 시 비활성(nexa-ctl 글리프 비활성 흐림) · 탐색기 ← 키·DBeaver 키·**타입어헤드**(nexa-beep 이식 · nexa-ctl `typeahead`/`hangul` 부품 · Windows 한/영 · 설정 `explorer.typeahead*` · HUD 위치 = 이미지 드롭다운 `PositionDropdown`) · DBMS 오브젝트 종류 라벨 영어 고정 · 설정 창 스위치 조각(Switch 빈 bounds) · 설정/트랜잭션 로그 창 IME 켬 + 검색 즉시 반영.
- **09-19 Windows 세션 후반(76~79차 · [journal](docs/journal/2026-09-19.md))**: `editor.auto_close_pairs`(자동 닫기 = 편집 코어) · ★ **T-137 수동 커밋 잠금 방지 L1~L4**([56 §9](docs/56-manual-commit-lock-prevention.md) · `ReadEnd` · `txwarn.rs` · `tx_guard_step` · 막힘 감지 · 서버 안전망 · 설정 `tx.*` 8키) · 오류 카드+토스트 중복 제거 · 팔레트 마우스/스크롤 · 📐 탐색기 갱신 설계 [57](docs/57-explorer-refresh-after-ddl.md)(D-106~110 ✅ · T-138) · ★ **확장 패널**(`ext_panel.rs` · 활동 막대 아이콘 = 관리자 켜짐일 때만 · `ext.disable_mgr` · 관리자 끔 = 전 확장 정지 · 빈 목록 팔레트 안내 · 다운로드 추적 `NetStat` · [50 §14](docs/50-extension-system.md)) · 일반 편집 모드 괄호 색 끔 · 이웃 깊이 대비 색 순서(nexa-ctl `contrast_order` · [51 §12](docs/51-rainbow-brackets.md)) · **자체 캡처 = `NSQL_HOME` 격리 + `NSQL_STARTUP_CMD=open:<파일>,<명령 id>,…`**(키 주입 금지 — SendKeys는 사용자의 전경 창으로 간다). · **80차**: ★ T-138 객체 탐색기 갱신 ✅(`nsql-core/ddl.rs` `ddl_target` · 디프 갱신 · T1~T4 · 설정 `meta.refresh_*`) · T-137 "차단 중" 띠 · 안내 탭 = Plain Text + No connection · 📐 [58](docs/58-external-change-policy.md) 외부 파일 변경(결정 대기 D-119~124 · T-140). · **81차**: ★ T-140 외부 파일 변경 ✅([58 §5](docs/58-external-change-policy.md) · `extfile.rs` · nexa-ui `merge3`/`nexa-fs watch`) · 접속 유형 개발/시험/운영(`ConnectSpec.env` · 목록 우클릭 · PROD 칩 · `tx.prod_*` · `run.prod_confirm`) · 메모리 재점검 [26 §7-3](docs/26-performance-architecture.md) · 세션 창 유휴 CPU 결함 수정 · 기동 명령 `@connected:`. · **82차**: ★ 큰 파일 성능([59](docs/59-large-file-handling.md) · nexa-ctl 세대+줄 해시 캐시 · 4만 줄 그리기 78 → 3.3 ms · 입력 209 → 16 ms) · 되돌리기 = 차이 저장 · 메모리 회수 `memtrim.rs`(즉시 1회 + 유휴 주기) · 덮어쓰기 = 타임아웃 버튼 · 기동 명령 `@after:`. **결정 대기 D-125~128(큰 파일 모드 · 버퍼 교체 · 되돌리기 예산 · 디스크에서 실행) · 다음(Mac) = T-139 한글 입력.**
- **09-17 Windows 세션(52차 · [journal](docs/journal/2026-09-17.md))**: ■ 중지 버튼 결함 · **DR-33**(nsql-core `RowSource` 포트 · `ResultData` Arc 세그먼트 · `View` 인덱스 투영 · nsql-io 제네릭 · 복사/텍스트 보기/CLI 렌더러 하나 · `grid.null_text`/`cli.null_text` · 텍스트 캐시 예산) · **툴바 그룹 도크/플로팅**(nexa-ctl `ToolDock`·`ToolGroup`·`DockLayout`·`ToolItem::separator` · `toolfloat.rs` · 설정 `toolbar.layout` · View ▸ 툴바 배치 초기화 · 편집 UI = T-105) · 포터블 D-78/T-106 · **실행 속도 향상 `perf.boost`**(39 §4-6 · 강제값 > 개별 값 > 프리셋 · UI 전용 키만) · 트랜잭션 로그 창 44(T-107 ✅) · 실행 취소 포트 `CancelHandle`(SQLite/PG/Oracle/MSSQL Attention·소켓 · `mssql.encrypt`/`cancel` · PRELOGIN 탐침 · 44 §6) · 실행 상태 카드 · 개발자 모드 상세 로그 [48](docs/48-logging-architecture.md) · Auto Indent [49](docs/49-auto-indent.md) · 인텔리센스 설계 [47](docs/47-intellisense-metadata.md)(D-79~86 · MetaStore ①) · 데이터 일관성 엄격 모드 [43 §9](docs/43-fetch-model-and-result-tabs.md) · Sublime 커서 규칙 · 미니맵 [46](docs/46-minimap-features.md) · 줄 변경 표시 · 플러그인 검토 [50](docs/50-extension-system.md) · 향상 모드 벤치 [45](docs/45-perf-boost-benchmark.md). **⏳ 실기 = journal 52차 ①~㊴ · 결정 대기 D-87~90.**
- **09-18 mac 세션(56차 · [journal](docs/journal/2026-09-18.md))**: ★ **DR-34 세션 컨텍스트**([52](docs/52-session-modes.md)) — `sessions.rs` `Sess`(워커 + 실행·트랜잭션 상태 전부 · 활성 탭의 세션을 `self.sess`로 맞바꿈 · 잠든 세션은 `with_sess`) · **통제 단일화** `Sess::blocked`/`gate_open`/`sync_gate`(실행·Explain·페치·전체 조회·건수·키 조회·Commit/Rollback·접속 · 그리드 `session_blocked`) · 편집기 **`CONNECT <프로필|"접속 문자열">` = 탭 전용 세션** · `DISCONNECT`/탭 표식(nexa-ctl `TabBadge`) 메뉴 = 공유 복귀 · `session.mode=per-editor`(처음 활성화될 때 접속) · **공유 연결 N개**(접속 창 Connect = 기존 유지 + 추가 · 같은 서버 중복 금지 · `tab_bind` · 툴바 Disconnect ▾) · 유휴 닫기 `session.idle_secs`(전용만 · SQLite·열린 트랜잭션 제외 · 주기 핑 없음) · 설정 `session.private_connect/max_shared/max_private/idle_secs/idle_shared` · 분기 판단 = 순수 함수 + **MC/DC 테스트**(52 §12) · 맥 로그인 창 Details = 목록 크기 유지 + 오른쪽 확장 · 텍스트박스 앞부분 표시/스크롤 표시 자동 숨김(nexa-ui 32차). · **후반**: D-96~108 권장안 확정·개발 — **서버별 탐색기 `explorers.rs` `ExplorerSet`**(메타 = 인텔리센스·툴팁의 단일 원천 → 그 서버에 붙은 세션 ≥1이면 유지 · 0이면 접속만 닫고 트리 유지 · 메타 유휴 회수) · **세션 상태 추적** `alters_session_state`(상태 있는 세션은 유휴 닫기 제외 · 52 §6-4) · `CONNECT` 앞 문장 거부 · 탭 ✓/✗ · `TxLog` 전 세션 합본(`select_session`) · `session.max_shared` 8. · **57차**: 탐색기 = 서버 루트를 접속 순으로 한 트리처럼 이어 붙임(`ExplorerSet` 공용 스크롤 · `Explorer::set_clip`) · 로그인 목록 열 조절 = 결과 그리드 규칙(경계 추종 스크롤 · 경계 더블클릭 자동 맞춤 `pad + max(헤더, 값) + pad/2`) · 기본 창 크기(로그인 748×526 · 메인 1375×945) · `editor.minimap` on · `examples/oracle-refcursor-pkg.sql`. · **58차**: 📐 [53 접속 생존 관리](docs/53-connection-liveness.md)(VPN 끊김 실측 · 타 도구 비교 · Broken 상태·동작 직전 판정·TCP keepalive 제안 · D-109~114). · **59차**: ✅ 끊긴 세션 관리(53 §8 · D-109~114) — 워커 `ensure_alive`(`live_plan` · SYN 판정 · 실행·페치·건수·키·커밋·접속 전) · `Sess.broken`(표식·플러그 빨강·목록·상태줄) · `Session::is_alive` · TCP keepalive · Broken 중지 = `abandon_worker`. · **62차**: 신호등(창 열면 즉시 · 상한 = interval · 클릭 재확인) · SQLite 파일 판정 · 접속 문자열 열(`connection_string()`)/복사 · 기본 스키마 `?schema=`(Oracle CURRENT_SCHEMA · PG search_path · MSSQL USE) · **61차**: 세션 창 `sessions_win.rs`(별도 창 · 서버별 표 · Disconnect ▾/View) · **60차**: 접속 UX 전체 설계·구현([52 §7-2](docs/52-session-modes.md)) — 표식 메뉴(공유 목록 · 미연결 · 이 탭 접속 · 전용 정보) · 세션 관리자(서버별 · Disconnect ▾ `id#drop` · View) · Disconnect 본체 = 모두 해제 · 새 탭(공유 = 활성 연결 · 개별 = 접속 창) · 같은 서버 CONNECT = `connect.reconnect_same` · 표식 항상 · 플러그 색 = 지금 탭. **⏳ 실기 T-125 · 다음 = T-130 · T-121 잔여 · T-122.**
- **09-19 mac 세션(63차 · [journal](docs/journal/2026-09-19.md))**: T-131 폼 바뀜 표시·잃는 순간 3택 · 비밀번호 없는 `CONNECT` = 자격 빌리기였음 → 제거·거부(T-132) · No connection 유지 · 필수 항목 설계 [22 §10](docs/22-driver-extensions.md)(D-115·116) · **툴바 그룹 드래그 고스트·다중 행·Esc**(nexa-ui `ToolDock` 38차 · `DockLayout` `;` 행) · **그리드 컬럼 고스트·Esc**(`HdrDrag`) · 파일 열기 = 새 탭 · 공유 4탭 통제 검토 [52 §3-3](docs/52-session-modes.md) · 편집기 ↑/↓ 줄 끝 · 탐색기 가로 스크롤 + 셰브론 클립(`polyline_clipped`) · **Σ 건수 규정**(`Grid.countable` · 결과 출처 문장) · ★ **Disconnect 연결 모델 [54](docs/54-connection-model-and-disconnect.md)**(자리 = 범위 · `disconnect_plan` D16 · SharedAsk 팝업 · 배지 · 탐색기 루트 메뉴 · 해제 로그 경로 `DiscPath`) · 로그 창 선택 복사/체크박스/⌘C 물리 키(`input::shortcut_letter`) · 토글 창 위치 드리프트(`wingeom::place_outer`). **⏳ 실기 · 결정 대기 D-115~118.** · **64차**: 필수 항목 `*`·경고 띠(T-133 · `Dialect::login_required`) · 목표 열 커서(`goal_x`) · I-빔 · **입력 지연 [55](docs/55-editor-input-latency.md)**(`EditState::rev`/`chars()` · 내용 폭 캐시 · 슬라이스 줄 · `dirty_cache` · 벤치 `nexa-ctl examples/bench_editor` · 2 MB 195→13 ms · 잔여 T-136) · 임시 `dev.start_demo`(**Debug 전용 · Release 제외**) · **65차** 시작 접속 탐색기 루트 경주 수정 · **66차** 접속 창 **접속 중 막**(`ConnWin.veil` · 입력 차단 · 대상/단계/경과 · 닫힘).
- ★ 형제 저장소 clone은 `git@kiros33.github.com:SosomLab/<repo>.git`(SSH 별칭 · 사용자 지정 09-13).

### 참조 원천 (재발명 금지)

| 원천 | 경로 | 무엇 |
|---|---|---|
| **`nexa-ui`** | `../nexa-ui` | ★ 공용 UI — `nexa-gfx`·`nexa-ctl`(컨트롤 17종·토큰)·`nexa-conf`. path 의존 · **파일 관리·파일 대화상자 계층 설계 = nexa-ui docs/20**(3-OS 동일 · 네이티브 대화상자 0 · nexa-sql 배선 F-6) |
| `nexa-clip` | `../nexa-clip` | 앱 골격(plat·창·트레이·릴리스 파이프라인 3-OS·`check-3os.sh`·`21-manual-test` 실기표) |
| `nexa-dir2` | `../nexa-dir2` | `nexa-gui/widgets`(dock·tabbar·menubar·columns·rows) · **`wasmi` 플러그인** |
| `nexa-dir` | `../nexa-dir` | `docs/13`(라이선스 정책) · `docs/17`(Ed25519 활성화 설계) — ⚠️ 이 기기에 없음(09-14) |
| **`nexa-license`**(✅ 09-14 생성 · 공개 · T-45 🚧) | `../nexa-license` | ★ 라이선스 **공유 라이브러리**(형식·서명·검증·기기 ID·프로토콜) — 앱 `nsql-license`와 비공개 서버 `../nexa-license-server`(`nexa-licensed`·발급기)가 함께 씀 · [25 §9](docs/25-license-tiers-and-server.md) |

## 2. 확정 결정 (요약 — 전문 [docs/10](docs/10-decision-record.md))

| # | 결정 |
|---|---|
| DR-1 | **Rust 올 네이티브 · nexa-ui 확장**(프레임워크 도입 없음) |
| DR-2 | PolyForm NC 1.0.0 — 영리만 유료 |
| DR-3 | 외부 crate 0 지향의 명시 예외 = **드라이버·셰이핑**(어댑터 안에 격리 · 원장 기록) |
| DR-4 | 공용 컨트롤은 `nexa-ui`로 먼저 분리(완료) |
| DR-5 | **편집기 = Sublime Text 차용 · 확장 = Sublime 패키지 구조**(파일 형식 그대로 · WASM 플러그인) |
| DR-6 | **CLI `nsql` 포함**(접속·스크립트·export/import·bulk) — GUI와 같은 코어 |
| DR-7 | **4계층**: UI / Core / Network / 어댑터 — 허브 포트로만 연계(④→③←①, ①→②) |
| DR-8 | **세션 변수는 클라이언트에 산다** — 리터럴 대입 로컬 · 방언 재작성(MSSQL `sp_executesql` OUTPUT · `SELECT @X=`) |
| DR-10~18 | 단일 앱 · 지원 범위 3급 · 드라이버 선택 · 자체 결과셋 · rustybuzz · WASM 플러그인 · 예산 · **CLI+최소 GUI 병행** |
| DR-19 | **한글·고정폭 1급**(`nexa-font` · 편집기/그리드 고정폭 + 한글 폴백) |
| DR-20 | 코드 서명은 **별도 요청 시** |
| DR-22 | **연결 프로필 = 사용자 폴더 `nsql-vault`**(비밀번호만 봉투 · Windows DPAPI 기기 키 · CLI·GUI·다중 인스턴스 공유) — [21](docs/21-connection-profiles.md) |
| DR-23·24 | **접속 대화상자 = DBeaver식 + Golden 로그인 리스트** · **드라이버 확장 = GitHub Releases 최신 다운로드 · stdio JSON-RPC 프로세스 · SxS 다중 버전 · 관리자(목록·삭제)** — [22](docs/22-driver-extensions.md) · T-27~31 |
| DR-27~29 | **배포 = 설치본만**(목적별 exe · [33](docs/33-distribution-and-packaging.md) · **포터블 모드는 D-78 재검토 · 새 상태 저장은 `NSQL_HOME` 규약**) · **PostgreSQL 내장** · **내장 드라이버 정적 링크 유지 · 확장 드라이버 = in-process cdylib(C ABI · 지연 dlopen · IPC 0 · 프로세스는 격리 선택)**([22 §0](docs/22-driver-extensions.md)) |
| DR-33 | ★ **결과 데이터 = 한 세트 + 뷰 투영** — `ResultSet` 덩어리 · GUI `ResultData`(Arc 세그먼트 · 복사 0) · `View`(행·열 인덱스) · 렌더러·그리드·복사·정렬은 `RowSource` 포트 하나 · NULL 글자 = 설정 1(`grid.null_text`) · 컬럼형은 T-102 후보 |
| DR-34 | ★ **세션 컨텍스트 = 통제의 단위**([52](docs/52-session-modes.md)) — 기본 사상 *1 인스턴스 · 1 서버 · 1 계정* · 한 세션 = 한 번에 한 작업(`Sess::blocked` · 문지기 `gate_open` 하나) · 공유 연결 N(추가 접속 · 중복 금지) · 전용 세션 = 편집기 `CONNECT` · 개별 모드 `session.mode=per-editor` · 유휴 닫기 |
| DR-25 | **라이선스: 공유 라이브러리 `nexa-license`(형제 저장소) + 인증 서버는 비공개 저장소 `nexa-license-server`** · 티어 4단(Device·User 5대·Team·Org 서버) — [23](docs/23-license-activation.md) · [25](docs/25-license-tiers-and-server.md) · D-23~39 대기 |

### 설계 문서 지도(요구별)
**아키텍처 패턴·확장점·부품 원장 [30](docs/30-architecture-patterns.md)** · **세션 모드(공유 N·전용 CONNECT·개별 · 실행 통제 · 유휴 · MC/DC) [52](docs/52-session-modes.md)** · **변수 관리(Usage 19군 · VarStore/MacroStore/Caps · 방언별 기법 · 단계) [63](docs/63-variable-management.md)** · 편집기 색·선택 참조(DBeaver·Golden 실측) [35](docs/35-editor-colors-reference.md) · 파일 검색 탭·프로젝트 [36](docs/36-find-in-files-and-project.md) · 파일 선택기·트리 성능 점검 [37](docs/37-file-picker-performance.md) · **자원 거버넌스(기준·부하원 원장·`perf.mode`·게이트) [39](docs/39-resource-governance.md)** · 결과 → SQL 키 규칙 [41](docs/41-sql-copy-key-rules.md) · DBMS 오류 정규화·토스트 [42](docs/42-db-error-normalization.md) · **트랜잭션 로그(DBeaver QM + Tx 열) [44](docs/44-transaction-log.md)** · 성능 향상 벤치 [45](docs/45-perf-boost-benchmark.md) · 미니맵 기능 조사·우선순위 [46](docs/46-minimap-features.md) · **객체 인텔리센스(메타 저장소 한 벌 · D-79~86 · 설정 §8) [47](docs/47-intellisense-metadata.md)** · **실행 로그 상세 계측·개발자 모드·저비용 게이트 [48](docs/48-logging-architecture.md)** · Auto Indent(Sublime 변수 4·규칙 세트) [49](docs/49-auto-indent.md) · **수동 커밋 잠금 방지(도구 조사 · L1~L4 · D-100~105) [56](docs/56-manual-commit-lock-prevention.md)** · **DBMS별 종속 설정(Oracle Instant Client 자동 탐지/직접 지정) · 드라이버 내장 ↔ 확장 검토 [64](docs/64-dbms-clients-and-driver-packaging.md)** · ★ **OS별 성능 비교(속도·용량·메모리 · Windows·mac·Linux · 최적화 전/후) [65](docs/65-cross-os-performance-comparison.md)** · **대용량 파일(큰 파일 모드 · 탭 격리 적재 · 실측) [59](docs/59-large-file-handling.md)** · **되돌리기 재설계(연산 기록 · 11종 조사 · 남은 제안) [60](docs/60-undo-redo-redesign.md)** · ★ **핵심 설계의 불변식 · 작업 규칙 · OS별 차이(다른 PC에서 이어 가기) [61](docs/61-core-design-and-working-rules.md)** · **객체 탐색기 갱신(DDL 뒤 그 폴더만 · 유휴 워터마크 · 못 찾음 신호 · D-106~110) [57](docs/57-explorer-refresh-after-ddl.md)** · 플러그인 시스템(GitHub 매니저 · WASM/프로세스 · Python 검토) [50](docs/50-extension-system.md) · **트랜잭션 로그(DBeaver QM 검토 · Tx 열 · T-107) [44](docs/44-transaction-log.md)** · **페치 모델·결과 탭(상한·커서·Ctrl+\\) [43](docs/43-fetch-model-and-result-tabs.md)** · 들여쓰기 설정 계층 [31](docs/31-indentation-settings.md) · 연결 프로필 [21](docs/21-connection-profiles.md) · **배포판(설치본만·목적별 exe·macOS) [33](docs/33-distribution-and-packaging.md)** · 서버 메시지·실행 중 로그 [32](docs/32-server-messages-and-live-log.md) · 트랜잭션 UX [34](docs/34-transaction-ux.md) · 오브젝트 탐색기 [28](docs/28-object-explorer.md) · 드라이버 확장·접속 대화상자 [22](docs/22-driver-extensions.md) · 정품 인증·기능 게이트 [23](docs/23-license-activation.md) · 라이선스 종류·인증 서버·저장소 분리 [25](docs/25-license-tiers-and-server.md) · 설정 체계·i18n·테마 [24](docs/24-settings-and-vscode-analysis.md) · 편집기 점증 순서 [17](docs/17-editor-incremental-plan.md) · 세션/hot exit/프로젝트 [18](docs/18-session-and-projects.md) · 외부 변경(git식 병합) [15](docs/15-external-file-changes.md) · **외부 변경 정책(병합·확인 빈도·감지 시점·감시 구조 · D-119~124) [58](docs/58-external-change-policy.md)** · **대용량 파일(병목 수정 · 메모리 회수 · 편집기 비교 · 3단계 제안 D-125~128) [59](docs/59-large-file-handling.md)** · 비교/git/오브젝트 시점 캐시 [19](docs/19-compare-git-and-object-history.md) · 폰트·기능 모듈 [14](docs/14-fonts-and-feature-modules.md) · 사용자 Sublime 프로필 [12](docs/12-user-sublime-profile.md).

## 3. 작업 규약

- ★ **세션 공통(로컬 메모리에서 저장소로 올림 · 09-20 · 자세히 = [docs/61](docs/61-core-design-and-working-rules.md))**:
  - **답은 한글로**(진행 안내 한 줄까지 · 식별자·명령어는 그대로).
  - **"commit · main 병합 · push"** = 작업 브랜치 → 커밋 → `main`에 `--ff-only` → 로컬 브랜치 삭제 → `docs/BRANCHES.md` 한 줄 → push(**nexa-ui 먼저**) → CI 결과를 journal에. 커밋 메시지 끝에는 그 세션이 안내하는 `Co-Authored-By:` 줄을 그대로. 끝나면 CLAUDE.md "현 단계" 줄도 최신인지 본다.
  - **앱을 띄워 시험할 때는 `NSQL_HOME` 격리 + 적용됐는지 확인**(격리 폴더의 `settings.conf` 수정 시각 · 실제 설정 파일은 그대로인지) · **단위 테스트는 실제 설정 폴더에 쓰지 않는다**(폴더를 인자로 받는 `*_in` 패턴 · `temp_dir()` 아래에서만).
  - **사용자가 자리에 있을 때 키·마우스 입력을 주입하지 않고 포커스를 빼앗지 않는다**(SendKeys · System Events 키 · `SetForegroundWindow` 금지) — 앱은 `NSQL_STARTUP_CMD`로 몰고 화면은 창 단위 캡처(Windows `scripts/win-burst-capture.ps1` · macOS `screencapture`). 키가 있어야 보이는 것은 단위 테스트로 덮고 실기는 사용자 몫으로 보고한다. **사용자의 클립보드를 덮어쓰는 명령(잘라내기·복사)을 자동 시험에 넣지 않는다.** 프로세스는 **내가 띄운 PID만** 끝낸다.
  - ★ **내가 직접 관리하는 대상은 승인 없이 정리한다(사용자 09-21)** — 사용자가 걱정하는 것은 *이 프로젝트 범위 밖*의 폴더·파일이 지워지거나 바뀌는 일이다. **직접 관리 대상** = 이 세션이 만든 것: 세션 스크래치패드 · 격리 `NSQL_HOME` 폴더 · 비교용 워크트리(`../_cmp/…`)와 그 빌드 산출물·로그 · `target/` 아래 산출물·캡처 · 내가 띄운 PID · 실서버의 임시 객체(`pg_temp`). 이것들의 생성·삭제·덮어쓰기는 **묻지 않고 진행**하고 보고에 한 줄로 남긴다(지우기 전에 "내가 만든 것이 맞는지"만 확인 — 목록·경로·만든 시각). **여전히 먼저 묻는 것** = 두 저장소와 스크래치패드 **밖**의 경로 · 사용자의 실제 설정 폴더·프로필·볼트 · 추적 중인 파일을 되돌릴 수 없게 버리는 일(`git reset --hard` · `git clean` · 강제 push) · 사용자 파일(`.claude/settings.json`은 병합만) · 내가 띄우지 않은 프로세스. 자세히 = [61 §2-4](docs/61-core-design-and-working-rules.md).
  - **측정 먼저 · Release로 잰다**(`scripts/win-big-probe.ps1` · Linux `scripts/linux-perf-all.sh` · `NSQL_TRACE_FRAMES`(프레임 + `[startup]` 기동 구간) · nexa-ui `bench_editor`/`bench_undo`) · 자료 구조를 바꾸면 **단순 모델과 난수 대조 테스트** · 교체는 호환 경로로 기존 테스트를 먼저 통과시킨 뒤 이관.
  - **편집기 불변식**: 좌표 = 글자 인덱스(바이트는 `TextBuf` 안에서만) · 본문을 바꾸는 길은 nexa-ctl의 셋(`splice_rec` · `replace_many_inner` · `apply_ops`)뿐 · 본문 전체를 `Vec<char>`/`String`으로 뜨는 코드를 자주 도는 길에 넣지 않는다(`buf()` 조회) · 줄별 캐시 = 변경 기록의 소비자 · 오래 걸리는 일은 **그 대상(탭) 하나에 가둔다 — 창 전체 입력을 막지 않는다** · 뒤에서 끝난 일이 실행으로 이어지면 시작할 때의 탭을 확인한다.
  - Windows 전용 함정: Git Bash heredoc이 `\n`·백슬래시·따옴표를 망가뜨린다 → 코드 패치는 스크립트 **파일**로 · `"$VAR\\$1"`은 글자 그대로 `$1` · 실행 중인 exe가 링크를 막는다 · nexa-sql 교차 타깃 검사는 이 PC에서 안 된다(CI에 맡긴다).

- ★ **진행 보고 규칙(사용자 09-19)**: 사용자가 직접 지시하면 **즉시 "진행 중 목록" 맨 아래에 추가하고 갱신된 목록을 바로 보여 준다**(요구가 순차적으로 쌓이므로 무엇이 접수·진행 중인지 사용자가 확인할 수 있게). Task 하나가 끝날 때마다 **중간 보고**(완료 내용 + 그 항목을 뺀 진행 중 목록만 출력). 최종 보고는 중간 보고 내용을 다시 모으되, 요구가 서로 영향을 미치면 **최종 요약 보고서**로 정리한다. **요구가 겹치거나 뒤의 요구가 앞의 요구를 뒤집은 경우(충돌)** 는 최종 보고에 "충돌·조정" 절을 따로 두어 ① 무엇과 무엇이 부딪쳤는지 ② 어느 쪽을 따랐는지(기본 = 나중 지시) ③ 그 결과 달라진 것(되돌린 코드·바뀐 기본값·폐기한 설계)을 적는다(사용자 09-19 win 추가). 접수만 하고 아직 손대지 못한 요구도 목록에 '대기'로 남겨 빠뜨리지 않는다.
- **문서·커밋/푸시 규약 SSOT = [docs/16](docs/16-doc-git-conventions.md)**. 한 작업 = 한 트랜잭션 갱신(journal → DEVLOG → STATUS → MILESTONES/TODO).
- **push는 사용자 명시 요청 시에만.** `git add <파일>`만(`-A`·`.` 금지). push 전 `scripts/check-3os.sh`.
- 크레이트 경계: `nsql-core`는 의존 0 · 드라이버 크레이트는 `nsql-driver-*` 안에만 · UI는 `Box<dyn Session>`만 안다.
- ★ **확장점·부품 규칙(사용자 09-14~15)** = [30 아키텍처 패턴 원장](docs/30-architecture-patterns.md): 확장점은 **포트(trait) + 레지스트리 + 설정 선택** 세 조각(IoC = 생성자 주입·레지스트리 · 동적 배치 경계 = 프로세스/WASM/스레드) · 같은 문제를 두 번째 만나면 **부품으로 올려 §2 표에 등재** · 창·컨트롤 코드 복사 금지(WindowHost T-65) · **구현 상수는 설정 레지스트리로**(자주 안 바꾸면 `HIDDEN` · `nsql config list all`) · 새 기능은 30 §1-2 체크리스트를 먼저 본다.
- ★ **네트워크 부하 규칙(사용자 09-14)**: 앱이 스스로 만드는 트래픽 경로와 상한은 [26 §8](docs/26-performance-architecture.md) 표가 원장이다. 프로브·테스트·접속·재시도 등 **네트워크를 만드는 코드를 추가·변경하면 §8 체크리스트 6항목으로 점검하고 표를 갱신**한다 — 자동 재접속 금지 · 실패는 지수 백오프 · 동시성 상한 + 큐(`connect.max_concurrent`) · 접속한 적 없는 서버에 지속 트래픽 금지.
- ★ **부하원 등재 규칙(사용자 09-16)**: 스레드·소켓·디스크·매 프레임 재그리기·캐시를 쓰는 코드는 [39 §3](docs/39-resource-governance.md) 부하원 원장에 등재하고 **설정 키 하나 이상으로 끄거나 상한을 둘 수 있어야** 한다(`Entry.perf` · T-90a 뒤). 수치 기준은 39 §2 · 점검은 39 §6 게이트.
- ★ **실행 통제 규칙(사용자 09-18 · DR-34)**: DB로 가는 **모든 진입점은 `gate_open()`을 먼저 지난다**(막혔으면 상태줄 안내 + 아무것도 보내지 않음) · 버튼·도구줄의 활성은 `sync_gate`/`gate_view` 한 곳에서 · 세션에 딸린 상태는 `App`이 아니라 **`Sess`에** 둔다(다중 세션에서 섞이지 않게) · 분기 조건이 2개 이상이면 `sessions.rs`의 순수 함수로 뽑고 **조건별 독립 영향 쌍(MC/DC) 테스트**를 같이 넣는다([52 §12](docs/52-session-modes.md)) · 막힌 상태를 푸는 동작(■·접속 해제)만 예외.
- **실행 경로의 시간은 `nsql_core::Timeline`에 단계로 덧붙인다**(docs/26 — 각 층은 자기 단계만 · 문자열 포맷은 표시 시점에만). 결과 그리드는 속도·메모리 최우선.
- **배포 = 설치본만(DR-27)** — 포터블 모드·exe 옆 데이터 없음 · 사용자 데이터는 OS 사용자 폴더 · 새 실행파일은 목적별로(GUI/CLI/드라이버 프로세스) · [33](docs/33-distribution-and-packaging.md).
- **테스트 실행 = Debug exe · 그 전에 Release도 함께 빌드**(사용자가 `target/release`로 병행 테스트 · 09-15).
- **사용자 문자열은 전부 `nsql-i18n::Msg`**(기본 영어 · 한국어 열) — 리터럴 금지. **설정 키는 `nsql-settings::REGISTRY`에만** 추가(라벨·설명 = `Msg`).
- 스크립트 엔진을 고치면 `examples/golden-session-vars.sql`로 `nsql plan` 양 방언을 다시 본다.
- `.claude/settings.json`은 덮어쓰기 금지, 병합만.
- ★ **포커스 규칙(사용자 09-14 · 반복 버그)**: 한 창에 포커스 링은 **정확히 하나 이하**. nexa-ctl 버튼·콤보·체크박스는 MouseDown에서 스스로 `focused=true`가 되고 **스스로 끄지 않는다** → 컨트롤을 여러 개 담는 쪽(패널·창)이 **MouseDown마다 하나만 남기고 끄고**(`connect.rs own_focus`) · 창/패널이 포커스를 잃으면 전부 끈다. **컨트롤을 추가·배선할 때마다 점검**: ① 클릭 뒤 링 1개 ② 다른 컨트롤 클릭 뒤 앞 링 꺼짐 ③ 텍스트박스 클릭 뒤 버튼 링 0개 ④ Esc/창 전환 뒤 0개 — 실기 캡처로 확인하고 journal에 남긴다. ⑤ **가려지거나 교체되는 컨트롤은 일시 상태(hover·눌림·포커스)를 비운다**(`Button::clear_transient`) · 다시 보일 때 현재 커서로 hover 재판정 — 이벤트를 못 받는 동안 hover가 1.0에 멈춰 "선택된 것처럼" 남는 버그(09-14 Delete 무장).
- ★ **hover 효과 규칙(사용자 09-14)**: 마우스가 거쳐 가는 모든 대상이 이벤트를 만들어 속도를 떨어뜨리면 안 되고, **최종 위치가 아닌 대상의 효과는 즉시 취소**된다. 구현은 nexa-ctl `tokens::IntentFade` 하나로(행·콤보 항목·버튼 공통): 사건은 목표 **덮어쓰기만**(큐 0 · 비용 0) → `tick`이 70ms 머문 **마지막 목표만** 페이드에 넘김 → 목표가 바뀌면 켜지던 것은 바로 꺼짐. 속도 = 컨트롤 속성 `FadeSpeed { Fast, Slow }`(버튼·콤보 = Fast · 행 = Slow · `set_fade_speed`) → 실제 ms는 설정 `ui.fade_fast`(500)/`ui.fade_slow`(1000)에 연계 · 키보드 이동은 `jump`(즉시). 새 hover 효과를 넣을 때 자체 타이머·큐를 만들지 말고 이 부품을 쓴다.
- ★ **마우스 라우팅 규칙(사용자 09-15 · 다른 앱에서도 재발)**: 컨트롤을 여러 개 담는 창/패널은 **MouseDown/Up/휠을 커서 아래 컨트롤(`bounds().contains(p)`)에만** 전달한다 — 컨트롤이 스스로 걸러 주리라 기대하지 않는다(`prefs_win.rs route`). 컨트롤 자체의 히트 테스트도 **x·y 둘 다**(y만 보고 행을 고르던 nexa-ctl 트리 결함 = 오른쪽 클릭이 왼쪽 메뉴를 선택). 키 사건은 포커스 컨트롤에만. MouseMove는 hover용으로 넓게 보내도 된다. 새 창·패널을 만들 때 "클릭이 영역 밖 컨트롤에 닿는가"를 실기로 한 번 확인한다.
- ★ **팝업 배치 규칙(사용자 09-21 "모든 우클릭 메뉴·툴팁은 화면에서 잘리지 않게")**: 떠서 그려지는 것(우클릭 메뉴 · 하위 메뉴 · 툴팁 · 드롭다운 · HUD · 카드)은 **창(그리기 표면) 밖으로 나가면 안 된다**. ① 위치는 직접 계산하지 말고 nexa-ctl의 공용 규칙을 쓴다 — `geom::place_popup`(정방향 → 반대쪽 → 밀어 넣기) · 이미 놓인 것은 `geom::nudge_into` · 영역은 `geom::popup_host(host, ctx.surface_size())` ② 우클릭 메뉴 = `ContextMenu` · 툴팁 = `draw::draw_tooltip(_in)` · 드롭다운 = `Combo`만 쓴다(셋 다 그리는 시점에 표면 크기를 배워 스스로 들어온다 — 호출자가 영역을 잘못 넘겨도 잘리지 않는 안전망) · **글자 상자를 직접 그려 툴팁·메뉴를 흉내 내지 않는다** ③ `open_at`의 `host`에는 아는 한 **창 전체 Rect**를 넘긴다(모르는 자리만 끝없는 영역 — 그때는 주석으로 안전망에 기댄다고 적는다) ④ 새 팝업을 만들면 **창의 네 모서리 근처에서 연 캡처**를 한 번 남긴다(짧은 창 = 격리 폴더의 `window.main_size` · 기동 명령으로 메뉴를 여는 길을 같이 만든다 — 예 `result.menu`) ⑤ 팝업이 창보다 크면 잘리는 대신 **스크롤**이 있어야 한다(지금은 메뉴 항목 수를 창 높이 안으로 유지). 자세히 = [61 §2-2](docs/61-core-design-and-working-rules.md).
- ★ **팝업/메뉴 UX 규칙(사용자 09-14 "UX 관점에서 섬세하게")**: 우클릭 메뉴·입력란 편집 메뉴·콤보 드롭다운은 열려 있는 동안 모달이되, **바깥 좌/우클릭은 메뉴를 닫고 그 클릭을 그대로 진행**한다(다른 컨트롤 선택·포커스 이동·새 메뉴가 한 번의 클릭으로 · `conn_win.rs route` `outside_click`). 항목 선택·Esc는 닫기만. 팝업은 창의 **맨 마지막**에 그린다(다른 컨트롤이 덮지 않게 · `paint_popups`). 닫힌 직후 커서 아래 대상을 다시 hover 판정(`rehover`). 기능을 막거나 추가할 때는 사용자의 다음 동작이 한 번의 입력으로 이어지는지 먼저 본다.

## 4. 새 세션 오리엔테이션

1. 이 파일 + [docs/61](docs/61-core-design-and-working-rules.md)(규칙·불변식·OS별 차이) + [docs/00](docs/00-foundation-report.md) → 2. [STATUS](docs/STATUS.md) 맨 위 → 3. [TODO](docs/TODO.md)의 🔧/P1부터(지금 = **T-154 Windows 실기 → T-150 PG refcursor → T-151 → T-152 `Caps` → T-153** · 그 밖 T-145 · 순서와 고칠 자리 = [docs/61 §6](docs/61-core-design-and-working-rules.md)).
2. 새 PC면 형제 저장소 `../nexa-ui`를 **먼저** pull(path 의존) → 두 저장소 `cargo test --workspace` → [docs/61 §3](docs/61-core-design-and-working-rules.md) "맥에서 처음 할 일".
