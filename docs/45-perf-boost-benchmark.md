# 45 · 실행 속도 향상(`perf.boost`) 켬/끔 성능 비교 (1차 2026-09-17 · **2차 2026-09-22 §4** · Windows 실측)

> **요청**(사용자 09-17): *"실행속도향상을 켠 상태와 끈 상태에 대한 다양한 측면의 성능 비교 · 실 연결은 61.81.244.214:1521 · MSSQL은 등록된 SQL Server · 별도 문서에 비교표로."*
> **설계**: [39 §4-6](39-resource-governance.md) · **측정 스크립트**: `scratchpad/bench-boost.ps1`(세션 임시 · 저장소 이식 = T-90e) · 원자료 `target/bench-boost.csv`.

## 0. 결론 다섯 줄

1. **유휴 자원이 줄었다**: 유휴 10초 CPU 시간 125ms → **0ms**(캐럿 깜빡임·페이드 타이머 0) · 스레드 14 → **11** · 핸들 269 → **225** · 작업 집합 26.6 → **22.3 MB** · 사설 메모리 9.6 → **8.1 MB**.
2. **기동 시간은 같다**(창 핸들 생성까지 155 ↔ 156 ms): 기동 경로에는 향상 대상이 거의 없다(둘째 창·git·아이콘은 기동 뒤 일어남). 기동 자체의 단축은 T-102(링크·초기화 순서) 몫.
3. **질의 실행·페치 시간은 같다**(Oracle COUNT 18ms · 200행 execute 6~7ms + fetch 22ms · MSSQL 200행 37ms): 향상 모드는 설계대로 **실행 경로(왕복 수·페치 크기)를 건드리지 않는다**. 네트워크 쪽 이득은 실행이 아니라 **백그라운드 트래픽**(신호등 60s → 300s · ICMP 0 · 라이브 로그 폴링 0)에서 난다.
4. **체감 효과**는 애니메이션·페이드·툴팁·아이콘 래스터·우클릭 클립보드 읽기가 0이 되는 데서 온다 — 수치로는 유휴 CPU 0과 스레드 −3이 그 증거이고, 입력 지연은 이번 도구로 재지 않았다(T-90e).
5. 결과에 영향이 있는 키(행 수·페치 크기·타임아웃)는 의도적으로 제외했으므로 **질의 결과·시간은 두 모드에서 동일**해야 하고, 실측이 그것을 확인한다.

## 1. 환경

| 항목 | 값 |
|---|---|
| 기기 | Windows 11 Pro 10.0.26200 · 개발 PC(Ryzen/Intel 미기록 · `nexa-sys` CPU 수 미기록) |
| 빌드 | `target/release`(옵션 그대로 · LTO/strip 미적용 = T-102 전) · nexa-sql 52차 작업 트리(DR-33 · 툴바 도크 · 향상 모드 포함) |
| Oracle | `SNOPDB_19c` = BISCM@**61.81.244.214**:1521/BISCM(WAN) · `M4S_I002040` 155,316행 |
| SQL Server | `M4PLAN` = BISCM_MS@192.168.0.58:1433/M4PLAN_MS(LAN) |
| 방법 | 모드마다 `nsql config set perf.boost off/on` → GUI 3회 기동(창 핸들까지 ms · 4초 뒤 자원 · 유휴 10초 CPU) · CLI 질의 3회(`nsql run --timing`) · **중앙값** |
| 주의 | GUI 3회 중 1회는 다른 캡처 작업과 겹쳤을 수 있어 중앙값으로 본다 · 유휴 측정은 창에 포커스 없음(백그라운드) |

## 2. 비교표

### 2-1. GUI(메인 창 1 · 접속 0)

| 지표 | 끔 | 켬 | 변화 | 무엇이 바뀌었나 |
|---|---:|---:|---:|---|
| 기동 → 창 핸들 | 155 ms | 156 ms | 0 | 기동 경로엔 대상 없음(`log.open_at_start`는 이미 off) |
| 유휴 10초 CPU 시간 | 125 ms | **0 ms** | −100 % | 캐럿 깜빡임 타이머 0 · 페이드/애니메이션 0 · 프레임 상한 30 |
| 스레드 수 | 14 | **11** | −3 | git 감시 스레드 0 · 프로브 동시 1 · 아이콘 워커 0 |
| 핸들 수 | 269 | **225** | −44 | 스레드·타이머·아이콘 핸들 |
| 작업 집합 | 26.6 MB | **22.3 MB** | −16 % | 아이콘·미니맵·HTML 복사 캐시 0 · 아이콘 캐시 128 |
| 사설 메모리 | 9.6 MB | **8.1 MB** | −16 % | 위와 같음 |

### 2-2. CLI 질의(3회 중앙값 · `--timing` 단계)

| 질의 | 단계 | 끔 | 켬 | 변화 |
|---|---|---:|---:|---:|
| Oracle `SELECT COUNT(*) FROM M4S_I002040` | execute | 17.9 ms | 18.1 ms | 0 |
| Oracle `SELECT * … WHERE ROWNUM <= 200`(498 KB) | execute / fetch | 6.8 / 21.5 ms | 6.2 / 21.8 ms | 0 |
| Oracle 같은 질의 프로세스 전체(접속 포함) | wall | 227 ms | 237 ms | 0(±10 ms 잡음) |
| Oracle `WHERE PROJECT_CD='SEBANG'` 전량(무제한 · 수만 행) | wall | 9.79 s | 10.41 s | 0(±0.6 s 잡음 · WAN) |
| SQL Server `SELECT TOP 200 * FROM INFORMATION_SCHEMA.COLUMNS` | execute | 38.1 ms | 37.4 ms | 0 |
| SQL Server 프로세스 전체 | wall | 135 ms | 140 ms | 0 |

### 2-3. 향상 모드가 바꾸는 값(참고 · 26키)

렌더링 8(애니메이션 off · fps 30 · 캐럿 깜빡임 off · 페이드/슬라이드 0 · hover 의도 120ms) · 아이콘/부가 표시 7(탐색기·OS 파일·메뉴 아이콘 off · 탭 툴팁 off · 미니맵 off · 선택어 강조 off · 빈 폴더 셰브론 프로브 off) · I/O·기동 4(git off · HTML 복사 off · 우클릭 클립보드 읽기 off · 시작 시 로그 창 off) · 메모리 1(아이콘 캐시 128) · 폴링/스레드 6(프로브 300s·동시 1·재시도 1·ICMP off · Oracle 라이브 로그 off·5000ms). 제외 목록과 근거는 [39 §4-6](39-resource-governance.md).

## 3. 해석 · 다음

- 목표 ①(기동)은 이 스위치로는 안 움직인다 → **T-102**(링크 옵션 · 초기화 지연 · 폰트/설정 읽기 순서)에서 다룬다.
- 목표 ②(네트워크)는 실행 경로가 아니라 백그라운드 트래픽 감소로 나타난다(신호등 60s → 300s · ICMP 0) — 이 도구로는 안 보이고 [26 §8](26-performance-architecture.md) 표의 상한이 근거.
- 목표 ③④⑤(스레드·메모리·체감)는 수치로 확인됐다(스레드 −3 · 메모리 −16% · 유휴 CPU 0).
- **미측정**: 입력 지연·프레임 시간(체감의 직접 지표) · 접속 1개 열린 상태의 유휴(라이브 로그·프로브 스레드 차이가 여기서 커진다) · 대용량 결과(155k행) 전체 조회 뒤 메모리 회수. → T-90e(`scripts/bench-boost.ps1`로 이식 · 접속 상태 시나리오 추가). **✅ 09-22 §4에서 전부 메움**(접속·결과·큰 스크립트·패널 시나리오).

---

## 4. 2차 점검 — 2026-09-22(Windows 93차 뒤 · 사용자 *"성능향상모드에 대한 점검도 추가"*)

> 1차(§2)는 **접속 0인 빈 창**만 봤다. 그 뒤 들어온 기능(큰 파일 모드 · 변수 관리 · 결과 다중 탭 · 확장 패널 · 프로젝트 탐색기 · 토스트 진행 막대)이 향상 모드에서 실제로 꺼지는지, 그리고 **사용자가 일하는 상태**(접속·큰 스크립트·10만 행 결과·패널)에서 얼마를 버는지가 이번 질문이다.
> 방법: `scripts/win-perf-all.ps1 -Stages boost` — 규정 시나리오 7개([71 §3](71-performance-review-process.md))를 **off → on 번갈아** · Release · 격리 `NSQL_HOME` · 입력 주입 0 · 유휴 CPU는 6초 창. 원자료 `target/perf-win-2026-09-22/`.

### 4-1. 강제 키 — 26개(09-17) → **36개**(09-22) → **37개**(09-22 후반 · `project.icons`)

| 09-17 이후 들어온 강제 키 | 무엇을 끄나 | 넣은 근거 |
|---|---|---|
| `editor.diff_marks` · `log.dev_mode` | 편집마다 줄 디프 · 상세 로그 게이트 | 09-17 후반 |
| `rainbowpair.enabled` | 괄호 색 대비 계산 | 09-19 |
| `tx.block_poll_secs`=0 · `meta.refresh_secs`=0 · `file.external_poll_ms`=0 | 막힘 감지 · 탐색기 워터마크 · 외부 변경 폴링 | 79~81차(데이터 안전 기능 L1·L2와 T1·T4는 **건드리지 않는다**) |
| `meta.refresh_highlight_ms`=0 · `editor.undo_persist`=off | 갱신 강조의 잔여 프레임 · 되돌리기 기록 파일 I/O | 89차 |
| `grid.result_tabs`=off | 동시에 들고 있는 결과 수 | 89차 끝(사용자 결정 — 메모리 억제가 목적) |
| `ui.toast_progress`=off | 토스트가 떠 있는 동안 30 ms 틱 | 93차 |
| `project.icons`=off | 프로젝트 탐색기의 OS 셸 아이콘 조회(워커 스레드 + COM · 셸 이미지 리스트) | 95차 후반 · **96차 실측** [26 §7-10](26-performance-architecture.md) = 패널 시나리오 11.79 → **9.77 MB · 핸들 281 → 225 · GDI 76 → 36** — 강제 키 하나로 되돌아가는 유일한 시나리오 차이 |

**넣지 않은 것**(결과가 바뀐다 → 개별 스위치로만): `vars.signature_lookup` · `pg.refcursor_expand` · `vars.persist` · `grid.result_per_statement` · 큰 파일 단계(`file.large_*`) · `project.*`(패널을 닫으면 0이라 강제할 이유가 없다) · `ui.ime_hint`(포커스 조건부 · low 프리셋에서만 off) · `gfx.mac_present`(macOS 전용 · D-133 대기).

### 4-2. 시나리오별 A/B

| 시나리오 | Private off → on | 유휴 CPU/6초 off → on | 핸들 | GDI/USER | 스레드 | 읽는 법 |
|---|---|---|---|---|---|---|
| 1. 기동(로그인 + 메인) | 11.13 → **9.45 MB**(−15 %) | 125 → **0 ms** | 223 → 219 | 40→30 / 16→10 | 11 → 11 | 1차(9.6 → 8.1)와 같은 폭 · 캐럿 깜빡임·페이드·아이콘이 0 |
| 2. 접속(SQLite) | 9.52 → 9.54 | 0 → 0 | 225 | 33 / 12 | 13 | **차이 없음** — 접속만 한 상태는 이미 조용하다(프로브는 주기 전) |
| 3. 보조 창 4 | 12.37 → 15.65 | 0 → 0 | 229 → 233 | 53→**79** / 22→32 | 13 | ⚠ **on이 더 크다** = 회귀가 아니라 **창이 더 빨리 열린다**(슬라이드 0 · 표본 시점에 off는 3개, on은 4개가 떠 있다 — GDI 53↔79가 증거) |
| 4. 2 MB 스크립트 | 21.14 → **15.60 MB**(−26 %) | 141 → **0 ms** | 228 → 225 | 33 / 12 | 14 | **가장 큰 이득** — 미니맵·선택어 강조·줄 디프·괄호 색·캐럿 깜빡임이 한꺼번에 꺼진다 |
| 5. 결과 10만 행 × 5열 | 39.38 → 39.02 | 62 → **0 ms** | 228 → 225 | 36 / 12 | 14 | 결과 데이터는 **그대로**(향상 모드는 결과에 손대지 않는다 — 설계 확인) |
| 6. 프로젝트 패널 | 9.91 → 9.92 | 0 → 0 | 225 | 39 / 12 | 13 | 패널은 지연 열거라 **상시 비용이 이미 0** · 강제 대상이 아니다 |
| 7. 확장 패널 | 10.16 → 10.21 | **688 → 94 ms**(−86 %) | 256 | 40 / 20 | 13 | 목록이 떠 있는 동안의 애니메이션·아이콘 래스터가 CPU의 대부분이었다 |

### 4-3. 해석 · 1차와 달라진 것

1. **유휴 CPU는 어느 상태에서도 0에 수렴한다** — 1차의 "빈 창 125 → 0"이 큰 스크립트(141 → 0) · 결과(62 → 0) · 확장 패널(688 → 94)에서도 같다. 이것이 향상 모드의 본질이다.
2. **메모리 이득은 편집기가 열려 있을 때 나온다**(−5.5 MB) — 빈 창의 −1.7 MB보다 크다. 미니맵 비트맵·선택어 강조·줄 디프 기준선이 파일 크기에 비례하기 때문.
3. **스레드는 더 이상 줄지 않는다**(1차 14 → 11, 지금 11 → 11) — 그 사이 git·프로브·텍스트 보기가 *일이 있을 때만 사는* 구조로 바뀌어 기본 상태가 이미 절약적이다([39 §3-7](39-resource-governance.md) 스레드 원장).
4. **결과·행 수·타임아웃은 두 모드에서 같다**(시나리오 5) — 제외 목록([39 §4-6](39-resource-governance.md))이 지켜지고 있다는 회귀 시험 역할을 한다.
5. **시나리오 3의 "on이 더 크다"는 함정** — 창을 여는 속도가 달라 표본 시점의 창 수가 다르다. 앞으로 이 시나리오는 창마다 `@after:`로 시차를 준다(T-168).

### 4-4. 남은 것

- 입력 지연·프레임 시간의 A/B(체감의 직접 지표 · `NSQL_TRACE_FRAMES`·`win-latency-probe.ps1`를 boost 단계에 붙인다).
- macOS·Linux에서의 향상 모드 A/B(지금은 Windows만 자동화 · [71 §7](71-performance-review-process.md) 표의 빈칸).
- 실서버 질의(§2-2)는 1차에서 "차이 0"이 확인돼 이번엔 다시 재지 않았다 — 실행 경로를 건드리는 키가 추가되면 다시 잰다.
