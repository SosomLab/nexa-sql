# TODO — 순차 백로그

> ID · 우선(P0~P2) · 규모(소/중/대) · 의존 · 상태(☐/🚧/✅/⏸). 목표순.

## 1. 결정 (코드보다 먼저)
| ID | 우선 | 규모 | 항목 | 의존 | 상태 |
|---|:--:|:--:|---|---|:--:|
| **T-1** | P0 | 소 | DP-1~10 사용자 확정 → DR 승격([10 §2](10-decision-record.md)) | — | ✅ 09-12(DR-10~20) |
| **T-2** | P0 | 소 | ★ D-15~D-17 사용자 답(다음 우선순위 · 실기 OS · 캐시 위치) → DR 승격 · D-14는 DR-21 | — | 🚧 |
| **T-2b** | P0 | 소 | `integration` 워크플로 green → 실서버 결함 3건 수정 · T-4/T-5 닫힘 | — | ✅ 09-12 |
| **T-2c** | P1 | 소 | 사내 Oracle(192.168.0.58/BISCM 19c) 실접속 — `nsql conn test biscm` · `it-oracle.sql` 통과 · GUI 창 접속 (win) · ☐ mac에서도 | T-4 | ✅ 09-13(win) |

## 2. M1 — CLI 관통
| ID | 우선 | 규모 | 항목 | 의존 | 상태 |
|---|:--:|:--:|---|---|:--:|
| **T-3** | P1 | 중 | `nsql-net` — 취소 토큰(OCIBreak·TDS Attention) · 타임아웃 · SSH 터널(어댑터가 소켓 소유 → 터널·자격증명만) | — | ☐ |
| **T-4** | P0 | 대 | `nsql-driver-oracle` — ✅ 구현 · ✅ **실서버 검증**(Oracle Free 23ai · integration) · ✅ Instant Client 경로(`NSQL_ORACLE_CLIENT_DIR`)·DPI-1047 안내 · ☐ 취소(OCIBreak) | — | ✅ 09-12 |
| **T-5** | P0 | 대 | `nsql-driver-mssql` — ✅ 구현 · ✅ **실서버 검증**(SQL Server 2022 · integration) · ☐ Entra/통합 인증 · 오류 줄 보정 | — | ✅ 09-12 |
| **T-6** | P0 | 중 | `nsql run` — 파일/stdin · `&1..&n` · `WHENEVER` · 형식 출력 | — | ✅ 09-12 |
| **T-7** | P1 | 중 | `nsql shell` — ✅ 최소(줄 누적 실행) · ✅ 09-16 별칭(T-52)·SPOOL·`@@` · ☐ 줄 편집·히스토리·자동완성 | — | 🚧 |
| **T-8** | P1 | 중 | `nsql-io` export — ✅ 6형식 · ☐ 스트리밍(행 단위 fetch) · xlsx/parquet | — | 🚧 |
| **T-9** | P1 | 소 | ✅ 09-16 48차 — `SPOOL` 포트(`nsql_run::Spool` · CLI tee · APPEND/CREATE/OFF · GUI는 미지원 안내) · `@`/`@@` 상대 경로·인자·복원·`.sql` 보완·깊이 32 · 엄격 모드 `script.strict`(HIDDEN) · `SHOW <kind>` · SERVEROUTPUT 폴링은 드라이버에 이미 있음(확인) | — | ✅ |
| **T-10** | P1 | 소 | `cargo-deny` 라이선스 게이트 · `THIRD-PARTY-NOTICES` | T-4 | ☐ |

## 3. M2 — GUI 관통
| ID | 우선 | 규모 | 항목 | 의존 | 상태 |
|---|:--:|:--:|---|---|:--:|
| **T-11** | P0 | 중 | 셰이핑 스파이크(DP-6) — `rustybuzz`로 한글 조합·CJK 폭 · `nexa-gfx` 통합 | T-1 | ☐ |
| **T-12** | P0 | 대 | `nexa-edit` Buffer·Transaction·History(soft undo) — 순수 로직·테스트 | — | ☐ |
| **T-13** | P0 | 대 | `nexa-edit` Layout/View/Selections(멀티커서·컬럼) · **IME preedit 오버레이** | T-11 T-12 | ☐ |
| **T-14** | P0 | 중 | `.sublime-keymap` 파서 + context 평가기 + Command 레지스트리 | T-12 | ☐ |
| **T-15** | P0 | 대 | `nexa-grid` 가상화(10만 행 · 컬럼 리사이즈/정렬/고정 · TSV 복사) | nexa-ui U-2 | ☐ |
| **T-16** | P0 | 중 | 창 하나 관통 — ✅ 최소(TextBox·자체 그리드·워커) · **T-16b** ✅ 연결 프로필(`nsql-vault` · 09-13 DR-22) · ✅ 클립보드(09-14 · Ctrl+C/X/V · 우클릭) · ☐ 프로필 목록·삭제 UI([22 §1](22-driver-extensions.md)) · **T-16c**(✅ 09-21 — 변수 입력 창 D-137 · [63](63-variable-management.md)) 치환 변수 대화상자 · **T-16d** 세션 hot exit([18](18-session-and-projects.md)) | — | 🚧 |

## 3-1. 편집기 E1~E9([17](17-editor-incremental-plan.md)) · 세션([18](18-session-and-projects.md)) · 외부 변경([15](15-external-file-changes.md)) · 비교/git/오브젝트 캐시([19](19-compare-git-and-object-history.md))
| ID | 우선 | 규모 | 항목 | 의존 | 상태 |
|---|:--:|:--:|---|---|:--:|
| **E-1** | P0 | 대 | `nexa-edit` E1 Buffer(로프·인덱스·CP949·Transaction·History) | D-9 | ☐ |
| **E-2** | P0 | 중 | E2 Selection · E3 Commands+Keymap(Default 키맵 데이터) | E-1 | ☐ |
| **E-3** | P0 | 대 | E4 Display · E5 멀티커서 → **GUI 재정비 ①** | E-2 T-11 | ☐ |
| **S-1** | P0 | 중 | 세션 hot exit(유휴 2s/30s 원자 저장 · 복원) · 프로젝트/워크스페이스 파일 | T-16 | ☐ |
| **X-1** | P1 | 중 | 외부 변경 감지(워처·해시·fast-forward) · 비모달 배지 | S-1 | ☐ |
| **C-1** | P1 | 중 | 비교 뷰 2-pane(`similar`) · 스크립트 저장 시점 스냅샷 · git gutter | E-3 | ☐ |
| **O-1** | P1 | 대 | 오브젝트 시점 캐시(열기/컴파일 전후) · 복원 명령 | C-1 Catalog | ☐ |
| **X-2** | P2 | 대 | 3-pane 병합 | C-1 X-1 | ☐ |

## 3-2. 드라이버 확장 · 접속 대화상자([22](22-driver-extensions.md) · DR-23·24 · 사용자 09-13) — 순서 고정(프로토콜 → 보관 → 다운로드 → Oracle → GUI)
| ID | 우선 | 규모 | 항목 | 의존 | 상태 |
|---|:--:|:--:|---|---|:--:|
| **T-27** | P0 | 대 | **09-15 개정(DR-29)**: in-process cdylib C ABI(`nsql-abi` vtable+코덱 · 드라이버 `cdylib` 타깃 · 지연 `dlopen` 로더 · 외부 crate 0) — 프로세스 호스트는 같은 ABI 위 선택 모드 · 원안: `nsql-driver-rpc` — stdio JSON-RPC v1(프레임·Session 메서드·Value 인코딩) + 프록시 Session + 참조 구현 `nsql-driver-sqlite-rpc` | — | ☐ |
| **T-28** | P0 | 중 | `nsql-ext` — `drivers/<id>/<ver>/` 레이아웃 · `nexa-driver.json` · 최신 선택 규칙 · 참조 카운트 · 삭제/휴지통 · `nsql driver list/rm/prune/path` | T-27 | ☐ |
| **T-29** | P0 | 대 | GitHub 다운로드 — 색인 · 최신 태그 · HTTPS(rustls) · sha256 · Ed25519 · 원자적 설치 · `install/update/search` · 갱신 배지 · `--file` 오프라인 | T-28 D-19 | ☐ |
| **T-30** | P0 | 대 | Oracle OCI 확장 — kubo 드라이버를 RPC 프로세스로 · Instant Client 19/23 SxS · 프로필 `driver=` · ORA-28040 힌트 | T-29 D-20 | ☐ |
| **T-31** | P0 | 대 | GUI — DBeaver식 접속 대화상자 · **Golden식 로그인 리스트 = 그리드(사용자 09-14 · nexa-grid ConnectionGrid · [nexa-ui 21](../../nexa-ui/docs/21-grid-family.md))** · 드라이버 관리자(목록·Download·Update·Delete). 기본 패널은 ✅ 09-14(`connect.rs`) | T-28 · nexa-ui U-3 | 🚧 |
| **T-31b** | P1 | 중 | 접속 창 보강 — Import/Export(프로필 파일 · 비밀번호 제외 기본) · Help/Options · Read Only · Pin 열 · ~~목록 정렬/스크롤~~(✅ 09-14 14차 · 폭 조절·결합 정렬 포함) · ~~삭제 확인~~(✅ 09-14 TimeoutButton 2단) · **삭제 복구**(`.deleted/` 이동 · N일 보관 · 09-14 실수 삭제 사례) · 접속 그리드 = nexa-grid `ConnectionGrid`(G-4) | T-31 F-2 | ☐ |
| **T-62** | P1 | 소 | ✅ 09-16 49차 — Windows `.rc` + `crates/nexa-sql/build.rs`(rc.exe/windres 있을 때만 · 외부 crate 0) · macOS `.icns`(iconutil) · Linux `.desktop` + hicolor 8종 · `make-ico.py` | 릴리스 파이프라인 | ✅ |

## 3-3. 정품 인증 · 기능 게이트([23](23-license-activation.md) · 티어·서버·저장소 분리 [25](25-license-tiers-and-server.md) · 사용자 09-14) — 코드는 D-23·24·32·33 답 뒤 · **라이브러리 저장소(T-45)부터**
| ID | 우선 | 규모 | 항목 | 의존 | 상태 |
|---|:--:|:--:|---|---|:--:|
| **T-32** | P1 | 중 | `nsql-license` — 파일 형식 파서·정규화·Ed25519 검증·`Feature`/`check` · 3-OS 기기 ID · 테스트([23 §5](23-license-activation.md)) | D-24 D-26 | ☐ |
| **T-33** | P1 | 소 | `nsql license request/status/install/remove` · 종료 코드 4 | T-32 | ☐ |
| **T-34** | P1 | 중 | GUI 설정 라이선스 탭 · 상태줄 배지 · Denied 대화상자(요청 코드 복사) · i18n `license.*` | T-32 T-37 | ☐ |
| ~~T-35~~ | | | → **T-41**(`nexa-license-tool`은 서버 비공개 저장소의 두 번째 bin · [25 §9-1](25-license-tiers-and-server.md)) | | |
| **T-36** | P1 | 소 | 게이트 배선 — D-23 목록의 진입점 1곳씩 · 확장 manifest `requires` | T-32 D-23 | ☐ |
| **T-45** | P1 | 중 | ★ **`SosomLab/nexa-license` 저장소 생성**(형제 · path 의존) — format(자체 파서)·types(`Product`)·verify(**SigVerifier 포트** · ed25519 feature)·request(`NEXAREQ1`)·machine(계열 공통 기기 코드)·fs(경로 주입)·keys(ed25519·p256 루트)·테스트 픽스처 · 3-OS CI · 문자열 0(열거형만)([25 §9·§10](25-license-tiers-and-server.md)) — T-32의 검증·기기 ID·테스트는 여기로, `nsql-license`는 얇은 층 | D-24 D-26 D-40 | 🚧 09-14(저장소 생성 · 공개 · format/base32/Product 골격 + CI · 8588bc7 push) |
| **T-40** | P1 | 중 | `nexa-license` 확장 — `kind`·`machine.N`·org 키(`seats`·`seat_mode`·`server_key`) · 리스 2단 체인 검증 · `License.source` | T-45 D-32 D-33 | ☐ |
| **T-41** | P1 | 대 | **`SosomLab/nexa-license-server`(비공개)** — `nexa-licensed`(init/install/service · SQLite 좌석·리스·감사 · 최소 HTTP · named/device/concurrent · 관리 CLI+정적 페이지 · 3-OS 서비스) + `nexa-license-tool`(발급기 · 재발급 대장) | T-40 D-36~38 | ☐ |
| **T-42** | P1 | 중 | 클라이언트 리스 — `license.server`·`license.user` · 부팅 갱신·하트비트·release · 오프라인 리스 · 상태줄 배지 · `nsql license server/status/renew` | T-40 T-33 | ☐ |
| **T-44** | P2 | 소 | 관리자 설치 문서([25 §4-6](25-license-tiers-and-server.md)) · 100 동시 부하 · 만료·유예 실기 | T-41 T-42 | ☐ |
| **T-46** | P1 | 소 | 라이선스 백업·복원 — `nsql license export/import` · GUI 백업/복원 · **시작 시 알려진 위치 자동 복원**(사용자 폴더 → 기기 공용 → exe 옆) · Device는 기기 공용 폴더(`%ProgramData%` 등) · 라이선스 ID 복사([25 §11-3](25-license-tiers-and-server.md)) | T-32 | ☐ |

## 3-4. 앱 설정 · i18n · 테마(사용자 09-14)
| ID | 우선 | 규모 | 항목 | 의존 | 상태 |
|---|:--:|:--:|---|---|:--:|
| **T-37** | P0 | 중 | `nsql-i18n`(영어 기본 · 한국어) + `nsql-settings`(레지스트리 · `settings.conf`) · `nsql config` · GUI 전 문자열 · ☐ CLI 나머지(run/shell/export/conn) 문자열 카탈로그화 | — | ✅ 09-14 |
| **T-38** | P0 | 소 | 테마 System/Light/Dark(기본 System) — OS 판정 3-OS(`theme.rs`) · `Ctrl/⌘+⇧T` 순환 · `ThemeChanged` 추종 · ☐ mac/Linux 실기 | T-37 | ✅ 09-14(win) |
| **T-39** | P1 | 중 | ✅ 09-15 1차(`prefs_win.rs` · 검색 · 트리 · 카드 · Switch/Combo/TextBox · 색/단축키 보조 버튼 · 초기화 · 고급 · 즉시 반영 `apply_setting`) · 잔여 = 변경 바 애니메이션 · 키맵 카테고리 그리드 · 설정 화면 — VS Code식 좌 TOC + 검색 + 항목 카드 + 변경 바 + Reset([24 §2·§3](24-settings-and-vscode-analysis.md)) · **T-39b** 프로젝트 스코프 `.nexa/settings.conf` | 메뉴바 이식 · T-37 | 🚧 |

## 3-5. 성능 계측·경량 구조([26](26-performance-architecture.md)) · CLI 규약([27](27-cli-conventions.md)) · 사용자 09-14
| ID | 우선 | 규모 | 항목 | 의존 | 상태 |
|---|:--:|:--:|---|---|:--:|
| **T-46b** | P0 | 중 | ✅ 접속 흐름(공용 `from_parts`/`test_connection` · CLI `conn add/test` 필드 · GUI 접속 패널) · ✅ 계측 골격(`Stage/Timeline` · `--timing` · 푸터) | — | ✅ 09-14 |
| **T-47** | P1 | 소 | Send 스팬 · MSSQL/SQLite Execute·Fetch 분리 · **파일 로그 싱크**(`LogSink` · 배치 flush · 회전 · `NSQL_LOG`) · `--timing=json` | D-44 | ☐ |
| **T-54** | P1 | 중 | `session.mode` 실체 — 편집기 탭 다중화 시 per-editor 세션(워커 세션 맵 · CONNECT 범위 경고) · **한 탭의 접속 장애가 다른 탭에 번지지 않게(사용자 09-14 14차 · 워커 패닉 격리·빠른 판정은 ✅, 세션 분리는 여기)** · 로그 창 필터/지우기 ✅ 09-16 22차(종류·컬럼 · 우클릭 메뉴) · 검색 ☐ · 로그 창 Grid = nexa-grid(G-3 뒤) | E-3 D-45 | ✅ 09-18 56차 → **[52](52-session-modes.md) DR-34**(세션 분리·전용·개별·공유 N · 로그 창 검색/nexa-grid는 별도 잔여) |
| **T-48** | P0 | 대 | ✅ 09-15 1차 상한 · ✅ 09-16 25차 OFFSET 폴백·도구줄·자동 페치 · **✅ 09-16 49차 a 서버 커서 유지**(`CursorHandle` · SQLite 세션 스레드 · Oracle 소유 ResultSet · PG DECLARE CURSOR(실서버 미검증) · MSSQL 폴백 · 러너 fetch_next/all/count · Navigate 스팬 · 자동 커밋 유예) · **✅ d CLI**(`cli.max_rows` · `\more/\all/\count/\pager` · `cli.auto_more`) · **✅ b 09-17 51차** 전체 조회 스트리밍(`query_stream` + `fetch_all` 배치 진행률 · ■/Esc 취소 · 예산 수신 중 판정 · MSSQL은 일괄) · **c** MSSQL 스트림 · `db.cursor_idle_secs` 실체 · PG 실서버 | 43 D-68~72 | 🚧 |
| **T-49** | P1 | 소 | `DBMS_OUTPUT.GET_LINES` 배열 회수 · 실행 중 주기 폴링 옵션 · UNLIMITED 안내 | — | ☐ |
| **T-50** | P0 | 대 | 컬럼 지향 결과 저장소 + 페인트 할당 0 + 폭 캐시 — **nexa-grid(U-3 · dir2 `rows.rs` 이식 · [nexa-ui 21 그리드 계열](../../nexa-ui/docs/21-grid-family.md))** 와 함께 · 결과 그리드는 속도·메모리 최우선(사용자 09-14) | nexa-ui U-3 | ☐ |
| **T-51** | P1 | 중 | D-41 반영 — 옵션 파서 재정리(별칭·충돌 안내) · `-e` · `-v` · `-b` · `-W`/`NSQL_PASSWORD` · `-S host,port` · 진영별 `--help` 예시 | D-41 | ☐ |
| **T-55** | P0 | 중 | 편집기 탭 — TabBar(nexa-ctl 이식 U-2) 배선 · 탭별 버퍼 · `tabs.rows`(single ◀▶·드래그 / multi) · `tabs.tooltip`(TabInfo 카드 · 구문) · ☐ `session.mode` per-editor 마커(T-54) | U-2 T-54 | ✅ 09-14 |
| **T-57** | P0 | 대 | **인텔리전스 = 메타 저장소 한 벌**([47](47-intellisense-metadata.md) 📐 확정 09-17 · D-79~D-86 · [29 §6](29-editor-syntax-palette-statusbar.md) UI) — ⓪ 설정 `intel.*`/`meta.*` 48키 등록(47 §8 · DBeaver 카테고리 · 종속 잠금) ✅ ① `nsql-run::meta` 저장소(인터닝 · exact 해시 · 버킷 정렬/이진 탐색 · `Arc<Snapshot>` · 디프 · LRU · 09-17 자율 배치 3) ② 카탈로그 벌크 컬럼·코멘트·워터마크(방언 4) ③ 탐색기 = 저장소 뷰 ④ 인텔 워커(문맥·alias·랭킹·예산) ⑤ 팝업 컨트롤 ⑥ hover 카드 ⑦ 디스크 캐시 ⑧ CLI Tab · 시스템 오브젝트 패키지 | T-56 E-2 28 | 🚧 |
| **T-58** | P0 | 대 | **다중 커서·정규식 찾기**([29 §7](29-editor-syntax-palette-statusbar.md)) — 리전 벡터(정렬·병합) · `Ctrl+D`/`Alt+F3`/`Ctrl+⇧L`/컬럼 선택 · 찾기 패널 정규식 → `Alt+Enter` 전부 선택 · `Ctrl+H` `$1` · 뒤→앞 적용 · 트랜잭션 1 = Undo 1 | E-2 T-59 | ☐ |
| ~~T-59~~ | — | — | ~~자체 정규식 엔진(Thompson NFA)~~ → **D-76 `regex` + `fancy-regex`로 대체(✅ 09-16 34차 · `rx.rs`)** · 잔여 = `.sublime-syntax` 컨텍스트 호환 · 강조 행 시작 상태 캐시(큰 파일) | — | ☐ |
| **T-60** | P2 | 소 | 서식 복사 — Linux 다중 형식(자체 Wayland/X11 클립보드 소유) · macOS osascript 실기 · 잘라내기 서식 옵션 | — | ☐ |
| **T-61** | P1 | 소 | 상태줄 — `statusbar.*` 표시 설정 · 세션 모드/Read-only 배지 · 인코딩·줄끝·탭 크기 세그먼트 · 클릭 동작(Goto line · 로그 창) · 좁을 때 왼쪽부터 숨김 | T-39 T-54 | ☐ |
| **T-56** | P0 | 대 | ✅ 09-15 1차(`nsql-catalog` 5방언 · `nsql cat` · DESC · `explorer.rs` 메타 세션 스레드 · 지연 1단계 · 노드별 ⚠ · 소스/SELECT 새 탭 · 우클릭) · 잔여 = 툴팁 카드 · 스레드 풀 ≤4 · 취소 · 자동 갱신 · nexa-grid `CatalogSource` · 필터 상자 · **오브젝트 탐색기**([28](28-object-explorer.md)) — `Catalog` 포트 · Oracle/MSSQL/SQLite `children` · `nsql-run::explorer`(메타 세션 · 스레드 풀 ≤4 · 노드 상태 · 캐시 · 취소 · catch_unwind) · `nsql cat` · 트리 그리드(nexa-grid `CatalogSource`) · 설정 4키(등재 ✅) · 자동 갱신(기본 off) | nexa-ui G-1~3 · D-46 | 🚧 |
| **T-53** | P1 | 중 | ✅ 09-15 1차(`keymap.rs` Sublime win/mac 기본 · `key.*` 20키 · `keys_win.rs` 캡처 창 · 충돌 배지 · Ctrl+Shift+E) · 잔여 = 검색 · 그룹 · **단축키 설정 화면** — KeymapGrid(그룹·이름·설명·단축키 · [지정]/[초기화] · 충돌 배지 · 검색) + `HotkeyCapture` 캡처 대화상자(적용 → 그리드 값·키맵 파일) — [nexa-ui 21 §3-1](../../nexa-ui/docs/21-grid-family.md) · 명령 레지스트리는 E3(T-14) | nexa-ui G-6 · T-14 | 🚧 |
| **T-63** | P2 | 소 | 프로브 부하 원칙(09-14 검토 · [26 §8](26-performance-architecture.md)) — ✅ 대상 집합 밖 항목 재예약 금지 · ✅ 슬롯 초과 깨우기 1s · ✅ Test/Connect 동시 상한+큐(`connect.max_concurrent`) · ☐ 호스트명 DNS 캐시(프로필 수백 개 대비) | 14차 검토 | 🚧 |
| **T-65** | P1 | 중 | **WindowHost** 부품 — 창 3개(접속·로그·색)가 복사한 softbuffer/RasterCtx/입력 변환/틱/페인트 프레임을 한 타입으로(nexa-clip 창 골격 이식) · 창은 `layout/route/paint`만 | [30 §3-A](30-architecture-patterns.md) | ☐ |
| **T-66** | P2 | 소 | 편집기·그리드·로그 창 상수 설정화(`EditorTuning/GridTuning` · 비노출) — 접속 창 `ConnTuning`과 같은 방식 | [30 §3-D](30-architecture-patterns.md) | ☐ |
| **T-67** | P1 | 중 | 🚧 09-15 씨앗 = `keymap::COMMANDS`(id·라벨·기본 코드) · 잔여 = 메뉴·팔레트·툴바가 같은 표를 읽기 · 핸들러 · **Command 레지스트리**(id · 라벨 Msg · 기본 단축키 · 핸들러) — 메뉴·팔레트·툴바·키맵(T-53)이 같은 표를 읽는다 | [30 §3-E](30-architecture-patterns.md) T-53 | 🚧 |
| **T-68** | P2 | 소 | 실행 계층 메시지 규약 문서화(`Cmd/ConnOutcome/RunEvent/TestResult` · 버전 · 큐/스레드 소유권 표) — RPC 드라이버 규약(T-27)과 정렬 | [30 §3-F](30-architecture-patterns.md) | ☐ |
| **T-69** | P1 | 중 | ✅ 09-15 19차 탭별 계층(상태줄 팝업 = 활성 탭 · 붙여넣기 탭→공백 · 탭 폭 렌더 탭마다) · ✅ 09-15 1차 전역 계층(`editor.tab_size`/`indent_spaces` · nexa-gfx 탭 폭 주입 · Tab=공백 · 변환 · 상태줄 세그먼트+팝업) · 잔여 = 문법/프로젝트/탭별 계층 · 감지 · Tab/Shift+Tab 줄 들여쓰기 · **들여쓰기 설정 계층**([31](31-indentation-settings.md)) — 전역 → 문법(`<Syntax>.nexa-settings`) → 프로젝트 → 현재 탭(메모리·세션) · 상태바 `Tab Size: 4`/`Spaces: 4` 세그먼트 + 팝업(공백/탭 · 폭 1~8 · 버퍼에서 감지 · 변환 · 문법/전역 기본으로 저장) · nexa-gfx 탭 폭 주입(4칸 고정 제거) · Tab/Shift+Tab 줄 들여쓰기 | 29 T-61 T-67 | 🚧 |
| **T-70** | P1 | 소 | PostgreSQL TLS(`sslmode=prefer/require` · rustls) · `postgres-rustls` 원장 · 접속 폼 SSL 토글 | nsql-driver-pg | ☐ |
| **T-71** | P1 | 중 | ✅ 09-15 1차(`oracle.live.*` · V$SESSION/로그 테이블 폴링 · 로그 창 `[live]`) · 잔여 = CLI `--live` · Live 세그먼트 · DBMS_PIPE/LONGOPS · **Oracle 라이브 로그 모니터**([32 §2](32-server-messages-and-live-log.md)) — 메타 세션 폴링(로그 테이블 · V$SESSION client_info/action · DBMS_PIPE · LONGOPS) · 로그 창 Live 세그먼트 · `oracle.live.*` 설정 · CLI `--live` · 실행 중에만 1s 이상 | 28 T-56 D-48 | 🚧 |
| **T-72** | P1 | 대 | ✅ 09-16 49차(DR-32 · 보조 에이전트) — `packaging/{macos,windows,linux}` + `lib.sh` · macOS `.app` Universal 2/pkg(postinstall `/usr/local/bin/nsql` 링크)/dmg 실기 생성 · WiX v4 MSI · deb/rpm · `release.yml`(태그 → 3-OS → 설치 스모크 → sha256 → Release 초안) · THIRD-PARTY-NOTICES 초안. 잔여 = 서명 키(DR-20) · winget/choco/brew 매니페스트 · zig glibc 2.17 · arm64 매트릭스 · 동봉 Packages 로더 · T-10 | T-62 T-10 DR-32 | ✅ |
| **T-73** | P1 | 중 | ✅ 09-15 1차(`findbar.rs` · Ctrl+F/H · F3 · Aa · Replace/All · 순환) · ✅ 정규식(D-76 34차) · ✅ 단어 단위 · ✅ 09-16 48차 **일치 전부 표시**(`set_find_marks` 반투명 · 열린 동안 갱신) — 편집기 **찾기/바꾸기** 완료 | T-58 T-59 | ✅ |
| **T-74** | P1 | 중 | ✅ 09-15 20차 1차(Open/Save/Save As · 최근 파일 · 끌어놓기 · `*` 더러움 · 닫기 2단 · CRLF) — 잔여 = 외부 변경 X-1(docs/15) · 프로젝트 폴더 · export 경로 · **파일 열기/저장**(File ▸ Open/Save/Save As · 최근 파일 · 외부 변경 X-1) — nexa-ui 자체 파일 대화상자([nexa-ui 20](../../nexa-ui/docs/20-file-management-and-dialogs.md) F-6) 선행 | nexa-ui 20 | 🚧 |
| **T-75** | P2 | 소 | 그리드 복사 확장 — Copy as JSON/Markdown · 컬럼 헤더 우클릭(컬럼 복사·숨김) · 셀 편집기(nexa-ui 21 §3-2) | 09-15 3차 | ☐ |
| **T-76** | P2 | 소 | 🚧 09-16 1차(builtin = 편집기 탭 + 저장 감시 반영) · 잔여 = **settings.json 내장 편집기**(`settings.json_editor = builtin`) — 편집기 탭에서 열고 저장(T-74 파일 저장) → 같은 감시 경로로 반영 · JSON 구문 강조 패키지 · 스키마 힌트(레지스트리 라벨/허용값) | T-74 T-17 | ☐ |
| **T-77** | P1 | 대 | ✅ 09-16 49차 1차(**공유 세션** · DR-30) — 대기 목록 + 탭 배지 `●n`/`⚠n` · 상태줄 세그먼트+팝업(모드 전환·Commit(n)·Rollback(n)·목록) · 툴바 Commit/Rollback(Material · 활성/색) · 닫기/해제/종료/전환 팝업 · Oracle/MySQL 암묵 커밋 · `tx.stale_min`·`tx.close_action`·`tx.badge`·`tx.smart_commit`. 잔여 = 탭별 세션(T-54) · 프로필 `autocommit=` · `nsql shell` `*n` · 트랜잭션 로그 창 필터 | T-54 T-55 T-61 | 🚧 |
| **T-64** | P2 | 중 | 설정 화면(T-39)에 색 선택기 연동 — `ColorPanel`을 hover/눌림 외 테마 주요 색에도 · `ui.fade_fast/slow` · `probe.*` · `input.scroll_natural` 노출 | T-39 | ☐ |
| **T-52** | P2 | 소 | ✅ 09-16 48차 — 셸 별칭 `\d \dt .tables \dv \d <n> .schema \i .read \c \?`(→ SHOW/DESC/@/CONNECT · `\?` 표 · `shell --help`) · [40 Step 11](40-cli-usage.md) | T-7 | ✅ |

## 4. M3+ (요약)
syntect `.sublime-syntax`(T-17) · 컬러스킴/스니펫/완성(T-18) · `Default` 패키지(T-19) · `Catalog` 포트 + 오브젝트 브라우저(T-20) · import/bulk 6방언(T-21) · 데이터 편집기 변경 SQL 미리보기(T-22) · MySQL/ODBC 드라이버(T-23 · PG ✅ 09-15 `nsql-driver-pg` · SQLite ✅) · WASM 플러그인 API(T-24) · 릴리스 파이프라인·서명(T-25) · 라이선스 키(T-26 → **T-32~36** [23](23-license-activation.md)).
| **T-78** | P1 | 중 | 구문 토큰 종류 확장(자료형·함수·바인드·구분자·명령) + 색 프리셋 `editor.color_preset`(nexa/dbeaver/golden · [35](35-editor-colors-reference.md)) · 선택 반투명 옵션(D-54) | D-53 D-54 | ☐ |
| **T-79** | P1 | 중 | 파일 인코딩 — 열기/저장 대화상자 하단 인코딩·줄끝 콤보(Auto/UTF-8/UTF-8 BOM/UTF-16/EUC-KR·CP949 표 내장) · 탭별 인코딩 기억 · 상태줄 세그먼트 · 3-OS 동일(자체 대화상자) | T-74 | ☐ |
| **T-80** | P2 | 소 | 그리드 SQL 복사의 키 = PK(카탈로그 `nsql-catalog` 연동 · 없으면 첫 컬럼) · 테이블 추정 개선(별칭·스키마) | T-56 | ☐ |
| **T-81** | P1 | 대 | **파일 검색 탭 + 프로젝트**([36](36-find-in-files-and-project.md)) — **✅ 09-16 49차 b `nsql-search` 엔진 + `nsql grep`**(병렬 열거 · 무시 규칙 · SWAR · 스트리밍 · 취소 · search_text · 28 테스트 · 21k파일 0.3~0.6s) · **✅ 09-17 50차 a 패널**(`search_panel.rs` · Ctrl+⇧F · Where · 열린 탭+폴더 스트리밍 · 결과 트리 · 클릭 이동 · 설정 4키) · 잔여 = c 프로젝트 파일 · d 바꾸기 미리보기/적용 · e 캐시·EUC-KR | 22차 활동 막대 · T-74 · T-59 | 🚧 |
| **T-82** | P0 | 소 | 트리 `rows()` 평탄화 캐시([37 P-1](37-file-picker-performance.md)) | — | ✅ 09-15 |
| **T-83** | P0 | 소 | 트리·탐색기 페인트를 첫 가시 행부터([37 P-2](37-file-picker-performance.md)) — 탐색기는 이미 `skip(first)`+행 캐시 · nexa-ctl TreeView/TreeGrid ✅ | — | ✅ 09-15 |
| **T-84** | P1 | 소 | `nexa-fs::entry_of` — 링크일 때만 경로 stat(53× · [37 P-3](37-file-picker-performance.md)) | — | ✅ 09-15 |
| **T-85** | P2 | 중 | 배치 도착마다 전체 재구성 완화([37 P-4](37-file-picker-performance.md)) — ✅ 150ms 간격 · 잔여 = `refresh_grid` 사본 제거(증분 append) | T-82 | 🚧 |
| **T-86** | P2 | 대 | `nexa-ctl/controls/file/*` 추출 — 20 §1 계층 복원([37 P-5](37-file-picker-performance.md)) = F-3 | — | ☐ |
| **T-87** | P2 | 소 | 우클릭 메뉴 항목 앞 아이콘(사용자 09-16 요청 · **대상 메뉴 미확인** — 편집기 탭/상태줄 Tab/접속 창 목록/결과 그리드 중) · 파일 대화상자 메뉴 방식(`CtxItem::with_icon`) | — | ⏳ 사용자 |
| **T-88** | P1 | 소 | **Windows 재검증**(09-16 맥에서 고친 것): 잉크 기준 세로 정렬(`text_center_y`) · 스플리터 · Material 아이콘 · 메뉴 배율 · 툴팁 다중 행 — 맥 세션 후반은 캡처를 못 봐 빌드/테스트로만 확인 | — | ☐ |
| **T-89** | P1 | 소 | ✅ 09-16 **줄끝 정책**([38](38-line-endings.md)) — `eol.rs`(다수결 판정·정규화·정책) · `file.eol_new`/`file.eol_save` · 상태줄 LF/CRLF 세그먼트+팝업 · 줄끝 변경 = 더러움. ✅ 09-16 48차 Edit ▸ 줄끝 3종 메뉴 · 잔여 = 다른 이름으로 저장 콤보(T-79) | — | ✅ |
| **T-90** | P1 | 대 | **자원 거버넌스**([39](39-resource-governance.md) · DR-31) — **✅ 09-16 49차 a** 레지스트리 `perf.mode`·`PERF` 원장 24키·`Settings::effective`·`nsql config list perf`·`nexa-sys`(D-60) · **✅ d** 상한 세터 4(log.max_lines · undo_max · glyph_cache · icon_cache) + main.rs 배선 · **✅ 09-17 52차 `perf.boost` 실행 속도 향상**(39 §4-6 · 강제+잠금 · `ui.menu_icons`/`ui.clipboard_probe`) · 잔여 = 상태줄 ⚡ · 설정 창 Performance 카드 · `nsql --perf` · **b** DB(`db.statement_timeout` cancel 포트 · 탐색기 숨김 = 세션 없음) · **c** CPU/GFX(`editor.highlight_max_kb` · `max_occurrences` · `ui.max_fps` · `ui.animations` · `caret_blink` · `file.os_icons` · `probe_chevrons` 배선) · **e** 진단·게이트 · **f** 후보 | DR-31 | 🚧 |
| **T-91** | P2 | 소 | 편집기 페인트의 O(본문) 항목 — `content_w` 전 줄 측정 · `logical_lines` 사본 · 강조 상태를 첫 가시 행까지 매 프레임 전달 → 변경 시에만 재계산(캐시) · [39 §3-3](39-resource-governance.md) 등재 | — | ☐ |
| **T-93** | P1 | 중 | ✅ 09-16 49차 — `results.rs` `ResultPanel{tabs}`(편집기 탭 쌍 · 활성 그리드만 그림 · swap) · Ctrl+\\ 새 결과 탭 · TabBar 단일 행(auto/always) · 우클릭(닫기/다른/오른쪽/고정/맨 앞·뒤/첫·마지막) · 상한 8 + 자동 정리 · 끄기 · **메모리 예산 D-72** · 설정 5키. 잔여 = 탭 이름 바꾸기 · Export 항목 · 커서 close 연동(T-48a 뒤) | T-48a T-48b | ✅ |
| **T-99** | P2 | 중 | ✅ 09-16 42차 **글리프 오토힌트 근사**(nexa-gfx `set_text_hint` · `ui.text_hint`) · ✅ 44차 잔여(곡선/대각선 피팅) = **Windows GDI 글리프 경로**(`ui.text_gdi` · D-77)로 해소 | 31차 | ✅ |
| **T-100** | P3 | 중 | ✅ 09-17 55차 **macOS CoreText**(nexa-gfx `coretext.rs` · 기본 켬 `OS_DEFAULTS`) · 잔여 = Linux FreeType · **macOS/Linux OS 래스터 경로**(CoreText `CTFontDrawGlyphs`/FreeType 힌팅) — D-77의 Windows GDI와 같은 포트(`Font::set_face_family` + `set_text_gdi`류 전역) · 내장 오토힌트로 충분하면 보류 | 44차 | ☐ |
| **T-102** | P1 | 대 | **최적화 패스**(사용자 09-17 · 전체 개발 뒤): 실행 속도 · 실행 파일 크기(release 프로필 · strip · LTO · 의존 감사) · 라이브러리 정적/동적 분리(33 §1) · 로딩 속도 · 점유 메모리 · 실행 후 회수(잠든 탭·캐시 해제) · **텍스트 보기 창 단위 렌더**(파생 `text_lines` 전체 실체화 → 보이는 블록만) · **결과 저장 컬럼형/아레나**(DR-33 후속 · 셀당 32B+힙 문자열) · `nsql-run::fetch_all` 세그먼트 스트리밍 — 기준 = [26](26-performance-architecture.md)·[39 §2](39-resource-governance.md) · **3-OS 점검**(리눅스 glibc/동적 lib 버전 실행 불가 전례 → 정적 링크·최소 glibc 명시 · Windows CRT · macOS 최소 버전) · 코드 수정 포함 · **09-22 Linux(91차)**: ✅ 글꼴 가족 탐색 걷기 1회 캐시(nexa-font) · ✅ 아이콘 래스터 `memo` + 찾기 막대 지연 · `[startup]` 구간 계측 · 남음 = 시작 시 보이는 도구줄 아이콘(개당 ≈15 ms · SVG 표본마다 다각형 전체 검사 → 스캔라인/캐시 파일) · 10만 행 재실행 시 옛 결과를 먼저 놓기(힙 조각 8 MB) · Linux 기동 = 111 ms(VM · 예산 300 ms 안) | 26 39 33 | ☐ |
| **T-103** | P1 | 중 | **위키 + 맥 캡처 매뉴얼**(사용자 09-17): ✅ 1차 `examples/demo.sql` · `scripts/mac-capture.sh`(NSQL_HOME 격리 · PID 지정 · 9장) · ✅ 09-17 52차 **Demo 프로필 자동 생성**(내장 스크립트 · 최초 1회 팝업 · 도움말 메뉴 · [21 §5](21-connection-profiles.md)) · 잔여 = 스크립트 보완 3건(클립보드 타이핑 · 창 제목 캡처 · 인자 접속 탐색기 결함 수정) → 재캡처 → `docs/wiki/*.md` 10장 → `scripts/wiki-publish.sh` → wiki push | 40 | 🚧 |
| **T-104** | P2 | 소 | ✅ 09-17 52차 인자 접속 = Connect 버튼 경로(`last_spec` · 탐색기 접속 · `-c`/`--fill`/`--help` · [21 §6](21-connection-profiles.md)) · 잔여 = AppleScript 고속 keystroke 글자 유실 점검 | T-103 | 🚧 |
| **T-105** | P2 | 중 | **툴바 편집 UI**(사용자 09-17 "편집 기능은 할일에만"): 그룹 안 아이콘·구분자의 **표시 여부·순서** 편집(설정 창 카드 또는 우클릭 ▸ 편집…) · 플로팅 창을 도크 위로 끌어 붙이기 · 세로 도크 · 그룹 추가/이름 — 모델은 nexa-ctl `ToolGroup.items` 계층 그대로 · 저장은 `toolbar.layout` 확장 | 52차 도크 | ☐ |
| **T-106** | P1 | 중 | **포터블 배포 모드**(D-78 · 사용자 09-17): exe 옆 `data\` 감지 → `NSQL_HOME` · zip 채널(`packaging/`) · 기기 키/라이선스 기기 ID 정책 · 33 §0 개정 · DR 승격 | D-78 33 23 | ☐ |
| **T-107** | P1 | 중 | ✅ 09-17 52차 — **트랜잭션 로그 창**([44](44-transaction-log.md)): nsql-run `txlog`(TxEntry/TxRecord/TxLog 링·필터·`outcome_of` 표시 시점) · `txlog_win.rs` 모덜리스(검색 · 7열 · Tx 열 = 대기/커밋됨/롤백됨/암묵/자동/끊김 · 스위치 3 · 취소선) · 호스트 `tx_close(outcome)` · `txlog.max_entries`. **잔여(T-107b)**: `tx_pending` 원천을 TxLog로 교체 · ✅ 우클릭(복사·편집기로 · 09-17) · 중지(T-108) 연결 · 서버 롤백 판정(44 §4) | T-108 34 39 | ✅ |
| **T-108** | P1 | 대 | **실행 중 상태·중지**(사용자 09-17): ✅ 09-17 ① `Session::cancel_handle` 포트 + SQLite/PG/Oracle 구현 + 워커 `cancel_run` + 툴바/카드 ■ + "중지됨" 판정 · ✅ 배지 · ✅ 실행 상태 카드. **잔여**: ✅ 편집기 탭 실행 중 표시(▶ · 09-17) · 중지 시점까지 받은 행 표시(행 단위 · `ExecResult.interrupted` · 지금은 결과 비움) · ✅ MSSQL 취소(`mssql.encrypt=login` = Attention 세션 유지 · `required` = 소켓 종료+재접속) · 후속 = tiberius 포크로 TLS 안 Attention · 트랜잭션 로그 중지 결과 연결 · 카드 라이브 단계 이벤트 | 26 43 44 | 🚧 |
| **T-90e** | P2 | 소 | ✅ 09-17 `scripts/bench-boost.ps1` 이식 · 잔여 = · 접속 1개 열린 유휴 · 입력 지연/프레임 시간 · 155k행 전체 조회 뒤 회수 · [45](45-perf-boost-benchmark.md) 갱신 | 39 45 | ☐ |
| **T-110** | P1 | 중 | **미니맵 표시 확장**([46](46-minimap-features.md) · 사용자 09-17): ✅ 1순위(찾기 띠 · 오류 줄 · 뷰포트 always/hover+테두리 · 클릭 center/text · 09-17 자율 배치) → 2순위 = View 토글·키맵 `view.minimap` · 현재 문장 범위 · git 변경 줄 · 수정 줄 · 좁은 창 숨김 → 3순위 = 자동 숨김 페이드 · 배율 · 왼쪽 · 섹션 헤더. 구현 = nexa-ctl `MinimapMark` 한 벌 + `set_minimap_marks(kind, ranges)` | 46 39 | 🚧 |
| **T-112** | **P3(최하위)** | 중 | **실행 대기열**([43 §8](43-fetch-model-and-result-tabs.md) 설계 · 사용자 09-17): 편집기 실행 중 재실행 = 지금은 막힘(✅) → 나중에 큐(실행 1 + 대기 2 · 카드 3장 · 대기 취소 × · 취소 시 뒤 카드 당김 · 끝나면 다음을 **새 결과 탭(Ctrl+\\ 경로)**로 자동 실행 · 일괄 취소는 실행 중 제외 · 대기열 관리 팝업 · 설정 `run.queue_max`/`queue_new_tab`) | T-108 43 44 | ☐ |
| **T-115** | P2 | 소 | **Auto Indent 후속**([49 §5](49-auto-indent.md)): `bracketIndentNextLinePattern` · ✅ 줄 주석 뒤 제외(09-17) · `unIndentedLinePattern`(주석 전용 줄) · 문자열/주석 안 괄호 무시(강조 토큰) · 붙여넣기 재정렬(`reindent`) · `.tmPreferences` 읽기 · 다중 캐럿 증가 규칙 | 49 | ☐ |
| **T-116** | P2 | 소 | **일관성 후속**([43 §9-3](43-fetch-model-and-result-tabs.md)): ✅ `strict_all`(09-17) · 겹침 검사(offset−1 행 비교) 옵션 · CLI `\\more` 같은 규칙 | 43 | ☐ |
| **T-117** | P3 | 소 | 줄 변경 표시 후속(09-17): 기준선 = git HEAD 선택(Sublime `mini_diff: auto`) · 띠 클릭 → 그 줄 되돌리기/디프 팝업 · LCS 상한 초과 시 Myers/patience | 29 | ☐ |
| **T-118** | P1 | 대 | **확장 시스템**([50](50-extension-system.md) · D-87~D-90 결정 뒤): ① 매니저 **✅ 1차 09-17 맥 54차**(`extensions/manager.rs` · 저장소 = 이 저장소 `extensions/` 폴더(index.json + 폴더별 extension.json · README 명세 v1) · 팔레트 Sublime식 8명령 · sha256 검증 · SxS · data 설치/되감기 · 켜기/끄기 · 설정 그룹 Extensions + 분류 숨김 · 원격 = curl) · 잔여 = 서명 인덱스 · Upgrade 명령 · `requires` 자동 설치 · **동적 설정 등록**(extension.json `settings[]` → 레지스트리) · 잠든 탭 쌍 표 해제 · 성능 게이트 50 §11 측정 ② **✅ 09-23 97차(win) WASM 런타임**([75](75-extension-sdk-and-dynamic-loading.md) · `wasmi` 1.1 · `extensions/wasm.rs` 연료/메모리/시간/브레이커 · op 큐 · SDK `extensions/sdk/nexa-ext-sdk`(ABI v1 · 의존 0) · 샘플 `hello-ext`·`rainbow-pairs`(76 KB · 공식 패키지 kind=wasm 1.1.0 = 내장 대체·폴백) · 매니저 wasm 설치 + `files[].url`(Releases 자산) · `scripts/ext-build.*` · 시험 3 · 잔여 = 75 §9(설정 스키마 동적 등록 · 동적 명령 라벨/키맵 · dev_dir 핫 리로드 · 서명 D-89 · 능력 승인 D-90 · wasm-opt) ③ 결과 후처리·포매터 훅 ④ (선택) 프로세스 플러그인 + Python 러너 + 3-OS 샌드박스 | 22 09 30 | 📐 |
| **T-119** | P1 | 중 | ✅ 09-17 맥 54차 — **Rainbow Pairs 배선**([51 §11](51-rainbow-brackets.md)): `App.extensions` Registry · `rainbowpair.*` → 전 탭 `set_bracket_opts` · 키맵 Ctrl+Alt+, . [ ] · 편집 메뉴/우클릭 "괄호 이동 ▸" · 팔레트 · 끄기 = 효과 off. 잔여 = 실기 · 29/24 문서 표 · 색 목록 설정 창(색 창 재사용) → WASM export(T-118 ②) | 51 | ✅ |
| **T-120** | P3 | 소 | `scripts/check-3os.sh` 교차 타깃이 이 Windows 기기에서 실패(`ring` 빌드에 `x86_64-linux-gnu-gcc` 필요 · 환경 문제 · 09-17): `cargo-zigbuild` 또는 `cross` 도입 · 아니면 교차 빌드는 CI에만 두고 로컬은 `--quick` | — | ☐ |
| **T-121** | P1 | 중 | 🚧 09-18 **서버별 탐색기 ✅**([52 §2-2](52-session-modes.md) · `ExplorerSet` · 세션 ≥1이면 유지 · 0이면 오프라인 · 메타 유휴 회수 · 머리줄 서버 목록) — 잔여: `MetaStore` 인텔리센스를 서버 키로 묶기 · 오프라인 메타 LRU `meta.cache_mb` · 노드 우클릭 "이 서버로 새 탭" · 접속 창 다중 "접속됨" · `tab_bind` 세션 복원 | T-57 | 🚧 |
| **T-122** | P1 | 소 | 문지기 우회 불가화 — `worker.send` 직접 호출을 `Sess` 메서드 뒤로 · [30 §1-2](30-architecture-patterns.md) 체크리스트에 "DB로 가는 새 진입점 = `gate_open()`" · 호스트에 남은 분기 3개(52 §12 ②④⑤) 순수 함수화 + MC/DC | — | ☐ |
| **T-123** | P2 | 소 | ✅ 09-18 세션 상태 추적(`alters_session_state` → 유휴 닫기 제외 · 세션이 바뀌면 1회 안내 · [52 §6-4](52-session-modes.md)) — 잔여: 자동 커밋의 Oracle GTT(`PRESERVE ROWS`) INSERT 감지(카탈로그 필요) | — | 🚧 |
| **T-124** | P2 | 소 | ✅ 09-18 트랜잭션 로그 **세션 열**(전 세션 합본 · 세션별 트랜잭션 격리) — 잔여: 실행 로그 창 세션 태그 · 세션 필터 UI | — | 🚧 |
| **T-125** | P0 | 소 | ⏳ **실기 점검 52 §13 ①~⑳**(Windows·mac) — 통제 흐림/복귀 · `CONNECT`/`DISCONNECT` · 탭 표식 메뉴 · 개별 모드 · 유휴 닫기 · 공유 N · Disconnect ▾ · 로그인 창 확장(목록 크기 유지) · 텍스트박스 앞부분 표시/스크롤 표시 자동 숨김 · **57차**: 탐색기 서버 루트 이어 붙이기(공용 스크롤·키보드 이동·오프라인 제거) · 로그인 목록 경계 끌기/더블클릭 자동 맞춤 · 새 기본 크기 · `examples/oracle-refcursor-pkg.sql` 실행(`PRINT rc`) · **60차**: 표식 메뉴(공유 목록/미연결/이 탭 접속) · 세션 관리자 ▾ · Disconnect 본체 = 모두 해제 · 새 탭 규칙(개별 = 접속 창) · 같은 서버 CONNECT 옵션 · 표식 항상 · **59차**: VPN 끊긴 채 F5 → 몇 초 안에 "서버에 닿지 않음" + 플러그 빨강 + 표식 끊김 · VPN 복구 뒤 F5 → 재접속 로그 + 결과 · 실행 중 끊김 → ■ = 즉시 중지 | — | ☐ |
| **T-126** | P3 | 소 | CLI `nsql shell` — 세션 1개 전제 확인 · [27 §6 ④](27-cli-conventions.md) 문구를 DR-34에 맞춤(GUI = 전용 세션 · CLI = 세션 교체) | — | ☐ |
| **T-127** | P1 | 중 | ✅ 09-18 59차 접속 생존([53 §8](53-connection-liveness.md)) — 동작 직전 판정(`live_plan` · Run/Fetch/Count/Keys/Commit · 접속 전에도) · `Sess.broken` + 표식/플러그 빨강/목록/상태줄 · Broken 중지 = 워커 교체 · 유휴 닫기도 워커 교체 | — | ✅ |
| **T-128** | P1 | 소 | ✅ 09-18 `Session::is_alive()`(Oracle `status()` · PG `is_closed()`) · `session.call_timeout_secs` — 잔여: MSSQL read timeout | — | 🚧 |
| **T-129** | P2 | 소 | ✅ 09-18 TCP keepalive(PG · MSSQL `socket2`) `net.keepalive_secs` · 26 §8·39 §3 등재 — Oracle은 `status()`로 대신 | — | ✅ |
| **T-130** | P2 | 소 | `nexa-sys` 네트워크 경로 변경 신호 → 다음 동작 때 반드시 판정 | D-113 | ☐ |
| **T-131** | P1 | 중 | ✅ 09-19 접속 폼 바뀜 표시 + Save 유도([22 §9](22-driver-extensions.md)) — 스냅숏 · 칸 왼쪽 띠 + 라벨 `•` · `Save •` 강조 · 상태줄 미저장 · 목록 `*` · 잃는 순간(불러오기·New·닫기·Esc) 저장/버림/취소 · `dirty_fields` MC/DC 3 · nexa-ctl `set_modified`/`ButtonTone::Accent` — 잔여: 라벨 `•` 클릭 = 그 칸만 되돌리기 · 폼 우클릭 "변경 취소" · 삭제 때 지킴이 | — | 🚧 |
| **T-132** | P1 | 중 | 비밀번호를 접속 문자열에 쓰지 않는 길([52 §4](52-session-modes.md) · 09-19 자격 빌리기 제거의 후속): ① 편집기 변수(`DEFINE`/`:pw` · 값은 세션 메모리만 · 로그/이력에 남기지 않음) ② `CONNECT`에 비밀번호가 없으면 **모달 비밀번호 입력**(잃는 순간 규칙과 같은 팝업 · 저장 안 함) ③ CLI `--password-stdin`/프롬프트 · 로그인 창 세션 비밀번호(`session_pw`)와 한 저장소 — **② ✅ 09-21**(일회성 비밀번호 입력 창 · `Secret` 0 덮어쓰기 · `user:@host` = 빈 비밀번호 명시 · CLI 프롬프트도 같은 구분) · 남음 = ① 편집기 변수 · ③ `--password-stdin` | 52 §4 · **③ ✅ 09-21 추가 24**(`--password-stdin` · CLI도 비밀번호 없으면 서버에 가지 않음) · 남음 = ① 편집기 변수 · **흠(mac 09-21 90차)**: `nsql conn add`는 `--password-stdin`·`NSQL_PASSWORD`를 무시하고 비밀번호 없이 저장(`conn.rs` add = `-p`·프롬프트만 · `ensure_password` 미사용) | 🚧 |
| **T-149** | P0 | 대 | ★ **변수 관리**([63](63-variable-management.md) · 사용자 09-21). ✅ V0 기반 결함 · ✅ V1 REF CURSOR 자동 표시·결과 라벨·서명 추론·딸린 결과 탭 · ✅ D-138 문장마다 결과 탭 · ✅ D-135 계층형 표 · ✅ D-137 입력 창 · ✅ D-136 파일별 보존 · ✅ **V2 변수 창**(`vars_win.rs` · 제자리 편집·공유 토글·새 변수) · ✅ V5 일부(`captures` · D-139 행 수 정책 · SQL Server `@@ROWCOUNT` · PG 배열 조각) · ✅ V7 1차 계측(`bench_vars`). ☐ **PG refcursor**(`FETCH ALL IN` — 열 타입 필요 · Codespaces/CI로 검증) · ☐ `Caps` 포트(방언 `match` 걷기) · 서명 추론 PG/SQL Server/MySQL · DATE·TIMESTAMP·BOOLEAN · ☐ V6 MacroStore · 시스템 변수 · `${v:형식}` · `-v name=value` · ☐ `ACCEPT [HIDE]` · `COLUMN … NEW_VALUE` · 입력 창 타입 열·미리보기 · ☐ 편집기 hover 값 · 미정의 경고 · 옆 패널 · ☐ 이름 없는 탭 보존(S-1) · 프로필 층 값의 출처 · ☐ V7 잔여(값 크기 상한 `vars.max_value_kb` · 4방언 GUI 실기 · Codespaces) | 63 08 | 🚧 |
| **T-150** | P1 | 중 | ✅ 09-21 89차(win · 실서버 PG 14.5) — 드라이버 `expand_refcursors`(`pg_cursors` 확인 → `FETCH … IN "이름"` → `CLOSE` · `result_labels` · 자동 커밋이면 한 트랜잭션으로 감쌈 · 커서/단순 경로 · `SETOF refcursor`) · 통합 테스트. 원문: **PostgreSQL refcursor → 결과 탭**([63 §3-1](63-variable-management.md) · [61 §6](61-core-design-and-working-rules.md) 순서 2): 커서 이름을 같은 트랜잭션에서 `FETCH ALL IN "이름"` → `result_sets` → `CLOSE` · 열 타입(`refcursor`) 확보가 선행(단순 질의 경로에는 타입이 없다) · 자동 커밋이면 감싼다 · 검증 = `integration.rs` + 작업 브랜치에서 `gh workflow run integration.yml --ref` | 63 | ✅ |
| **T-151** | P1 | 중 | ✅ 09-21 89차 후반(win · 실서버 Oracle 19c `biscm` · SQL Server 2019 `M4PLAN`): Oracle DATE·TIMESTAMP·BOOLEAN **진짜 바인드**(종전 = 시각 잘림 · ORA-01722) · `VarType::Boolean` · SQL Server `sys.parameters` 서명 → **`OUTPUT` 보충**(`tsql_call_shape` · `mark_output`) · 선언 없는 글자 변수 넓히기(`NVARCHAR(2)` 잘림) · 꼬리 0 정리 · 통합 테스트 세 서버 13/13. 닫음: `Direction::Out`(이득 없음 — journal) · MySQL(드라이버 없음 → 드라이버 도입 때). 앞 기록: 🚧 09-21 89차(win): ✅ `routine_args` PostgreSQL(`pg_proc`) + 러너 `call_captures`(OUT 자리의 바인드가 값을 받는다 — 종전 = 이름이 다르면 조용히 버림) · ✅ `VAR X NUMBER` 표기 보존. **잔여**(서버 필요 — CI 컨테이너/허락 뒤): SQL Server `sys.parameters` · MySQL · Oracle DATE/TIMESTAMP/BOOLEAN 실제 바인드 · `Direction::Out` · `VAR X`(타입 없음). 원문: **서명 추론·타입 넓히기**: `routine_args` PG/SQL Server/MySQL · DATE/TIMESTAMP/BOOLEAN 실제 바인드(Oracle 드라이버의 VARCHAR2(4000) 일괄 바인드 걷기) · `Direction::Out` 생성 · `VAR X NUMBER`의 표기 보존 · `VAR X`(타입 없음) | 63 | ✅ |
| **T-152** | P1 | 대 | ✅ 09-21 89차(win) — `nsql-core/caps.rs`(11 능력 · 포트 `Session::caps` · 레지스트리 `Caps::of` · `Engine::set_caps`) · 엔진·러너 방언 분기 12곳 → 능력 질의 · 동작 불변(6방언 `plan` 동일 · 전수 테스트 · 실서버 PG). 잔여: `multi_result`·`bind_scope`·`paging`은 쓰는 코드가 생길 때 추가. 원문: **드라이버 능력표 `Caps` 포트**([63 §3](63-variable-management.md)): 엔진·러너의 방언 `match`를 능력 질의로(marker · out_values · cursor_out · multi_result · bind_scope …) — MySQL/MariaDB·NoSQL 어댑터의 전제 | 63 30 | ✅ |
| **T-153** | P2 | 중 | 🚧 09-21 89차 끝(win): ✅ `${env:이름[:형식]}` OS 환경 변수(`vars.env_subst`). 앞 기록: 🚧 09-21 89차 후반(win): ✅ `COLUMN … NEW_VALUE` · ✅ 시스템 변수 9종(`Engine.sysvars` — 사용자 정의와 분리) · ✅ `${이름:형식}`(q·id·n·upper·lower · `vars.brace_subst`) · ✅ 값 상한 `vars.max_value_kb` 1024. **남음**: hover 값(편집기 hover 기반 필요 — 47과 함께) · 입력 창 타입 열·미리보기 · 프로필 층 출처 · 이름 없는 탭 보존(S-1). 앞 기록: 🚧 09-21 89차(win): ✅ `ACCEPT [타입] [FORMAT] [DEFAULT] [PROMPT|NOPROMPT] [HIDE]`(입력 창 = 안내 글·기본값·가림) · ✅ CLI `-v 이름=값` · ✅ 미정의 바인드 경고(`Diagnostic` 소비) · ✅ `.devcontainer` `NSQL_PG_URL`. **잔여**: `COLUMN … NEW_VALUE` · 입력 창 타입 열·미리보기 · MacroStore + 시스템 변수 · `${v:형식}` · hover 값 · `vars.max_value_kb` · 프로필 층 출처 · 이름 없는 탭 보존. 원문: **변수 UX 잔여**: `ACCEPT [HIDE] [DEFAULT]` · `COLUMN … NEW_VALUE` · 입력 창 타입 열·미리보기 · MacroStore 분리 + 시스템 변수 · `${v:형식}` · CLI `-v` · hover 값 · 미정의 경고(`Diagnostic` 소비) · `vars.max_value_kb` · 프로필 층 출처 · 이름 없는 탭 보존(S-1) · `.devcontainer`의 `NSQL_POSTGRES_URL` → `NSQL_PG_URL` | 63 | 🚧 |
| **T-154** | P1 | 소 | 🚧 09-21 89차(win) 자동화 가능한 몫 ✅(창 12종 그리기 · SQLite 수동 커밋 · 문장마다 결과 탭 · 입력 창/변수 창 그리기 · 메모리·CPU·릭 A/B = 회귀 없음 · PG 수동 커밋 = 통합 테스트). **사용자 실기로 남김**(키 입력 필요): 입력 창 Windows 한글 IME · 변수 창 제자리 편집 · 포커스/마우스 라우팅 · Oracle REF CURSOR 자동 표시 · SQL Server 수동 커밋 GUI. 원문: ⏳ **Windows 실기(맥 86~88차분)**([61 §3](61-core-design-and-working-rules.md) "윈도우에서 처음 할 일"): 창 11곳 열림·그리기(`present.rs`) · 변수 입력 창·변수 창(포커스·마우스 라우팅·Windows 한글 IME) · 수동 커밋 Commit/Rollback(SQL Server·PG — T-146) · 문장마다 결과 탭·새로고침 · Oracle REF CURSOR 자동 표시 | 61 63 | 🚧 |
| **T-169 위키 공개** | P3 | — | 🚧 `docs/wiki/`(Home + Large-Files-and-Limits · `images/` · 09-22) · ✅ `scripts/wiki-publish.sh`(clone/pull → 복사 → 커밋 · `--push`는 사용자) · 잔여 = 첫 push(사용자 확인) · **영어 판** · 맥 캡처 이미지(T-103) · 72 표를 바꾸면 위키도 함께 | 72 | 🚧 |
| **T-170 스트리밍 실행** | P3 | — | ⏳ CLI `nsql`·"열지 않고 실행"의 문장 분할기를 스트리밍으로(파일 통째 읽기 → 메모리 ≈ 크기 × 2~3 · 72 §3 · 59 §4 3단계 **D-128** 결정 뒤) | 59 72 | ⏳ |
| **T-174 탭별 변수 보존의 문서 식별**([74](74-vars-persistence-identity.md) · 사용자 09-23) | P2 | 중 | 📐 결정 대기 D-191~D-195 → P1(이관 훅 Save As·이름 없는 탭→파일·이름 바꾸기 + 프로젝트 모드 = 탭 블록 `vars=<id>` + 기록 v2 메타 줄) → P2(`fsid` Windows/Unix + 저장 뒤 갱신 + 불일치 → fsid → 지문 후보 UI · 설정 `vars.match_fsid`/`match_fingerprint`) → P3(옵트인 `vars.doc_id` 주석 · 복사본 감지 · 위키) · 39 §3 행 갱신 | 74 63 67 | ⏳ |
| **T-176 IntelliSense · 아웃라인 2차**([76 §7](76-intellisense-and-outline.md) · 사용자 09-23) | P2 | 중 | 1차 ✅ 97차(문맥·랭킹·문서 심볼·팝업·Goto Symbol·아웃라인 패널·메타 feed) · 남음 = 시스템 객체·내장 함수 정적 패키지(29 §6-4) · hover 카드(47 D-84) · JOIN 완성(FK) · INSERT 컬럼 목록 · alias 자동 삽입(`intel.insert_alias`) · 시그니처 도움 · 팝업 스크롤(ContextMenu) · 후보 계산 워커(예산 `intel.budget_ms` 실측 뒤 · D-202) · 디스크 캐시(D-85) · 워터마크 연동(47 §5) · 컬럼 코멘트·PK 표시(`nsql-catalog::keys`) · 39 §3 부하원 등재 → **T-178로 통합** | 76 47 29 | ⏳ |
| **T-177 개발 중 기능 점검·완성도**(사용자 09-23 할 일 1 · [77 §4](77-data-workbench-architecture.md)) | P1 | 중 | 99차 실기 확인표 U-81~U-96의 결과를 받아 결함 수정 · 검색/이력/범위 필터/아웃라인/제외 로그 마감 · 자동 점검 S69~S78 유지 · **🔧 S06·S46 좌표**(§129 — OPEN FILES·북마크 거터 뒤 `150/175`가 필터 상자를 누름 · `view.project` 토글이 앞 시나리오의 프로젝트 파일 `panel` 상태에 따라 닫힘 → 좌표 정정 + 패널 상태 고정 기동 명령 `project.panel:on`) | 77 | ☐ |
| **T-178 인텔리센스 완성**(할 일 2 · T-176 흡수 · 77 §1-2) | P1 | 대 | **1차 ✅ 99차 §129**(정적 표 `builtins` = 내장 함수·DBMS 패키지·사전 뷰 · 시그니처 도움(상태줄) · 함수 괄호/키워드 공백/alias 삽입 · INSERT 컬럼 목록 조각 · 팝업 스크롤 · 컬럼 상세 PK/FK/UQ/NOT NULL · 예산 로그 · 설정 7 · 위키 3분 사용법 · S77·S78 · U-97~U-99) · **남음** = JOIN 완성(FK 대상) + **`MetaStore` 확장**(컬럼 길이·기본값·코멘트 · FK 대상 · 프로시저 시그니처 · `Pending` 지연 채움) + 드라이버 포트 `Session::describe` · 시그니처 카드(캐럿 옆) · hover 카드(T-179와) · 후보 워커(예산 로그 실측 뒤 · D-202) · 디스크 캐시(D-85) | 77 76 47 | 🚧 · **100차(mac) ✅** 현재 스키마 = 서버 값(`pick_current_schema`) · 접속 직후 미리 읽기 `intel.preload` · `스키마.` 즉시 채움(`NeedObjects`) · 권한 반영 사전 뷰(`nsql_catalog::dictionary`) · 상한 없는 후보 · 미사용 버킷 즉시 회수([76 §9](76-intellisense-and-outline.md)) · 실기 = U-100~U-104 |
| **T-184 메타 단일 원천 마무리 — 트리 노드 중복 제거**(사용자 09-23 "중복되지 않는 메모리 구조 · 유니버설 어댑터" · [76 §9](76-intellisense-and-outline.md)) | P2 | 중 | 탐색기 `NodeKind::Object(ObjectInfo)`가 이름·상태를 문자열로 복사 → 노드는 `ObjId`만 · 이름은 인터너(`MetaStore.names`) · 트리 그리기/필터/타입어헤드가 스냅샷을 읽게 · 객체 행 tombstone 압축(`drop_bucket` 뒤 40 B/행) · 탭 닫힘 때 `DocCache` 정리와 회수 판정 연동 | T-178 | ☐ |
| **T-185 i18n 전체 점검 — 간략화**(사용자 09-24 "전체 i18n은 별도로 · 의미 살려 간략하게") | P2 | 중 | `nsql-i18n` 전 항목(라벨·설명·값·메시지 ≈ 1,300) — 라벨 ≤ 4어절 · 설명 ≤ 1문장 · 영/한 같은 길이감 · 의미 보존 · 설정 창 카드 높이 기준 · 자동 검사(길이 상한 시험) · 78 재배치와 같이 카테고리별로 | T-186 | ☐ |
| **T-186 설정 카테고리 재정리**(사용자 09-24 · [78](78-settings-reorganization.md)) | P1 | 중 | 그룹/카테고리 트리 재배치 · 카테고리 안 순서 = `LAYOUT` 단일 원천(마스터 → 종속 들여쓰기 → 범위·동작·표시·한계) · `DEPENDS` 보강(30여) · ★ `FORCES`(상위 값 → 하위 강제값 · perf.boost `BOOST`를 일반화) + 설정 창 표시("○○ 때문에 고정") · `nsql config list` 머리글 · 위키 | — | 📐 |
| **T-187 메타 갱신 범위 규칙**(사용자 09-24 · [79 §3](79-metadata-ownership-and-refresh.md)) | P1 | 중 | `Coverage::Stale` · `mark_stale`/`mark_columns_unknown` · 탐색기 새로 고침(루트/스키마/폴더/객체) = 하위 버킷·컬럼까지 · 완성은 Stale이면 후보 + 재요청(깜빡임 0) · 시험 | — | ✅ 09-24 §177(폴더·객체 = 그 스키마 단위로 표시 · 세밀한 종류·객체 단위는 남김) |
| **T-188 완성 캐시 명시 갱신 명령**(사용자 09-24 · [79 §4](79-metadata-ownership-and-refresh.md)) | P1 | 소 | `intel.refresh`(팔레트 · Edit ▸ IntelliSense · 탐색기 루트 메뉴 · Ctrl+Shift+R/⌘⇧R) · 범위 = 현재 스키마/이 서버/전 서버 · 상태줄 · 위키 | T-187 | ✅ 09-24 §177 |
| **T-189 메타 채움 주체 확장**([79 §2-2](79-metadata-ownership-and-refresh.md)) | P2 | 소~중 | 실행 결과 열 메타(단일 테이블 문장) · `DESC` 결과 → `set_columns` · 소유 규칙 MC/DC 시험(공유+전용 같은 스냅샷 · `CONNECT` 뒤 칸 전환) | T-179 | 📐 09-24 §186 단일 테이블 `SELECT *`만 안전 → 보류 |
| **T-191 nexa-sql 시험 병렬 SIGSEGV(간헐)**(09-24 게이트 1회 · 5회 재현 0) | P2 | 소 | 시험 프로세스 종료 시 살아 있는 메타 스레드(급한·백그라운드) 의심 · `Explorer` Drop에서 `Close`+join 또는 시험 끝에 채널 닫기 · 재현 스크립트(20회) | — | ☐ |
| **T-190 CLI 접속 2.0 s(Oracle) vs GUI 0.4 s**(09-24 맥 전수 CLI 표) | P2 | 소 | `nsql run` 경로에서 접속 앞뒤 무엇이 1.6 s인지(볼트 열기 · 설정 · Oracle 클라이언트 탐지 · DNS) `--timing`에 접속 단계 추가 후 지목 | — | ✅ 09-24 §183 원인 확정(Release: Instant Client 적재 `init_client` 1.3 s + 접속 왕복 0.4 s · SQLite 0.4 ms · 프로세스당 1회 · CLI `--timing`에 connect 단계 · `NSQL_TRACE_CONNECT`) — 줄이려면 클라이언트 적재(ODPI-C dlopen)라 앱 쪽 여지 없음 |
| **T-192 10만 행 조회 반영 시 1.7 s 프레임(맥)**(26 §7-11 s7) | P2 | 중 | `[frames]` chrome 32 ms 평균·max 1,726 — 결과 도착 → 그리드 세우기가 UI 스레드에서 한 프레임에 · Windows 96차 값과 비교 · 열 폭 측정/텍스트 캐시를 나눠 넣기(43 §11 Facade 뒤) | — | 🚧 09-24 §186 격리 재현(SQLite 10만 행 × 4열) = Release 최대 53 ms · 미재현 → 실서버 전수 때 재측정 |
| **T-193 맥 present 기본(D-133 ②)**(09-24 G) | P1 | 소 | 캐럿 깜빡임 100 ms/초·프레임 50 ms의 원인 = softbuffer CG present 36 ms → `gfx.mac_present=iosurface` 기본 전환 여부(CPU 20~40 ms/초 · footprint +40 MB · 09-21 빈 창 사고 재확인 U-115) | D-133 | ✅ 09-24 §178 기본 `iosurface`(Release 43 → 9.8 ms · Debug 48 → 13.9 ms · 빈 창이면 설정으로 되돌림 · Apple Silicon 미확인) |
| **T-194 맥 기동 = NSWindow 생성 168 ms**(26 §7-11 `[startup:resumed]`) | P3 | 소 | winit `create_window` 속성(아이콘 `with_icon` · 배율 · 숨김→보임) 중 무엇이 비싼지 A/B · 로그인 창을 메인 뒤에 만드는 순서 · 기동 592 → 400대 목표 | — | — 09-24 §186 winit 내부 · 아이콘 래스터 수 ms · 손댈 곳 없음(기록) |
| **T-195 Rainbow Alias 확장**(사용자 09-24 · 방향성 메모 — 개발 착수 때 상세 설계) | P3 | 중 | ★ 목표 = JOIN 문에서 지정한 테이블 alias(A · B …)를 **SELECT 절 등에서 색으로 구별**(alias 선언 순서대로 색 부여 · Rainbow Pairs와 같은 확장 틀 = WASM 확장 `extensions/` + `rainbowpair.*` 식 설정 · 색 팔레트는 Rainbow Pairs 깊이 색과 별도) + **컬럼 정렬(재배치)**: 같은 테이블 안에서 이름순 / 선언순(메타 순번) / 테이블 구별 없이 전체 정렬 — SELECT 목록을 편집기에서 재배치하는 명령(정렬 기준 = 완성의 둘러보기 순서와 같은 원천 76 §11). 접점 = `nsql-script::intel::alias_table`(alias 표) · `MetaStore` 컬럼 순번 · 편집기 강조 API(nexa-ctl 구문 토큰 색 · 51 §12 대비 색 순서) · 확장 SDK(75) | T-118 · T-178 | 📐 |
| **T-196 완성 목록 페이지 로딩**(사용자 09-24 "제한 200 유지 · 끝까지 스크롤하면 그리드처럼 자동 추가 페치 · 끝에 읽는 상태 · 메모리 점검·미사용 정리") | P2 | 중 | 📐 [76 §13](76-intellisense-and-outline.md) — 한 세트(`Intel.cands` 전체 랭킹 ≤ `intel.max_total` 5000) + 창 투영(`shown` = 페이지 `intel.max_items`) · nexa-ctl `take_reached_end`/`replace_items`(first·hover·위치 유지)/`close()` 항목 해제 · 끝 항목 = "N개 더" ↔ "불러오는 중…"(그리드 43 §5와 같은 글) · End = 전부 · 회수 = 닫힘 즉시 `Vec::new()` · 아이콘 캐시 80 KB 고정 비용 등재 · D-205·206 | T-178 · §163 | ✅ 09-24 §175(설정 `intel.max_total` · End = 전부) |
| **T-197 메모리 모니터**(사용자 09-24 · [80](80-memory-monitor.md)) | P2 | 중 | ✅ 1차(§167) — 상태줄 총량 · 메모리 맵 창(모델리스·최상위 · 1 s 갱신 · 카테고리 원장 `memstat::Cat` · 포트 `MemSource`) · **남음** = 이력 스파크라인(60칸) · 카테고리별 "정리" 동작(`memtrim` 호출 · 결과 탭 닫기 안내) · CLI `nsql mem`(같은 `Sample` 표) · Windows/Linux 실측(`HeapSummary` · `mallinfo2`) | T-193 | 🚧 |
| **T-198 부분 다시 그리기**(사용자 09-24 팝업 hover 지연 · §173) | P2 | 상 | 📐 후보: 기본 프레임 보존(IOSurface 2벌) 위에 팝업·카드·캐럿 영역만 다시 래스터·present — 전체 프레임 50 ms → 수 ms · 선행 = D-133 IOSurface 기본 전환(T-193) · 손상 영역 관리 부품(nexa-gfx `damage`) | T-193 | ⏸ 09-24 §186 IOSurface 뒤 13 ms라 보류 |
| **T-199 패키지 테이블 함수 ④**(09-24 검토 · §176 뒤 남은 것) | P3 | 중 | `FROM sch.pkg.fn() a` 뒤 `a.` 컬럼 = 반환 컬렉션의 요소 타입(`ALL_COLL_TYPES.elem_type_name` → `ALL_TYPE_ATTRS` · 스키마 수준 객체 타입까지 · 패키지 안 타입은 "컬럼 알 수 없음" 카드) · 12.2 미만 `TABLE(` 감싸기 = `Caps` 항목(서버 버전) · alias 표가 `sch.pkg.fn(...)`를 함수 호출로 인식 | T-152 · §176 | 📐 |
| **T-200 문법 참조 기반 완성**(사용자 09-24 "SQLite SELECT 목록에 HAVING · 각 DBMS 문법을 참조 파일로 · 파일 추가 = DB 지원 플러그인") | P1 | 중 | ✅ 1차 §185 — [82](82-grammar-driven-completion.md) · `.sqlg` 5개(ansi·oracle·mssql·postgres·sqlite) · nsql-script `grammar.rs`(해석·병합·레지스트리·`load_dir`) · `Context.clause`/`stmt_kind` · 예약어 = 절의 `next` · **남음** = `objects`로 객체 후보 게이트 · 절 안 위치(첫 항목 규칙) · MySQL 파일 · 공식 문법과 대조표 | T-178 | 🚧 |
| **T-201 완성 × 문서 크기·향상 모드**(사용자 09-24 "파일 용량별 레벨 제한 · 향상 모드 포함 검토") | P1 | 중 | ✅ §187 — ② 향상 모드 강제 키 5(`intel.icons/detail_card/document_words/preload/from_routines`) ③ 자기 감속(`intel.budget_ms` 연속 2회 초과 → 탭 문서 낱말 끔 · 상태줄) ① 창 방식(`intel.max_doc_kb` 1024 · 캐럿 앞뒤 256 KB · 문서 낱말·아웃라인 없음) · L1 = 수동·시그니처만 · L2 = 끔 · 72 표 · 시험 2 | T-178 · 72 | ✅ |
| **T-202 이름 있는 커서 결과(REF CURSOR `PRINT`·자동 표시)의 상한 초과**(사용자 09-24 질문에서 발견) | P1 | 소 | ✅ §189 — **정석**: `Session::fetch_cursor_page` · Oracle `CursorSrc::Ref` · 러너 `emit_cursor(page)` = 상한까지 + 열린 커서 보관(자동 커밋 미루기 그대로) · 커서 유지 불가 = 전체 조회 강제 + ⚠ `RunEvent::Warning` · `requery_ok` 게이트 → `FetchStop::CursorGone`(재질의 0) · 실측 BISCM `\more`/`\all` 커서 · 시험 4 · 실기 U-137 | 43 §3-6 | ✅ |
| **T-203 객체 탐색기 2차 — DBMS별 트리 + 유효성 배지**(사용자 09-25 DBeaver 캡처) | P1 | 대 | ✅ §190 — [83 §1~2](83-object-explorer-dbms-trees-and-generate-sql.md) · nsql-catalog `tree.rs`(`sub_kinds` 18 · `sub_items` 방언별) · `ObjectKind` +12 · `NodeKind::Sub/Item` · 배지 · 아이콘 8 · CLI `cat sub` · 4-DBMS 실서버 + GUI 덤프 점검 | 28 · 83 | ✅ |
| **T-204 Databases 층 · DB 수준 폴더 나머지**(D-201 · SQL Server `db.sys.*` 3부 · PG 접속 DB만 · Database Triggers · Storage/Roles) | P2 | 중 | 대기 — 지금은 접속 DB 하나 = 루트 | 83 §5 | 대기 |
| **T-205 Generate SQL + SQL Preview 모달**(사용자 09-25) | P1 | 중 | ✅ §190 — `gen.rs`(SELECT·INSERT·UPDATE·DELETE·MERGE·CALL·DDL · 4-DBMS · 하위 항목 DDL · 스마트 인용) · 탐색기 "SQL 생성 ▸" · `sqlprev_win.rs`(새로고침·파일로 저장·편집기에서 열기·복사·닫기) · CLI `cat gen` · GUI 덤프 점검 ✓ | 83 §3~4 | ✅ |
| **T-206 탐색기 2차 잔여**(PG `pg_depend` 전체 의존 · Oracle Recycle Bin · 권한 폴더 · 트리거 컬럼 · 큐 테이블) | P3 | 소 | 대기 | 83 §5 | 대기 |
| **T-207 서버 묶음 트리 + 연결 해제 UX**(사용자 09-25) | P1 | 중 | ✅ §191 — [54 §9](54-connection-model-and-disconnect.md) · `same_host` 그룹 · 헤더 행 + 연결 행 · 헤더 메뉴 모두/개별(`disc_menu` MC/DC) · 해제 행 제거(D-204 `explorer.keep_offline`) · D-205 `explorer.disconnect_pick` · 실기 U-142 | 54 | ✅ |
| **T-208 탐색기 검색창(객체 필터)**(사용자 09-25) | P1 | 중 | ✅ §192 — [28 §7](28-object-explorer.md) · filterbar 부품 · 전 서버/키보드 대상(`explorer.filter_scope`) · keep-set · 강조 · 헤더 일치 수 · Enter/Esc · ⌘F · 실기 U-144 | 28 | ✅ |
| **T-209 검색창 2차**(메타 저장소로 안 읽은 객체까지 · 서버별 범위 칩 · 일치 없는 헤더 접기 · 헤더 접기) | P2 | 중 | 대기 | 28 §7-3 | 대기 |
| **T-210 SQL Preview 편집기 동일 표시 + 생성 옵션 + DDL 샘플 모양**(사용자 09-25) | P1 | 중 | ✅ §193 — `preview_box` · 두 글꼴 그리기 · 체크박스 4(`gen.*`) · `GenOpts`/`Cx` · SQL Server/Oracle/PG 테이블 DDL(헤더·COLLATE·NULL·인라인 CONSTRAINT·확장 속성/COMMENT ON) · CLI 옵션 토큰 · 실측 4-DBMS | 83 §3-1 | ✅ |
| **T-211 글로벌 변수 층**(사용자 09-25 "글로벌 변수 개념 정리 · 필요하면 설계/개발") | P1 | 중 | ✅ §194 — [63 §11](63-variable-management.md) `Layer::Global` · `set_layer` · `VAR x GLOBAL|TAB` · `vars/global.sql`(`vars.global_persist`) · 변수 창 층 순환 버튼 · `Cmd::GlobalVars`/`RunEvent::Vars{global}` · 전 세션 브로드캐스트 · 시험 · 실기 U-150 | 63 §11 | ✅ |
| **T-212 카탈로그 단위 메타·트리 공유**(사용자 09-25) | P1 | 중 | ✅ §193 → **원복 §194**(사용자 "계정별 권한 = 인텔리센스 분리") — 기본 = 연결별 칸 · 카탈로그 공유는 `explorer.share_catalog`(기본 끔)로 유지 · [54 §10-4](54-connection-model-and-disconnect.md) | 54 §10 | ✅ |
| **T-213 시스템·빈 스키마 숨김 설정**(사용자 09-25 DBeaver 대조) | P1 | 소 | ✅ §193 — `SchemaOpts` · `explorer.show_system_schemas`(끔)/`hide_empty_schemas`(끔) · Oracle 24 · CLI `cat schemas all|nonempty` | 28 | ✅ |
| **T-214 탐색기 필터 v2**(사용자 09-25 ×3) | P1 | 소 | ✅ §193 — 판정기 보관·자체 재필터(⑩⑪) · 두 조건 규칙 + 최소 레벨 · `일치/전체` 개수 · 자동 펼침 복귀 | 28 §7-4 | ✅ |
| **T-216 편집기 CONNECT 두 번 = 앞 연결 대체**(사용자 09-25 · D-208) | P2 | 소 | 결정 대기 — 권장 = 모델 유지 + 로그 안내 문구("앞 연결 X를 닫고 Y로") · 대안 = `explorer.keep_offline` / 공유 연결로 남기기 | 54 §10-5 | 대기 |
| **T-217 CONNECT 동일성 진단**(사용자 09-25) | P2 | 소 | ✅ §195 — 재접속 사유 로그(계정 다름/자격 변경/설정/세션 없음) · 판정 로직 점검 결과 이상 없음(한 탭 번갈아 = 계정 다름 = 재접속 정상 · D-208) · 실기 U-152 | 54 §10-5 | ✅ |
| **T-218 미리보기 토글 시 탐색기 스크롤 밀림 · DDL 범위 정정**(사용자 09-25) | P1 | 소 | ✅ §196 — `pane_top` 앵커 복원(헤더 높이) · 기본 = 테이블·제약·인덱스 / 전체 = + 설명 + Oracle 저장 절 · PK 인덱스 중복 제거 · 실기 U-153 | 83 §3-1 | ✅ |
| **T-219 탐색기 검색 인덱스 — 선별 → 부분 노드 → 순차 완성 · 유휴 선적재**(사용자 09-25) | P1 | 중 | ✅ §197 — `nsql_catalog::name_index` · 스키마 순차(현재 먼저 · prio 2) · `LoadState::Partial`(`n/?`) · 완성 큐 디프 · 헤더 `인덱싱 d/T` · `prefetch_step`(tx_bg · 5 s) · 설정 5키 · CLI `cat index` · 🔧 이력 드롭다운 클릭/우클릭 · 실기 U-154~156 | [84](84-explorer-search-index.md) | ✅ |
| **T-221 메타 3층 L1/L2/L3 + 검색 스레드 분리**(사용자 09-25) | P1 | 대 | ✅ §200 — `Coverage::Names`/`load_names`/`reclaim` · `l1_step`·`arm_warm`·`warm_step`·`reclaim_meta` · 검색 판정 = 백그라운드 메타 스레드(`Req::Search` → `Resp::Hits`) · `by_name` 지연 · 설정 5키 · D-209 · 실기 U-159 | [85](85-metadata-layers.md) | ✅ |
| **T-222 메타 3층 후속** | P3 | 중 | ✅ ① 디스크 캐시(§201 · `metacache.rs` · `meta.disk_cache`) · ✅ ② 메모리 창 3층 · 남음 = 스키마 순서 학습 · 컬럼 이름 인덱스 · Oracle `last_ddl_time` 증분 · 실기 U-160 | 85 §9 | 🚧 |
| **T-220 검색 인덱스 후속** | P3 | 중 | ✅ ① Oracle DBA→USER→ALL 빠른 길(§198 · 3.6 → 0.27 s) · ✅ 진행 표시 = 테두리 애니메이션(84 §8-1) · 남음 = 스키마 순서 학습 · 컬럼 인덱스(옵션) · 디스크 캐시 · `n/?` 진행 점 | 84 §11 | 🚧 |
| **T-223 객체 상세 패널**(사용자 09-25) | P1 | 중 | ✅ §207 1차 — `objdetail.rs` · `nsql_catalog::object_details` · CLI `cat detail` · View ▸ Object Details · 설정 3키 · 실기 U-166 | [86](86-object-details-panel.md) | ✅ |
| **T-225 객체 상세 후속** | P3 | 중 | ✅ ①코멘트=설명 ②스키마 객체 수 ③더블클릭=편집기 ④▾/▴ 도형(§208) · 남음 = 섹션 접기/탭 · 짧은 LRU · 컬럼 → 주인 요약 | 86 §8 | 🚧 |
| **T-224 더티 영역 present**(애니메이션 중 전체 재그리기 13~17 ms/Debug · §203 ③) | P3 | 중 | 후보 — 부분 갱신 없는 구조 · Release ≈ 4 ms라 보류 | 26 | 대기 |
| **T-215 Format SQL**(생성 옵션 · 포매터) | P3 | 중 | 대기 — SQL 포매터가 없어 옵션에서 뺌 | 83 §3-1 | 대기 |
| **T-179 객체 정보 조회**(할 일 3 · 77 §1-3) | P1 | 중 | `ObjectInfo`(열 표 · 인덱스 · 제약 · DDL 미리보기 · 데이터 200행 버튼) · **객체 정보 탭**(뷰 탭 자리) · **hover 카드 부품**(`HoverCard` · 요약 + 동작 버튼 · 팝업 규칙) · 설정 `intel.hover_ms` | 77 | ☐ |
| **T-180 객체 탐색·바로가기**(할 일 4 · 77 §1-1·1-3) | P1 | 중 | `resolver`(편집기 낱말+alias · 탐색기 노드 · 그리드 열) → `ObjectRef` · **Ctrl+클릭**(설정 `intel.ctrl_click` = info/reveal/columns) · F4 · `obj.*` 액션 레지스트리(팔레트·우클릭·키 같은 id) · 그리드 열 머리 → 객체 · FK 따라가기 · 애매하면 후보 팝업 | 77 30 | ☐ |
| **T-181 그리드 필터 + 조회용 Query 복사**(할 일 6 · 77 §2-2) | P1 | 중 | `Predicate` AND 목록 = `View` 두 번째 투영(세트 복사 0) · 열 머리 깔때기(값 목록 + FilterBar) · 필터 줄(칩 · ×) · 상태줄 n/N · **조회용 Query 복사** = 출처 문장 서브쿼리 + WHERE 합성(41 리터럴 규칙) · `Requery`로 서버 재조회 | 77 43 41 | ☐ |
| **T-182 그리드 데이터 편집/추가/삭제**(할 일 5 · 77 §2-3) | P1 | 대 | 편집 가능 판정(단일 테이블 + 키: PK/UK → ROWID/ctid/rowid/%%physloc%% → 전체 열 1행 확인 D-198) · `ChangeSet` 덧그리기(셀 색 · Esc · 되돌리기) · 인라인 편집(타입 검증 · NULL · 값 편집 창) · 행 추가/삭제/복제 · **`SqlGen`**(`Caps` · 41 · 날짜 바인드) + 미리보기 창 · ✓ 적용 = 한 트랜잭션 `gate_open` · 34 UX · PROD 확인 · 영향 행 수 검사 `grid.edit_strict` · `grid.edit_refresh` | 77 43 41 34 52 56 | ☐ |
| **T-183 내보내기**(할 일 7 · 77 §2-4) | P2 | 중~대 | 파일 CSV/TSV/JSON/SQL INSERT/**XLSX 자체 writer**(zip+XML · D-200) · **Excel 인스턴스**(Windows COM late binding · D-199 `windows` crate 예외 원장 · 없으면 파일 폴백) · 선택 행/열 · 인코딩 · 진행 카드 · 스트리밍 · 설정 `export.*` | 77 41 | ☐ |
| **T-175 프로젝트 필터 열거 워커**(journal 09-22 §114 · 사용자 09-23 "5000 기준 검토" → **§120 설계 변경 지시**) | P3 | 중 | ✅ 09-23 99차 — D-196은 사용자 지시("개수가 많아도 멈추지 않게 · 별도 스레드 · 분할 병렬 · 로그 병합")로 대체: 부품 `parwalk.rs`(코디네이터 + 워커 `project.scan_threads` 4 · 폴더 단위 큐 · 채널 하나 · 부모 먼저) · 패널은 틱마다 6 ms 예산으로 합침 · `project.scan_max` 기본 **0 = 무제한**(> 0 = 메모리 보호) · 읽지 못한 폴더 = 안내 + 로그 창 · 파일 검색은 제외 로그(`Batch::Skipped` · 패널 "제외됨(N)" · CLI 요약) · 39 §3 · 72 · 위키 | 37 39 72 | ✅ |
| **T-172 프로젝트 경로 이식**([73](73-project-path-portability.md) · 사용자 09-23) | P2 | 중 | 📐 결정 대기 D-186~D-190 → P1(열기 입구 `std::path::absolute` · 저장 정규화 · 절대 경로 경고 · 못 찾은 탭 = 자리 탭) → P2(`${folder:이름}`·`${home}` 앵커 · `folders[].name` · `../형제`) → P3(`${var}` + `vars` + `.local.json` + 미정의 팝업 + CLI) → P4(v2 절 구조 · `project.share_workspace` · Relocate · 위키) | 73 70 67 | ⏳ |
| **T-173 프로필 내보내기/가져오기**(비밀번호 제외 · DataGrip "비밀번호 없이 복사") | P3 | 소 | `nsql conn export/import` · 프로젝트 `profiles` 이름과 짝 · 21 §7 볼트는 안 따라감 | 73 21 | ⏳ |
| **T-168 성능 후속(95차)** | P3 | — | ① ✅(96차 [26 §7-10](26-performance-architecture.md): `list all` = HIDDEN 제외 · 405 − 9 ≈ 395) ② ✅(96차 두 표본 모두 228/33 — 259는 1회 잔상) ⏳ ③ 실행 카드 30장 스택의 그리기 비용(`run.toast_max` 강제값 근거) ④ ✅(95차 G 판정 유지 · 96차 1436 ms 기준선 안) ⑤ 39 §3 부하원 원장 전 행에 "읽는 코드 위치" 열 채우기(미배선 키 재발 방지 · 72 §6) ⑥ ✅(96차 인벤토리 395 · 부하원 24) ⑦(96차 추가) 기동 3초 CPU 중앙값 156 → 203 ms(표본 125~234)를 10회 이상으로 재고 시작 때 늘어난 일(북마크 워크스페이스 · `backups::prune` · DBMS 아이콘 마스크)의 몫을 `[startup]` 계측으로 나눈다 | 26 71 | ⏳ ③⑤⑦ |
| **T-154 확인표** | P1 | — | ⏳ **사용자 확인 항목**(키·마우스 입력이 있어야 보인다 — 입력 주입 금지라 사용자 몫 · 방법은 요청하면 항목별로 상세 안내 · 결과는 journal에): **U-1** 변수 입력 창에서 Windows 한글 IME 조합(값 입력란 · 조합 중 글자 · Enter = Run · Esc = Cancel · Tab 이동) · **U-2** 입력 창 `ACCEPT` 줄(안내 글 · 기본값 미리 채움 · `HIDE` 가림 · 가린 칸에 한글 입력 시 동작) · **U-3** 변수 창 제자리 편집(줄 선택 → 입력란 → Set/Enter · NULL · Share/Local · Delete · `이름 = 값` 새 변수 · ↑↓) · **U-4** 입력 창·변수 창 포커스 링 ≤ 1(클릭 뒤 1개 · 다른 컨트롤 클릭 뒤 앞 링 꺼짐 · Esc/창 전환 뒤 0) · **U-5** 같은 두 창의 마우스 라우팅(클릭이 커서 아래 컨트롤에만 · 영역 밖 컨트롤에 닿지 않음 · 휠) · **U-6** Oracle REF CURSOR 자동 표시(`examples/oracle-refcursor-pkg.sql` → 결과 탭 · 탭 제목 = 변수 이름) · **U-7** SQL Server·PostgreSQL 수동 커밋 GUI(Commit/Rollback 버튼 · 배지 · 다른 세션에서 미커밋 확인) · **U-8** 84~85차에서 넘어온 것(큰 파일 한글 조합 · 거대 선택 위 타이핑 안내 · 재시작 뒤 되돌리기) · **U-9** 편집기 밖 드래그 선택(89차 추가): 편집기에서 드래그 시작 → 포인터를 왼쪽 탐색기 위로 뺀 채 위/아래 → 줄 단위로 따라오고 끊기지 않는지 · 오른쪽 밖 = 줄 끝 · 위/아래 밖 = 멀수록 빠르게 · **U-10** 탭 드래그 고스트(편집기 탭·결과 탭을 끌 때 고스트가 포인터를 따라오고 놓일 자리가 표시되는지 · Esc/놓기 뒤 사라지는지) · 이름 바꾸기 상자(탭 옆에 붙음 · 바깥 좌/우클릭 = 취소) · 결과 탭 ▸ 실행 쿼리 복사 · **U-11** 설정 ▸ DBMS ▸ Oracle: 직접 지정 → 찾아보기…(폴더만 보임 · 클릭 = 이름 칸 · 더블클릭 = 들어가기 · 폴더 선택) · 직접 입력 · 자동 탐지 = 경로가 흐리게 · 적용 뒤 재시작해 Oracle 접속 · **U-12** 일회성 비밀번호(89차 추가 11): 편집기에서 `CONNECT oracle://BISCM@192.168.0.58:1521/BISCM` 또는 시작 인자로 같은 문자열 → 비밀번호 창(가림 · 복사 안 됨 · Enter = 접속 · Esc/취소 = "접속 취소") → 접속 + 탐색기 트리 · 로그·상태줄·세션 창에 비밀번호가 안 보이는지 · 틀린 비밀번호 = 서버 오류 1회(재시도 없음) · `…BISCM:@…` = 창 없이 바로 시도 · **비밀번호 창에서 Enter(짧게 · 길게 누르기 둘 다) → 편집기에 줄바꿈이 들어가지 않는지**(선택 영역이 있을 때도 · 변수 입력 창 Enter도 같다 · 남으면 `NSQL_TRACE_IME=1`) · 세션 자격 금고(추가 14): 비밀번호를 한 번 입력해 접속한 뒤 탐색기 루트 ▸ **이 연결로 새 탭** → 묻지 않고 새 탭이 전용 세션으로 접속되는지 · 앱을 다시 띄우면 다시 묻는지 · 설정 ▸ 접속 ▸ "입력한 비밀번호를 앱을 끝낼 때까지 재사용"을 끄면 매번 묻는지 · 무효화(추가 15): 접속해 둔 채 서버에서 그 계정의 비밀번호를 바꾼 뒤(또는 시험 계정으로) 이 연결로 새 탭 → "앞서 입력한 비밀번호를 서버가 거부해 폐기했습니다" 안내와 함께 입력 창이 **바로** 뜨는지 · 새 값으로 접속되고 그 뒤 새 탭은 묻지 않는지 · 네트워크를 끊었다 이은 뒤에는 다시 묻지 않는지 · 자격 바꿔 다시 접속(추가 16): ① `CONNECT oracle://BISCM@192.168.0.58:1521/BISCM`(입력 창 → 접속) ② 같은 탭에서 `CONNECT oracle://BISCM:@192.168.0.58:1521/BISCM` → 기존 접속이 끊기고 빈 비밀번호로 시도 → 오류(ORA-01005) + 탭 = 연결 없음 · **첫 실행에서 바로 오류 카드가 뜨는지**(추가 17) · 로그의 해제 경로 = "같은 서버에 다른 자격으로 CONNECT" ③ ①을 다시 실행 → **입력 창이 다시 뜨는지** ④ 같은 `CONNECT user:pw@…`를 되풀이 실행하면 다시 접속하지 않고 유지되는지 · `CONNECT M4PLAN`(프로필 이름)을 되풀이해도 다시 접속하지 않고 로그에 "기존 접속 유지"가 남는지(추가 20) · **U-14** 저장하지 않은 탭 닫기(추가 23): 이름 없는 스크립트에 글을 치고 X → 메뉴(저장하고 닫기 · 저장하지 않고 닫기 · 취소) → 저장하고 닫기 = 저장 창 · 이미 있는 파일 이름이면 타임아웃 버튼으로 다시 확인 · 저장 뒤 탭이 닫히는지 · 저장 창 취소 = 탭 그대로 · Ctrl+W·탭 메뉴 "모두 닫기"도 같은지 · SQL Server에서 `SELECT :V1, :V2` 머리줄 = 변수 이름(추가 21) · **U-13** 탐색기 우클릭(89차 추가 11): 서버 루트 = 새 탭/새로 고침/연결 해제 · 그 아래 = 새로 고침/이름 복사 · 메뉴가 누른 자리 바로 아래(아래가 좁으면 위)에 뜨고 대상 이름을 가리지 않는지 · 메뉴가 편집기 쪽으로 넘어가도 항목 클릭이 되는지 · 탭 표식 메뉴(연결 목록)를 연 채 탐색기에서 우클릭 → 표식 메뉴가 닫히고 탐색기 메뉴 하나만 남는지 · 항목으로 천천히 이동해도 메뉴가 유지되는지(추가 18) · 연결 해제 뒤(추가 19) 폴더에서 새로 고침 → "⚠ 접속 안 됨" 줄 + 경고 토스트 · 루트에 `offline` · 서버 우클릭 ▸ 연결 뒤 다시 펼치면 목록이 돌아오는지 · 바깥 클릭 = 닫히고 그 클릭이 그대로 동작 · 툴바 빈 곳 우클릭 메뉴(같은 결함으로 죽어 있었다) · **U-15** 토스트 남은 시간 표시(92차 §16): 없는 테이블 조회 → 실행 상태 카드의 **왼쪽 상태 막대가 위에서부터 옅어지고**(지나간 부분 = 같은 색 30%) 카드가 진척 따라 투명해진 뒤 사라지는지 · 완료 카드(초록)도 같은지 · 설정 ▸ 모양 ▸ 토스트 남은 시간 표시 끄면 종전(마지막 300ms만) · `perf.boost` 켜면 잠기고 꺼지는지 · 값 취향 = `ui.toast_fade_to`(35)·`ui.toast_bar_spent`(30) · **U-16** 프로필 이름 대/소문자 변경(§18): `biscm` → `BISCM` 저장 → 목록에 남고 `profiles/` 파일 수 = 프로필 수 · 옛 종전 꼴 파일 7개는 저장할 때 `<해시>.conf`로 옮겨지고 옛 파일이 사라지는지 · 삭제 뒤 파일 수 · **U-17** 설정 파일 복사(§19): 상세 폼 저장 아래 `<해시>.conf` + 복사 버튼 → 클립보드 = 파일 이름 · 목록 우클릭 맨 아래 "설정 파일 이름 복사"/"설정 파일 경로 복사" → 파일 이름/전체 경로 · 탐색기에 붙여 넣어 열리는지 · **U-18** 입력 언어 안내(§20): 한글 모드로 비밀번호 칸(접속 창 · 비밀번호 입력 창)에 글자 → 마우스 옆 `입력 언어: 가 KOR` 5초 · 영문 모드 = 안내 없음 · 설정 ▸ 모양 ▸ 가린 입력란 입력 언어 안내 0 = 끔 · **U-19** 편집기 가로 스크롤(§22): 긴 줄 파일에서 ① 마우스 틸트 휠 ② Shift+휠 ③ 편집기 아래 가장자리에 마우스 → 가로 막대 드러남·끌기 — 셋 다 가로로 움직이고 세로 휠은 가로 오프셋을 건드리지 않는지 · 그리드·로그 창도 같은 규칙 · **U-20** 편집기 클릭 안정(§24): 휠로 부분 줄이 걸린 상태에서 빈 줄·글자 클릭 → 캐럿만 이동, 화면·마지막 줄 그대로 · 여러 번 클릭해도 흔들림 없음 · **U-21** 동시 편집(§27): 탭 Shift+클릭 = 연속 3칸 · Ctrl+클릭 = 개별 · 4번째는 무시 · 칸 클릭 = 그 탭 활성(실행 대상) · 각 칸 독립 입력·스크롤 · 미니맵 없음 · 아무 탭 단일 클릭 = 단일 복귀+미니맵 · 설정 `editor.split_max` 1이면 늘 단일 · **U-22** 프로젝트 탐색기(§28): Project ▸ New(저장 창 · `.nsql-project`) → Add Folder → 트리 펼침·필터(하위 폴더 파일도 검색) · 파일 클릭 = `◦` 미리보기 탭 하나 재사용 · 편집하면 `◦` 사라지고 다음 클릭은 새 탭 · 더블클릭/Enter = 정식 · 프로젝트 없을 때 링크 행 클릭 = 열기/새로 · Switch 팔레트 · 재시작 = 마지막 프로젝트 · **U-23** 동시 편집 탭 상단 줄(§29): Shift/Ctrl로 묶인 탭 전부 상단 accent 줄 · 단일 클릭 = 활성만 · **U-24** 파일 열기 다중 선택(§30): 열기 창에서 Shift/Ctrl/방향키/Space/Ctrl+A로 여러 파일(폴더 제외 · 하위 폴더 펼친 파일 포함) → 이름 상자 `"a" "b"` + 안내 줄(개수·크기) → 열기 = 확인 팝업(10개 목록 · 초과분 제외 표시) → 열기 = 탭이 순서대로 채워지고 그사이 다른 탭 편집 가능 · 저장/폴더 창은 단일 · **U-25** 시작 모드(§31): `nexa-sql a.sql b.sql` = 파일 모드로 둘 다 열림 · `nexa-sql x.nsql-project` = 프로젝트 모드 · 두 번째 인스턴스(인자 없음) = 파일 모드 · 설정 `project.restore_last` 켜면 첫 인스턴스만 마지막 프로젝트 · **U-26** IME 안내(§34): 접속 창 비밀번호 칸에서 한/영 전환 → 칸 아래 카드 표시/즉시 사라짐 · 입력 창 HIDE 칸도 · 미리보기 탭 더블클릭 = `◦` 제거 · **U-27** 경로 축약(§38): File ▸ 최근 파일 긴 경로 = 가운데 … · Alt 누르는 동안 전체 경로(메뉴 열린 채 · 팔레트 · 파일 검색 결과 · 우클릭 메뉴) · 놓으면 다시 축약 · 설정 `ui.menu_max_width` · **U-28** 실행 Facade(§43): 자동 페치(커서) = 카드 없음 + 상태줄 "새 SQL 없음" · 다른 문장 실행 뒤 Fetch = "재질의" 카드 · SQL Server = 늘 재질의 카드 · Σ = 건수 카드 · 전체 조회 = 카드+진행+■ · **U-29** 그리드 선택 표시(§44): 셀 클릭 = 행 전체 연한 배경 + 행/열 헤더 진한 선택색 · n×m 끌기 = 걸린 행·열 전부 · NULL 더 흐림 · `grid.row_focus` 끄기/색 변경 즉시 · **U-30** 다중 열기 확인 = 중앙 · Enter = 모두 열기(§45) · **U-31** 실행 카드 시계 `00:00:00.000` · `run.toast_tick_ms`/향상 모드 1초(§48) · **U-32** 실행 카드 누적(§50): Ctrl+\ 두 번 = 완료 위·실행 아래 · 오류 5번 = 5장 · 넘치면 휠 · ■ 빨강(실행 중)/회색(끝)/hover 링 · 최신 최상단 · 픽셀 휠 · follow 켬/끔 · 전용 세션 둘의 카드가 한 스택(§51) · **U-33** 팝업 배타(§55): 풀다운 연 채 편집기 우클릭 = 풀다운 닫히고 편집 메뉴 · 편집 탭 메뉴 연 채 결과 탭 우클릭 = 앞 것 닫힘 · 우클릭 메뉴가 카드 위에 · 메뉴 글자 크기 정상 · **U-34** 바깥 우클릭 통과(§58): 편집 탭/결과 탭/그리드/탐색기 메뉴를 연 채 편집기 본문·다른 탭 바·그리드 셀·탐색기 행을 우클릭 → 앞 메뉴가 닫히고 **그 자리 메뉴가 한 번에** 열리는지(좌클릭도 그 자리 동작이 한 번에) · **U-35** Ctrl+K,Ctrl+D(§59): 단어 위 Ctrl+D → Ctrl+K,Ctrl+D = 선택이 다음 출현으로 옮겨감 · Ctrl+D 두 번 뒤 Ctrl+K,Ctrl+D = 마지막 것만 다음으로 · 이어 Ctrl+D = 그다음(건너뛴 것은 한 바퀴 뒤) · 상태줄 개수 · 맥 ⌘K,⌘D · 설정 ▸ 키 `Quick Skip Next` · **U-36** Edit 메뉴 그룹(§60): Edit ▸ Selection/Brackets/Line/Convert Case/Find/Line Endings에 `›` · hover·→·Enter로 펼침 · ←/Esc 접힘 · 다른 행 hover 접힘 · 하위 항목 클릭/Enter = 실행 · 창을 좁혀 오른쪽 끝에서 열면 왼쪽으로 뒤집혀 잘리지 않음 · **★ U-21~U-36 중 마우스·명령으로 닿는 것은 09-22 자동 점검(journal §62 · `scripts/win-func-check.ps1` S01~S28)으로 ✓ 확인** — 남은 실기 = 키 입력이 필요한 것(파일 창 드래그/Ctrl+Space · Alt 전체 경로 · 실제 Ctrl+K,Ctrl+D 조합 · 맥 프리셋) · **U-37** 선택 되돌리기(§65): 단어 위 Ctrl+D ×3 → Ctrl+U = 마지막 선택 빠짐 · 다시 Ctrl+U · Ctrl+Shift+U = 복귀 · 클릭/화살표 이동 뒤 Ctrl+U = 이전 자리 · 타이핑 뒤 Ctrl+U = 편집 되돌림(Ctrl+Z와 같음) · Shift+→ 여러 번 뒤 Ctrl+U = 한 번에 풀림 · 맥 ⌘U/⌘⇧U · Edit ▸ Undo Selection ▸ · **U-38** 상한(§66): `nsql config set editor.max_occurrences 100` 뒤 150개 단어에서 Alt+F3 → 100에서 멈추고 상태줄 안내 · Ctrl+D도 거부 · **U-39** 다중 선택 ←/→(§67): SELECT 위 Alt+F3 → ← = 모든 S 앞에 캐럿(다중 캐럿 유지) · → = 모든 T 뒤 · 그 뒤 타이핑이 전부에 · Shift+→ = 늘림 · Ctrl+U = 전부 선택으로 복귀 · **U-40** 복사 확인(§68 ③): 65 MB 파일 Ctrl+A → Ctrl+C = 상태줄 안내만 · 3초 안 다시 Ctrl+C = 복사 · `editor.copy_confirm_mb 0`이면 바로 · Ctrl+클릭으로 캐럿 추가가 `editor.max_occurrences`에서 막히는지 · **U-41** 팔레트 한글(§69): 그리드/탐색기를 클릭한 뒤 Ctrl+Shift+P → 한글 타이핑이 조합되는지 · **U-42** 프로젝트(§69): 파일 모드에서 Project ▸ 새 프로젝트 저장… → 저장 폴더가 기본 폴더로 · 프로젝트 모드 전환 · 패널 헤더 클릭 = 전환 팔레트 · 프로젝트 열기 창이 단일 선택인지 · **U-43** 북마크(§70): Ctrl+F2 토글(거터 띠·상태줄 개수) · F2/Shift+F2 순환 · Alt+F2 멀티커서 · Ctrl+Shift+3 → Ctrl+3 · Ctrl+Shift+B 패널(클릭 이동 · Delete · Enter · 필터) · 재시작 뒤 복원(`workspaces/default.nsql-workspace`) · **U-44** 북마크 추적(§70): 북마크 위에 줄 삽입/삭제 → 따라오는지 · 파일을 밖에서 고친 뒤 다시 열면 재탐색되는지(줄이 사라지면 회색 '줄 사라짐') · **U-45** 다중 캐럿 스크롤(§72): SELECT 위 Alt+F3(11곳) → 타이핑 → 화면이 그대로인지 · Enter를 넣어도 보이는 캐럿이 있으면 유지 · **U-46** 북마크 2차(§71): 패널 우클릭(항목/문서/그룹/빈 곳) 메뉴 넷 · 니모닉 ▸ 3 → 거터 숫자 상자 · 새 그룹 → 이름 편집 → 항목 이동 → 그룹 끄기(순회·거터에서 빠짐) → 삭제 2택 · Delete 뒤 토스트 클릭 = 되돌리기  · **U-47** 북마크 3차(§74): `nsql bookmark add <파일> <줄> --label x` 뒤 GUI에서 그 파일 열기 → 거터 띠 · 미니맵 오른쪽 가장자리 점 · 줄 끝 `◆ x` 흐린 글 · 라벨 40자 초과 = … · 설정 Bookmarks ▸ Display 셋 끄기 = 즉시 사라짐 · `nsql bookmark list --md` · **U-48** 🔧 풀다운 Project 7항목 = 팔레트 · 패널 링크와 같은 동작(새 프로젝트 저장 = 파일 저장 창 · 열기 · 전환 = 팔레트 · §75)  · **U-49** 파일 대화상자(열기/저장/폴더) 목록 빈 곳 클릭 = 선택 해제 · 폴더 고르기에서 해제 뒤 [폴더 선택] = 지금 폴더 · 저장은 파일명 유지(§77) · **U-50** 프로젝트 탐색기(§76~78): 셰브론·OS 아이콘 · 설정 Files ▸ 파일·폴더 아이콘 끔 · 향상 모드 켜면 아이콘 사라짐 · 루트 우클릭 Remove/Add · 프로젝트 닫았다 열면 마지막 선택 자리 · 탭 우클릭 Reveal(스크립트 탭·프로젝트 밖 파일 = 비활성) · 탭 전환 시 선택 표시(auto_reveal 켜면 펼침+스크롤) — "열린 파일 목록" 해석 확인  · **U-51** 필터 틀(§80~81): 프로젝트/북마크/확장 패널 Aa·ab·(.*) 토글 · 프로젝트 Path 토글(경로 일치) · 숨김/점 파일 토글 = 설정과 연동(파일 대화상자에서도 같은 값) · 정규식 오류 = 빨간 틀 · 가로 스크롤 · 빈 곳 클릭 해제 · '일치 없음/상한' 두 줄 · **U-52** 변수 확장 시점(§82): `vars.expand_at=use`로 `DEFINE v1 = 2` → `DEFINE v2 = &v1 + 5` → `DEFINE v1 = 5` → `SELECT &v2` = `5 + 5`(기본 assign = `2 + 5`) · `DEFINE` 목록에 `원문 → 값`  · **U-53** 탐색기 루트 DBMS 아이콘(§86): 프로필별 틀 색(Oracle 빨강 등) + 라벨 줄(ORA/CLE · MS/SQL · PG/SQL …) · 프로필 이름에 tibero/mariadb = 그 파일 · 모르는 ODBC = generic + 두 글자 · 라벨이 틀보다 커 보이면 알려 주세요(글꼴 px 제약) · **U-54** 탭 유형별 활성 줄 색(§85): 스크립트 탭 = 경고색 · 파일 탭 = 강조색 · 미리보기 = 흐린색 · 설정 Editor ▸ Tab line 셋으로 바꾸기 · 미리보기 탭 우클릭 Keep Open · 파일 탭은 Rename 비활성  · **U-55** 빈 곳 클릭 = 선택 해제 + 캐럿 테두리(§88): 프로젝트/북마크/객체 탐색기 · 파일 대화상자 — 테두리 행에서 ↓/↑ 이동  · **U-56** 작업 환경(§89): 파일 둘 + 스크립트 열고 캐럿 옮긴 뒤 프로젝트 닫기/재시작 → 탭·활성·캐럿(파일을 밖에서 몇 줄 바꿔도 같은 줄로)·북마크 복원 · OPEN FILES 클릭 = 탭 전환 · 설정 Project ▸ 자동 저장 끄면 종료 때 팝업 · 미저장 파일 탭 저장 확인  · **U-57** 보완 일괄(§90): 프로젝트 열린 채 북마크 추가 → 프로젝트 파일에만 기록 · `nsql bookmark list --project x.nsql-project` · OPEN FILES × 닫기 · 변수 창 `&이름` DEFINE 행(편집 → 다음 실행 반영) · 파일 탭 편집 후 30초 뒤 backups/에 스냅숏 · 재시작 복원 시 미저장 편집 복원(파일을 밖에서 바꾸면 로그) · `vars.expand_at=use`에서 `EXEC :V2 := :V1 + 5` → `:V1` 바꾼 뒤 `SELECT :V2` = 재계산 · 북마크 패널 클릭 = 미리보기 탭 · **U-58** 성능 재점검(§91): 프로젝트 패널을 연 채 5초 뒤 스레드 `nexa-fs-icons`가 사라지고(작업 관리자/Process Explorer) 다시 스크롤해도 아이콘이 그대로(캐시) · 새 폴더를 펼치면 워커가 다시 떠서 아이콘이 채워짐 · **U-59** 프로젝트 저장/복원 전면(§92): 미저장 탭+파일 탭 Ctrl+클릭 묶음 = 주황/파랑 각자 · 북마크 패널 더블클릭 = 정식 탭(한 번 = ◦) · Project ▸ 저장 뒤 파일에 `tabs`(경로)·`expanded`·`panel`·`profiles` · 저장 여러 번 = 탭 그대로 · 다른 프로젝트 열기 = 옛 탭 닫히고 교체 + 보이던 패널 + 탐색기 펼침 + 검색어 + "마지막 접속: … 직접 접속" 토스트 · 종료 후 다시 열면 그대로 · **U-60** 북마크 후속(§95): Ctrl+K,Ctrl+K 토글 · K,N/K,P 이동 · K,X 지우기(HHKB에서 Ctrl+F2 대신) · 거터 = 북마크 영역(띠·니모닉) → 줄 번호 → 본문 → 미니맵 · 줄 번호 꺼도 북마크 영역 유지 · 파일 a 니모닉 1 + 파일 b 니모닉 1·2 → Project ▸ 저장 → 패널 번호 그대로(1 / 1·2) · **U-61** 실행 카드(§96): 1행 오른쪽 복사 아이콘에 마우스 = 밝아짐 · 클릭 = 클립보드에 그 실행 SQL + 상태줄 "SQL n줄 복사됨" · 카드는 남음 · **U-62** 복사 버튼 피드백(§97): 클릭 순간 눌림(작아지고 진해짐) → 녹색 체크 → 2초 뒤 복사 아이콘 · 접속 창 파일 이름 복사 버튼도 같음 · `ui.copy_feedback_ms` 바꾸면 즉시 · **U-63** 프로젝트 닫기(§98): 탭 여럿·북마크 있는 프로젝트를 닫으면 빈 Script_1 하나 + 좌측 탐색기만 + 북마크 패널 비어 있음(파일 모드에서 만든 프로젝트) · 다시 열면 전부 복원 · **U-64** 작업 모드 셋(§99): `nexa-sql .`로 켜서 북마크 토글 → 그 폴더에 `.nsql\workspaces\default.nsql-workspace` 생김 · 인자 없이 켜면 `%APPDATA%` 것 · 폴더 모드에서 `${workspaceFolder}` = 그 폴더 · 프로젝트 닫으면 폴더 모드 북마크로 복귀 · **U-65** OPEN FILES(§100): 미저장 탭 = 작은 점(5px) · 점 자리가 예약돼 파일명이 전부 같은 x · 점 자리 시작 = "열린 파일" 머리글 시작 · **U-66** 미저장 본문 자동 복구(§101): 파일 탭 고친 채 프로젝트 저장(또는 30초) → 프로젝트 파일에 `text`·`hash` → 앱 종료 → 다시 열면 그 편집본이 ●로 올라옴 · 그 사이 파일을 밖에서 고치면 로그에 "밖에서 바뀜" · Script_N 탭에 찍은 북마크가 다시 열어도 그 줄에 · **U-67** 파일 형식 v2(§102): 프로젝트 파일을 열어 보면 JSON 헤더 아래 `%%NSQL-BLOBS%%` + `tab=<id> len=<n>` + 본문 그대로 · 헤더에 `"text":` 없음 · `nsql bookmark add --project` 뒤에도 블록이 그대로 · 옛 v1 파일도 열림 · **U-68** 프로젝트 없이 프로젝트 패널 = OPEN FILES(§103 · 클릭 = 탭 전환 · × 닫기) · **U-69** 저장 보증(§104): 파일 모드에서 북마크를 바꾸고 **가만히 두면** 5초 안에 `default.nsql-workspace` 수정 시각 갱신 · 바꾼 직후 **창 X로 종료**해도 파일에 반영(프로젝트 열린 채면 프로젝트 파일도) · **U-70** 이름 없는 탭 북마크(§105): 파일 모드에서 Script_N 탭에 북마크 → 패널에는 보이지만 `default.nsql-workspace`에는 안 들어감 · 앱을 다시 켜면 그 항목 없음 · 프로젝트 모드에서는 남고 복원됨 · **U-71** 거터 우클릭 메뉴(§107): 북마크 없는 줄의 거터(줄 번호 왼쪽 띠 자리) 우클릭 → "북마크 토글" 활성·"제거" 흐림 · 있는 줄 → 반대 · 니모닉 ▸에서 숫자 고르면 지정(있으면 ✓) · "니모닉 해제"는 니모닉 있을 때만 · 본문 우클릭은 편집 메뉴 그대로 · **정정(§108)** = 맨 위 'Bookmarks'(패널 열기) · 토글 하나(있으면 ✓ · 제거 항목 없음) · 마지막 줄 아래 빈 영역 = Bookmarks만 · 메뉴 폭 170 · **U-72** 북마크 패널(§109): 항목 우클릭 ▸ 이름 → 입력 상자가 니모닉·줄번호 다음 이름 자리에만 · 이름 있는 항목 = 이름 강조색·굵게 + 본문 흐림 · 필터 상자에 영문/한글 타이핑 → 목록이 즉시 걸러지는지(조합 중 자모로도) · **U-73** 한글 자모 검색(§110): 프로젝트 필터 · Extension 검색 · 찾기/바꾸기(Ctrl+F) · 파일 검색(Ctrl+Shift+F) · 북마크 필터에서 'ㄱ'·'기'·'내'를 치는 순간 결과가 나오는지(조합 완성 전) · 정규식 모드는 그대로 · **U-74** 파일 검색(§111): 범위 비움 = 파일 모드에서 열린 파일만 · `nexa-sql .` = 그 폴더 이하 · 프로젝트 = 프로젝트 폴더 + 추가 폴더 · 결과 한 번 클릭 = ◦ 미리보기 탭(포커스는 패널 · ↓로 다음 결과) · 더블클릭/Enter = 정식 + 편집기 포커스 · 파일 행 셰브론 모양 · **U-75** 쌍 강조(§112): 셸 파일 `"${X:-$HOME}"`의 `"` 옆에 캐럿 = 짝 밑줄 · `'{printf "%s", $1}'` 안 `{`·`"` 짝 · SQL `'('`가 빨개지지 않음 · 설정 Editor ▸ 쌍으로 다룰 종류에서 `""` 지우면 따옴표 쌍 사라짐 · 문자열 안의 쌍 끄면 `'…'` 안 `{`는 무시 · Rainbow Pairs 확장을 꺼도 현재 쌍 밑줄·자동 닫기 유지 · **U-76** 줄 변경 표시(§113): 빈 줄 여럿 사이에서 Enter → 표시가 그 줄(캐럿 줄)에 · 빈 줄 하나 지우기 = 쐐기가 그 자리 · 줄 번호 끄고도 표시 · **U-77** WASM 확장(§116 · 75 §6): Extension Manager 켬 → Install → Rainbow Pairs 1.1.0(wasm) → 로그 'wasm extension loaded: rainbow-pairs … ms' → 괄호 색·우클릭 괄호 이동 ▸·Ctrl+M 동작 · Disable/Enable · Remove → 'unloaded' + 내장 복귀(설치 기록 없으면 색 없음) · `rainbow_pairs.wasm`을 일부러 망가뜨려 설치(sha256 거부) · hello-ext 설치 → 팔레트 'Hello: Say Hi' → 로그 'hello from wasm' · **U-78** 코드 완성(§117): 접속 뒤 `SELECT e.` 타이핑 → 즉시 팝업(컬럼 = 탐색기에서 펼친 테이블은 타입까지 · 안 펼친 것은 '컬럼 불러오는 중…' 뒤 다시 열면 채워짐) · `FROM em` 2자 뒤 250ms 팝업(테이블·스키마) · Ctrl+Space 수동 · ↑↓ Enter/Tab 확정 · Esc · 타이핑 계속 = 다시 거름 · Backspace · 문자열/주석 안에서는 안 뜸 · `sc` → SALES_CUSTOMER(퍼지) · 설정 Editors ▸ Code completion 바꾸면 즉시 · **U-79** Goto Symbol(§117): Ctrl+R → 심볼 목록(문장 · DEFINE · CTE · 프로시저 …) → 타이핑 필터 → Enter = 그 줄 · PL/SQL 패키지 파일에서 서브프로그램 깊이 · **U-80** 아웃라인 패널(§117): 활동 막대 아웃라인 → 심볼 목록(줄 번호 · 이름 · 종류) · 편집하면 잠시 뒤 갱신 · 탭 바꾸면 그 탭 · 클릭 = 이동 · 필터(한글 자모) · 큰 파일(L1+) = 비어 있음 · **U-81** 북마크 문서 이름 추종(§118): 이름 없는 탭에 북마크 → 탭 우클릭 ▸ 이름 바꾸기 → 패널 문서 행이 즉시 새 이름 · 접어 둔 문서는 이름이 바뀌어도 접힌 채 · 그 탭을 닫으면 `(closed tab #id)` · 프로젝트 모드에서 저장/재시작 뒤에도 새 탭 이름으로 · **U-82** 스크롤 끝 클릭(§118): 휠로 끝까지 내려 위 줄이 반쯤 걸린 상태에서 온전히 보이는 마지막 줄 클릭 → 화면이 줄 경계로 튀지 않고 캐럿만 · ↓로 화면 밖 줄로 가면 그때 따라감 · **U-83** SQL `''` 이스케이프(§118): `DEFINE who = 'O''Neil'`에서 첫 `'` 옆에 캐럿 → 짝 밑줄이 마지막 `'`(가운데 `''`는 아님) · `SELECT 'it''s' FROM DUAL` · `"a""b"` 식별자도 같음 · Plain Text 탭에서는 종전(`''` = 빈 쌍) · **U-84** 검색어 이력(§119): Ctrl+F에 `abc` Enter · `def` Enter → 상자에서 ↑ = def · ↑ = abc · ↓ = def · ↓ = 치던 글 · Ctrl+Shift+F 검색어/범위 · 프로젝트/북마크/확장/아웃라인 필터에 글 넣고 다른 곳 클릭(포커스 잃음) 뒤 ↑ · 설정 창 검색 Enter 뒤 ↑ · 앱 재시작 뒤에도 남는지(`%APPDATA%\nexa-sql\search-history.json`) · 설정 ▸ Files ▸ 검색 상자별 최근 검색어 수 0 = 기록 안 됨 · 2로 줄이면 잘림 · **U-85** 프로젝트 필터 워커 열거(§120): Downloads 같은 1만 개 폴더를 프로젝트에 넣고 필터에 글자 → 타이핑이 안 멈추고 "열거 중… 폴더 N개"가 오르다 사라짐 · 결과가 도착하는 대로 늘어남 · 글을 바꾸면 즉시 새 열거 · 권한 없는 폴더(예: `C:\System Volume Information`)를 넣으면 "읽지 못한 폴더 1개" + 로그 창 `project filter: skipped …` · 설정 Project ▸ 필터 열거 상한 1000으로 두면 "1000개에서 멈췄습니다" · 스레드 수 바꾸기 · 작업 관리자에서 열거 중에만 `nsql-parwalk` 스레드 · **U-86** 파일 검색 제외 로그(§120): 1 MB 넘는 `.sql`과 이진 파일(`.xlsx`)이 있는 폴더에서 Ctrl+Shift+F → 결과 아래 "제외됨(N)" 접힌 행 · 클릭 = 경로 — 이유(크기 초과: 1200 KB > 1024 KB · 이진 파일) · 상태줄 "· 제외 N" · `nsql grep SELECT <폴더>` 끝에 `skipped N file(s): …` · `NSQL_GREP_LOG=1`이면 목록 · **U-87** 검색 입력란 × 지우기(§121): 찾기(Ctrl+F)·바꾸기·파일 검색 검색어/범위·프로젝트/북마크/확장/아웃라인 필터·접속 창 필터·설정 검색·트랜잭션 로그 검색·팔레트에 글을 넣으면 오른쪽 끝에 × · 클릭 = 비움 + 목록/찾기 즉시 복귀 · 빈 상자엔 안 보임 · 긴 글이 × 밑으로 안 들어감 · **U-88** 파일 검색 범위 필터(§122): 범위 상자에 `*.sql` → sql만 · `*.sql, *.txt` → 둘 다 · `-*.log` → 제외 · `*.sql, -*_test.sql, D:\proj` → 조합 · 패턴만 적으면 기본 범위 + 패턴 · `+pat` 명시 포함 · `nsql grep SELECT . -g *.sql --exclude *_test.sql` · **U-89** × 뒤 재적용(§123): 접속 창 필터에 글 → 목록 줄어듦 → × → 전체 목록 복귀 · 파일 검색 검색어 × → 결과·Skipped·상태 비워짐 · 나머지 상자(프로젝트/북마크/확장/아웃라인 필터 · 설정 · 트랜잭션 로그 · 팔레트 · 찾기)도 × 뒤 즉시 원래 목록 · **U-90** 검색 결과 가로 스크롤(§124): 긴 경로/일치 줄에서 틸트 휠·Shift+휠·아래 가장자리 막대 · **U-91** Alt 전체 경로(§124): Alt 누른 채 제외 행·파일 행이 전체 경로로 보이고 가로 스크롤로 끝까지 · **U-92** 열린 탭 범위 필터(§124): `+*.yaml`로 검색 → `.sql` 탭 결과가 안 나오는지 · `*.sql`이면 파일 탭만 · 이름 없는 탭은 `*` 없이 이름으로(`Script*`) · **U-93** Oracle 아이콘 라벨(§124): 글꼴 크기 키워도 `ORA/CLE`가 원통 안 · 다른 DBMS(MS/SQL · PG/SQL)도 · **U-116** JOIN 2테이블에서 SELECT 절 접두 없이 완성 → 두 테이블 컬럼(오른쪽 alias) · Enter = `A.컬럼` · Alt+Enter = 컬럼만 · **U-115** 창이 뒤에 있을 때 캐럿 고정·CPU 0 · `gfx.mac_present=iosurface` 실기(빈 창 없음) · **U-114** 팝업 1항목도 10행 · `이름 : 타입`만 · ⌘⇧? / ⌘⌥/ 블록 주석 · `B.`(JOIN alias) 컬럼 · **U-113** 카드 설명 행(테이블/컬럼 코멘트 있는 객체 · M4PLAN `MS_Description`) · **U-110** `A.`(ALL_TABLES) 컬럼 즉시(사전 0.2 s) · **U-111** ⌘/ 줄 주석 · ⌘⇧/ 블록 주석(선택/캐럿 · 두 번 = 원복) · **U-112** 팝업 트랙패드 끝에서 안 튐 · **U-105** 상세 카드(컬럼/테이블/함수 · 투명도 · `#i/n`) · **U-106** 클릭 = 선택 · 더블 클릭/Enter 확정 · ←/→ 가로 · 트랙 클릭 · **U-107** 자동/수동 팝업 같은 자리 · **U-108** `ALL_TABLES` 컬럼 즉시(stderr `[meta]` 시간) · **U-109** 접두 없음 = 순번 · `itcd` 약어 순 · **U-100** FROM 뒤 접두 없이 현재 스키마 테이블(M4PLAN `dbo` · `M4S_` → `M4S_*` 전부 · 300개 넘는 스키마도) · **U-101** `MI40` → `M4S_I002040` · **U-102** `sales.`처럼 다른 스키마 = 치는 순간 "객체 불러오는 중…" → 채워짐 · 그 이름을 지우고 잠시 뒤 로그 창(개발자 모드) `[intel] reclaimed` · **U-103** BISCM에서 `FROM ALL_`/`DBA_`/`V$` = 권한 있는 것만 · `FROM sys.` (M4PLAN) = 카탈로그 뷰 뒷부분 · **U-104** `intel.preload` 끄면 첫 완성 때 읽기 · **U-94** 이진 파일(§125): 검색 결과에서 `.o`/`.exe`를 미리보기로 열고 아웃라인 패널·Ctrl+R·Ctrl+Space → 앱 유지 · 빈 아웃라인 · 상태줄 안내 · Plain Text 구문 탭(`.txt`)에서도 아웃라인 비어 있음 · **U-95** 짝 찾기 fail-over(§126): `DEFINE who = "O'Neil"` 다음 줄 `SELECT 'a' || 'b'`에서 `'a'`·`'b'` 짝이 정상 · `"…"`가 문자열 색 · 짝 없는 `'`가 있는 줄 다음 줄부터 괄호·인용부호 짝 정상 · `"a""b"` 이스케이프 · **U-96** 검색어 이력 드롭다운(§127): 검색 상자(찾기·파일 검색·프로젝트/북마크/아웃라인/확장 필터·설정 검색)를 클릭 → 아래 최근 5개(설정 Files ▸ 드롭다운 행 수) · 6개 넘으면 오른쪽 스크롤 표시 + 휠 · ↑↓/PgUp/PgDn/Home/End · Enter/클릭 = 넣기 · 타이핑하면 그 글이 든 것만 · Esc · 마지막 항목에서 ↓ 한 번 더 = 목록(폴더/북마크/심볼/결과)으로 포커스 · Tab도 · 설정 보기 방식 Flat = 상자 안 ↑/↓만 · 치던 글에서 ↓ = 목록으로 · **U-97** 내장 함수·시그니처(§129): Oracle 접속 탭에서 `SELECT nv` → NVL(오른쪽 열 시그니처) · Enter = `NVL()` 캐럿 안 + 상태줄 `ƒ NVL(expr1, expr2)` · `SELECT sysd` → SYSDATE 괄호 없음 · `DBMS_OUTPUT.` → PUT_LINE… · 팝업 13행 넘으면 스크롤(휠·PgDn) · **U-98** 사전 뷰·카탈로그(§129): `FROM all_t` → ALL_TABLES · SQL Server `FROM sys.` → tables… · PG `FROM pg_catalog.` · 접속 없이도(SQLite `FROM sqlite_m`) · **U-99** 삽입 옵션·컬럼 조각(§129): 설정 Insert table alias 켜고 `FROM sales_c` → `SALES_CUSTOMER sc` · `INSERT INTO emp (` → 첫 후보 전체 컬럼(탐색기에서 emp 펼친 뒤) · 컬럼 오른쪽 열 `PK NUMBER(4)`·`FK … NOT NULL` · **U-117** 완성 목록 강조(100차 §162): `SELECT * FROM M4S_` → `M4S_` 4글자만 강조색 · `MI40` → 약어 글자 4개 강조 · 이름 전체를 다 치면 그 항목만 굵은 파랑 · hover 행에서도 읽히는지 · **U-118** FROM 뒤 `BISCM.`(§163): 뷰가 테이블 사이에 이름순으로 섞여 보이고 종류 아이콘 표시 · `V_` 같은 접두를 치면 뷰가 위로 · 함수·패키지·프로시저는 테이블·뷰 아래(테이블 함수 `PIPELINED`가 그중 먼저) · 함수 확정 = `NAME()` 캐럿 안 · **U-119** 팝업 밖(편집기·결과)에서 휠/트랙패드 상하·좌우 = 팝업이 닫히고 편집기가 스크롤 · 팝업 안에서는 종전대로 · **U-120** `FROM ORDDATA.ORDDCM_DOCS_USR.` = 팝업 없음 · `FROM ORDDATA.` = 객체 목록 · `SELECT ORDDATA.ORDDCM_DOCS_USR.` = 컬럼 · **U-121** `FROM ` 빈 접두에서 목록 끝 항목 "322개 더 — 계속 입력해 좁히기"가 보이는지(§166 · 페이지 로딩 T-196 전까지는 안내만) · `V`를 치면 `VM4S_I002040`이 나오는지 · **U-122** 메모리 모니터(§167): 상태줄 오른쪽 "NN MB"에 hover/클릭 효과 · 클릭 = 메모리 창 열림/닫힘 · 창의 Top 스위치 = 다른 창 위 유지(설정 `mem.always_on_top`과 동기) · 1초마다 숫자·막대 갱신(큰 결과를 조회하면 결과 데이터 막대가 늘고 탭을 닫으면 줄어드는지) · 창을 닫은 뒤 유휴 CPU가 0인지(활성 상태 보기) — ✅ 09-24 일부: 종료 뒤 반영 · 200행 재조회 즉시 감소(§169) · **U-123** 카드 머무름(§171): 완성 팝업에서 ↓를 누르고 있거나 트랙패드로 빠르게 스크롤할 때 카드가 첫 대상에 머물다 멈춘 뒤 마지막 행으로 한 번에 바뀌는지 · 로그 창(개발자 모드)에 상세 질의가 스크롤 중엔 안 찍히는지 · 카드가 완전 불투명(뒤 그리드가 안 비침)인지 · **U-124** 메모리 창(또는 세션·변수 창)을 클릭하면 메인 창과 다른 보조 창도 함께 앞으로 오고 고른 창이 맨 위인지(§172 · 설정 `window.focus` group) · **U-125** 팝업 항목 사이 마우스 이동 체감(§173): `gfx.mac_present=iosurface`를 켠 뒤와 비교 · **U-126** `SELECT A.*`(캐럿이 `*` 바로 뒤)에서 ⌃Space → "모든 컬럼 (N)" 하나만 · 카드에 컬럼 목록 · Enter = `A.*`가 `A.컬럼` 목록(한 줄에 하나 · 들여쓰기 유지)으로 · bare `*` + 두 테이블 = 둘 다 별칭 · 아직 안 읽은 테이블이면 "불러오는 중" 뒤 자동 표시(§174) · **U-127** 페이지 로딩(§175): `FROM ` 빈 접두 → 200개 + "322개 더" → 끝까지 휠/↓ 또는 End → 이어 붙어 `VM4S_I002040`이 셋째 페이지에 · 팝업 자리·강조 유지 · `*` 조각 = 줄 앞 쉼표(`A` ↵ `, B`) · 설정에서 여러 줄/Tab으로 바꿔 확인(기본 = 한 줄 · Space) · **U-128** 패키지 멤버(§176 · BISCM 패키지 1개): `FROM BISCM.<패키지>.` → 테이블 함수(있으면) → 함수 · 프로시저 없음 · `SELECT BISCM.<패키지>.` → 멤버 전부(순번 · 타입 `FUNCTION → NUMBER` 등) · 고르면 `NAME()` 캐럿 안 · `FROM BISCM.M4S_I002040.` = 여전히 팝업 없음 · **U-134** 4-DBMS 절별(§183 · 76 §14): Oracle `FROM DU` → `DUAL` · SQL Server 문장 시작 `TO` → `TOP` · `FROM dbo.` 프로시저 없음 · PG `FROM pg_catalog.` · SQLite `FROM sq` → `sqlite_master` · **U-135** 문법 절 키워드(§185): SQLite 탭 `SELECT a, ha` → HAVING 없음 · `… GROUP BY a ha` → HAVING · 문장 시작 `pra` → PRAGMA · Oracle `FROM t co` → CONNECT BY · 설정 폴더 `grammar/sqlite.sqlg`에 `dialect = sqlite` + `[clause SELECT] next += MYWORD`를 두고 재시작 → `SELECT my`에 MYWORD · **U-136** 큰 문서 완성(§187): 1 MB 넘는 스크립트에서 ⌃Space가 캐럿 문장 기준으로 뜨고 문서 낱말은 없는지 · **U-137** REF CURSOR 이어 받기(§189): BISCM `EXEC NSQL_DEMO_PKG.FIND_OBJECTS(:V_NAME, :rc, 500)` → RC 탭 200행 `200+` → 스크롤 끝 = 카드 없이 이어 붙고 로그 "커서에서 N행 이어 받음" · ⇊ = 나머지 전부(500) · 다른 문장을 실행한 뒤 다시 스크롤 끝 = 상태줄 "커서가 닫혀…" 한 줄 + `+` 사라짐(ORA-00900 카드 없음) · `run.cursor_autoshow` 끄고 `PRINT rc`도 같게 · **U-138** DBMS별 트리(§190): BISCM = Tables·Views·Materialized Views·Indexes·Sequences·Queues·Types·Packages·Procedures·Functions·Synonyms·Schema Triggers·Table Triggers·Database Links·Java·Jobs·Scheduler Jobs·Scheduler Programs 순 · 테이블 펼침 = Columns·Constraints·Foreign Keys·References·Triggers·Indexes·Partitions·Dependencies · M4PLAN = Unique Keys·Check Constraints·Extended Properties(MS_Description) · Repository = Extensions(plpgsql) 루트 아래 · **U-139** 유효성 배지(§190): BISCM Procedures의 `SP_CALL_DATA_COPY_KHJ`(INVALID) 아이콘 오른쪽 아래 빨간 점 · `VALID` 글자 없음 · 아이콘 끔(`explorer.icons`)이면 칩 빨강 · **U-140** SQL 생성 메뉴(§190): 테이블 우클릭 ▸ SQL 생성 ▸ SELECT/INSERT/UPDATE/DELETE/MERGE/DDL · 패키지 ▸ CALL/DDL · Constraints 잎 ▸ DDL → 모달 SQL 미리보기(줄 번호 · 미니맵 없음 · 편집 가능 · 제목 = `OBJ_what`) · **U-141** 미리보기 버튼(§190): 새로고침(안내 줄 "생성 중" → 본문 교체) · 파일로 저장…(기본 이름 `OBJ_merge.sql` · 상태줄 "저장했습니다") · 편집기에서 열기(새 탭 + 창 닫힘) · 복사(안내 줄 "N자 복사") · 닫기/Esc · ⌘A/⌘C/⌘V/⌘Z · **U-142** 서버 묶음(§191): BISCM + BISCM_SB(같은 192.168.0.58:1521) 둘 다 접속 → 헤더 하나("연결 2개") 아래 연결 행 둘(`BISCM · BISCM` / `BISCM · BISCM_SB`) · 헤더 우클릭 ▸ 연결 해제 ▸ 모두 해제(2)/각각 · 연결 행 우클릭 ▸ 이 연결 해제 → 그 행이 사라짐 · 설정 `explorer.keep_offline` 켜면 오프라인 행으로 남음 · `explorer.disconnect_pick=always`면 1개여도 하위 메뉴 · **U-143** SQL 미리보기 창(§191): 테이블 ▸ SQL 생성 ▸ DDL → 창이 메인 위 · 다른 앱 갔다 돌아와도 위 · Generate SQL 하위 메뉴 DDL 앞 구분선 · **U-144** 검색창(§192): 탐색기 클릭 → ⌘F → `M4E` 입력 → Tables 아래 M4E_*만 + 안 읽은 폴더 유지 · 일치 글자 강조 · 헤더 "일치 N" · Enter/⇧Enter 이동 · Esc 지움 · Aa·(.*) 토글 · ↑ 이력 · **U-145** SQL 미리보기 표시(§193): DDL 미리보기 본문이 편집기 탭과 같은 글꼴·색·줄 번호(미니맵·북마크 없음) · 하단 체크박스 4개(정규화 이름·간결·전체 DDL·외래 키 분리) 토글 = 즉시 재생성 + 설정에 남음 · M4PLAN 테이블 DDL = 헤더 주석·COLLATE·NULL 명시·인라인 CONSTRAINT·확장 속성 · BISCM = `-- BISCM.T definition` + COMMENT ON · **U-146** 카탈로그 공유(§193): BISCM + BISCM_SB 접속 = 헤더 아래 **칸 하나**(루트 행 `BISCM · BISCM, BISCM_SB`) · 스키마 목록 한 번 · 헤더 우클릭 ▸ 연결 해제 ▸ 계정별 · BISCM(첫 계정)만 끊으면 트리 유지 + 메타 세션 재바인딩(펼침 계속 됨) · **U-147** 시스템 스키마(§193): 기본 = SYS/SYSTEM/CTXSYS/MDSYS/XDB… 숨김(DBeaver와 같은 30개) · 설정 켜면 45개 · 빈 스키마 숨김 켜면 16개 · **U-148** 필터 v2(§193): 필터 중 새 서버 접속·펼침이 즉시 보이고 스크롤 끝까지 감 · 미일치·안 읽은 폴더 숨김 · 일치 0 = 헤더+연결 행만 · `Tables (3/449)` · 필터 지우면 자동 펼침 복귀 · **U-149** 캡처(§191 하단 행 겹침)가 재현되면 창 크기·스크롤 위치와 함께 알려 주기 · **U-150** 글로벌 변수(§194): 변수 창에서 값 선택 → 층 버튼(공유 → 글로벌) 두 번 = Layer `global` · 다른 서버 탭에서 `PRINT :이름` 보임 · 앱 재시작 뒤에도 남음 · `VAR X GLOBAL`/`VAR X TAB` · **U-151** 연결별 칸 원복(§194): BISCM + BISCM_SB = 헤더 아래 칸 둘(각자 트리·완성) · 설정 `explorer.share_catalog` 켜고 다시 접속하면 한 칸 · 서버 헤더가 필터 상자 왼쪽과 정렬 · **U-152** CONNECT 진단(§195): 탭 A `CONNECT …BISCM:BISCM@…` 두 번 = 로그 "기존 접속 유지" · 탭 A에서 `BISCM_SB`로 바꾸면 "다시 접속(계정 다름)" · 탭 B를 따로 두면 둘 다 유지 · **U-171** 패키지 본문(§213): 패키지 우클릭 ▸ 본문 열기 → `CREATE OR REPLACE PACKAGE BODY` 탭 → 고쳐 실행 = 컴파일 · **U-170** 설명 워머(§212): 테이블 폴더를 펼친 뒤 몇 초 지나 테이블·컬럼을 골라도 설명이 즉시(왕복 0) · 코멘트 없는 테이블 = 흐린 NULL(이름 아님) · **U-169** 컬럼 설명(§211): 테이블 클릭 뒤 컬럼들을 차례로 눌러도 설명이 즉시(교체 깜빡임 없음) · 코멘트 없는 컬럼 = 흐린 NULL · 공백 코멘트 = 공백 · **U-168** 객체 상세 축소판(§209): 테이블 = 설명·PK·인덱스만 · 컬럼 = 이름·설명·타입·NULL·기본값 · 프로시저 = 인자 · 뷰 = 사용 테이블 · 머리 줄 = 칩 옆 설명만 · ▼/▲ 채운 삼각형 · 본문 줄 겹침·하단 미표시 없음 · **U-167** 객체 상세 후속·플래시(§208): 코멘트 있는 테이블의 설명 = 코멘트 · 스키마 클릭 = 종류별 개수 · 머리 줄 더블클릭 = 편집기 탭 · ▾/▴ 도형 · 검색 완료 순간 상자 안쪽 번쩍 · **U-166** 객체 상세(§207): View ▸ 객체 상세 → 테이블 클릭 = 섹션 · ▾ 축소/▴ 확장 · 복사(설명) · Shift+복사(종류-이름-설명) · 본문 선택 ⌘C · 스플리터 끌기 · 탐색기 스크롤과 겹치지 않음 · **U-165** 검색 중 새 서버(§206): Oracle 검색 뒤 MSSQL 접속 = 새 서버 헤더에 일치 수·부분 폴더가 바로 나오는지 · **U-164** 변수 입력 창(§205): SQL Server에서 `SELECT :Top`(미선언) 실행 → 입력 창이 메인 앞에 뜨고 Esc = 취소 · **U-163** 북마크 니모닉(§204): 스크래치에서 만든 북마크가 파일 저장·재시작 뒤에도 거터에 숫자 상자로 보이는지 · **U-162** 입력(§203): Oracle 검색어 5자 입력이 즉시 · 첫 글자 뒤 멈춤 없음(Debug) · **U-161** 검색 종료(§202): Oracle에서 검색어를 넣고 앱을 가만히 두어도 `인덱싱 d/T`가 끝까지 오르고 완료(깜빡임 → 초록)되는지 · 필요하면 `NSQL_STARTUP_CMD=@after:9000:explorer.stat:<파일>`로 상태 덤프 · **U-160** 디스크 캐시(§201): 앱을 껐다 켜고 접속 직후(인덱싱 전) 검색·완성에 지난 실행의 객체가 바로 나오는지 · 설정 폴더 `meta/`에 접속마다 파일 하나 · 메모리 창에 메타 L1/L2/L3 세 줄 · **U-159** 메타 3층(§200): Oracle 접속 뒤 검색어를 넣어도 애니메이션이 끊기지 않는지(판정 스레드) · 접속 뒤 ≈8 s 지나 첫 검색 = 즉시 전 스키마 일치(`인덱싱` 표시 없음) · 완성 팝업에서 다른 스키마 객체가 접속 직후부터 뜨는지(L1) · 현재 스키마 테이블 `A.` 컬럼이 첫 요청부터 즉시(L2) · 설정 ▸ Explorer ▸ meta.* 다섯 키 · **U-158** 검색 후속(§199): 검색어를 둔 채 새 서버 접속 = 그 서버도 바로 검색 · 완료(초록) 뒤 상자를 클릭/글 변경하면 기본 테두리로 · 진행 애니메이션 = 혜성 꼬리 + 테두리 물들임이 눈에 띄는지 · **U-157** 검색 진행 표시(§198): 검색어를 넣으면 상자 테두리를 따라 밝은 선이 한 바퀴씩 돌고, 끝나면 두 번 깜빡인 뒤 초록 테두리로 남는지 · 검색어를 지우면 보통 테두리 · Oracle BISCM 스키마 하나 ≈ 0.3 s · **U-154** 검색(§197): 새로 접속한 서버에서 아무 폴더도 안 펼친 채 검색어 입력 → 헤더 `인덱싱 d/T`가 오르고 일치 객체가 `Tables (n/?)` 폴더로 올라온 뒤 `n/전체`로 바뀌는지(Oracle BISCM = 첫 결과 1 s 안 · 전체 30 스키마 ≈ 20 s) · **U-155** 검색어 이력 드롭다운: 행 클릭 = 그 검색어 · 트리/편집기 좌·우클릭 = 즉시 감춤 · **U-156** 접속 뒤 검색 없이 2~3분 두었다가 검색 = 바로 전체 일치(유휴 선적재) · 설정 ▸ Explorer ▸ Search index 다섯 키 · **U-153** 미리보기 토글(§196): 체크박스를 눌러도 탐색기 스크롤이 그대로 · 기본 DDL = CREATE TABLE + 제약 + 인덱스(설명 없음) · 전체 DDL 켜면 설명(확장 속성/COMMENT ON) + Oracle TABLESPACE/STORAGE · 5 MB(L1)에서 자동 팝업은 안 뜨고 ⌃Space는 뜨는지 · 향상 모드 켬 → 팝업에 아이콘·카드 없음 · 설정 창에서 다섯 키 잠김 · **U-129** 인텔리센스 캐시 새로 고침(§177): 다른 도구로 테이블을 만든 뒤 ⌘⇧R → 상태줄 "버킷 N · 객체 M" · 그 뒤 `FROM ` 완성에 새 테이블 · 탐색기 루트/스키마 우클릭 "메타 새로 고침" 같은 동작 · Edit 메뉴 "이 서버"/"전 서버" · **U-130** `ㅁ.`(비식별 글자 + `.`) 뒤 ⌃Space = 팝업 없음(§178 · 종전엔 별칭 컬럼 전체) · **U-131** 이동 반응(§178): 새 Debug 빌드(의존 최적화 + IOSurface 기본)에서 팝업 ↑/↓·마우스 이동 체감 · `[frames] present backend = iosurface`(NSQL_TRACE_FRAMES) · 화면이 비면 설정 `gfx.mac_present=softbuffer` · **U-172** Generate SQL ▸ DDL(패키지) 미리보기 = 스펙 끝 `/` + 빈 줄 + 본문 끝 `/` → 편집기에 붙여 실행 = 둘 다 컴파일(§214 ①) · **U-173** SQL Preview: 드래그 선택 → ⌘C = 선택만 · 선택 없이 ⌘C = 전체 · 우클릭 메뉴 = 잘라내기·붙여넣기 흐림 · 메뉴 항목 클릭이 아래 버튼을 누르지 않음 · ⌘V/⌘Z 무반응 · **U-174** 객체 상세 우클릭 메뉴 표시·복사·전체 선택 · 바깥 클릭 = 닫힘 + 그 클릭 진행 · **U-175** 상세 창(포커스)에서 한글 자모 입력 = 아무 표시 없음 · **U-176** 상세 창 스플리터: 확장 상태에서 띠 위 커서 ↕ · 드래그 · 헤더+4줄 아래로 안 줄어듦 · 축소(▼) = 헤더만 바닥 · 띠 없음 | 61 63 76 | ⏳ |
| **T-155** | P1 | 소 | ✅ 09-21 89차(win) — **자동 커밋 모드의 빈 `COMMIT` 제거**: 서버가 문장마다 커밋하는 DBMS(PG·SQLite·SQL Server·MySQL)에서는 열린 트랜잭션(러너가 연 것·사용자 `BEGIN`)이 있을 때만 보낸다(`Caps::autocommit_needs_commit` MC/DC · 문장마다 왕복 1회 절약 · PG 경고 제거) · CLI 종료 커밋 = `Runner::commit_at_exit` | 56 63 | ✅ |
| **T-156** | P2 | 중 | 📐 **드라이버 배포 구성**([64 §3](64-dbms-clients-and-driver-packaging.md) · 사용자 09-21 검토 요청): 결론 = 순수 Rust 드라이버·Oracle 드라이버 코드는 내장 유지. 후속 후보 — ① **경량 배포판**(Cargo feature 조합 · CI 매트릭스 한 줄) ② **Instant Client 내려받기 도우미**(재배포 조건 확인 뒤 · D-143) ③ `nsql-abi` + cdylib 프록시 세션(첫 실제 확장 = ODBC) ④ 프로세스 모드(Oracle 여러 버전) | 64 22 | ☐ |
| **T-157** | P2 | 소 | **DBMS 그룹의 다음 칸**([64 §1](64-dbms-clients-and-driver-packaging.md)): PostgreSQL TLS(`rustls` · `sslrootcert`) · SQL Server 인증서 신뢰·통합 인증 검토 · ODBC 드라이버 관리자/DSN 탐지(드라이버 도입 때) — 같은 틀(자동 탐지 / 직접 지정 / 읽기 전용 정보 `INFO_KEYS`) | 64 | ☐ |
| **T-158** | P2 | 소 | **드래그 자동 스크롤을 타이머식으로**(nexa-ctl · 89차 추가의 후속): 지금은 포인터가 밖에 있어도 **움직일 때만** 선택이 늘어난다 → 밖에 가만히 둬도 거리 비례 속도로 계속 스크롤(VS Code·Sublime) · `tick` 기반(새 타이머·스레드 0 · 드래그 중에만 · 39 §3 등재) | 55 61 — ✅ 09-21 추가 24(`TextBox::tick` · `drag_autoscroll_active` · 50 ms · 거리 비례) | ✅ |
| **T-159** | P2 | 소 | **Oracle thin(`oracledb` 순수 Rust · Oracle 공식) 재스파이크**([64 §4-2](64-dbms-clients-and-driver-packaging.md)): OUT 바인드·REF CURSOR·DBMS_OUTPUT·취소가 되는지(09-13에는 없었다 · 09-21 최신 = 여전히 beta.3 · 소스에 `out_bind_data`) → 되면 `nsql-driver-oracle-thin` + 설정 `oracle.driver = oci|thin` + **경량판 C**(Instant Client 없이 Oracle) | 64 22 | ☐ |
| **T-160** | P2 | 중 | **탭 제자리 이름 편집**(nexa-ctl `TabBar` — 탭 안에서 바로 글자 편집 · 브라우저/시트 탭 방식 · 89차 추가 3의 추천 ②) + 입력하는 대로 제목 미리보기 — 지금 = 탭에 붙는 입력 상자 + 강조 테두리 | 61 | ☐ |
| **T-161** | P3 | 소 | nexa-ui `nexa-fs` 파일 감시 테스트가 가끔 실패(09-21 1회 · 재실행 통과 — 타이밍 의존) → 대기 시간을 조건 대기로 | — — ✅ 09-21 추가 24(표지 요청 둘로 조건 대기). 같은 날 `shell::folder_icon_and_kind_come_from_shell`이 전체 실행에서 1회 실패 뒤 통과(Windows 셸 아이콘 API 의존) — 되풀이되면 그때 본다 | ✅ |
| **T-162** | P2 | 소 | **변수 샘플 검증에서 나온 흠**(09-21 89차 추가 6 · `examples/variables/*.sql`): ✅ SQL Server `EXEC :V := 서버 식`이 오류 없이 빈 값(`SET @V`를 세션 옵션으로 오인 → `needs_declare_prepend`) 수정. **남음**: ① SQLite의 `@x` · `$x` 표기가 변수로 묶이지 않는다(`Wrong number of parameters` — [63 §3-1](63-variable-management.md)의 "한 변수"는 설계만) ② SQL Server `EXEC SELECT 식 INTO :V`(**FROM 없음**)가 구문 오류(Msg 102 — `rewrite_select_into_tsql`) ③ CLI의 대입 메아리(`:V_API_TOKEN = abc123`)가 **비밀 이름 규칙(D-140)을 안 거친다**(`SHOW VARIABLES`는 가림) ④ `DEFINE v = O'Neil`(맨 작은따옴표)이 뒤 문장들을 글자 상수로 삼킨다 → 큰따옴표로 감싸야 함(안내 또는 DEFINE 줄은 줄 끝에서 끊기) ⑤ SQL Server에서 선언 없는 변수가 받은 수가 `VARCHAR2`로 남는다(`V_CNT VARCHAR2(3) 797`) ⑥ `nsql plan -d postgres`는 서버 식 대입 뒤의 `PRINT`를 "변수 없음"으로 센다(접속 없는 plan의 한계 — 오류 대신 안내로) | 63 · **09-21 추가 24: ② ✅(FROM 없는 꼴) ③ ✅(메아리·PRINT 가림)** · 남음 = ①④⑤⑥ | 🔧 |
| **T-165** | P1 | 대 | **프로젝트(워크스페이스) 관리** — [67](67-project-workspace.md) P1~P5: `nsql-project` 크레이트(모델·원자적 저장·스냅숏 `backups/`·`close_plan` MC/DC) → 메뉴 Project + 팔레트(New/Open/Recent/Switch/Save/Save As/Close/Add·Remove Folder) → hot exit(디바운스 1s+5s · 묵시 복원 · 닫기 3택/개별) → 탐색기 패널(OPEN FILES 자동 높이 + FOLDERS + sash · 활동 막대 `view.project`) → 파일 검색 연동 · 캡처 · 문서. 결정 D-144~149 권장안으로 착수(사용자 확인). **🚧 09-22(journal §28)**: P1(모델 `project.rs` — 크레이트 대신 GUI 모듈 · JSON) · P2(메뉴 Project · 팔레트 · 대화상자 `FilePurpose::Project` · 최근 · Switch · `project.last`) · P4 일부(FOLDERS 트리 · 깊이 무관 필터 · **미리보기 탭** `open_preview`/승격 · 빈 상태 링크) ✅ → 남음 **P3 = [70 자동 저장·저장 큐·복원](70-autosave-and-restore.md)(P3-a~e · D-178~183 사용자 확인)** · OPEN FILES/sash · 감시 · 우클릭 · 활성 탭 동기 · P5 | 67 · 18 · 36 · 58 · 70 | 🚧 |
| **T-167** | P1 | 대 | **북마크 관리** — [69](69-bookmarks.md) U-1·U-2(nexa-ui 선행: `merge3::line_map` 공개 · 거터 글리프 마크 `set_gutter_glyphs`/줄 끝 주석 `set_line_annotations`/미니맵 마크) + B1~B8: `nsql-bookmarks` 크레이트(의존 0 · `DocKey{File\|Object\|Scratch}`·`Anchor`·`State` · 줄 보정 · **재탐색**(diff 매핑 → 줄 원문 → 양방향 탐색 → 유일 일치 → 무효) · 정리 · 상한) → 편집기 배선(`TextBuf` 줄 변경 기록 소비 · 토글/이동/니모닉 0-9 문서별 · 거터·미니맵·인라인) → 저장소 포트(워크스페이스 → 프로젝트) → **B4a~d 관리자 패널**(활동 막대 `view.bookmarks` · `bookmarks_panel.rs` = search_panel 골격 · 그룹→문서→항목 · 필터+↑↓ · 가상 스크롤 · 좌클릭 미리보기/더블클릭 이동 · 우클릭 메뉴 넷 · 그룹 만들기/기본/켜기끄기/삭제 2택 · 제거 5초 실행 취소 · **설정 새 그룹 `Bookmarks` 분류 셋 · 키 38**) → 객체 DDL 문서(`OpenSql` `origin` · 확인 후 접속 재개 · 파일 저장 시 복사본) → 무효·부활·`stale_days` 정리 → 공용/개인 2단 → CLI `nsql bookmark`. 결정 **D-156~177**(사용자 09-22 5라운드 + 설계 판단 D-171·176·177) · 저장은 **67 프로젝트/워크스페이스 파일에 얹는다**(별도 파일 없음) · **🚧 09-22 1차 = B1·B3·B4a ✅ · B2·B4d 🚧 · 남음 U-1·U-2·B4b·B4c·B5~B8(journal §70)** | 69 · 67(T-165) · 58 · 59 · 60 · 57 | 🚧 |
| **T-168** | P2 | 소 | **성능 점검 시나리오·도구 보강**([71](71-performance-review-process.md)): ① 보조 창 4 시나리오는 창마다 `@after:` 시차를 줘야 표본이 안정된다(09-22에 off 3개/on 4개가 섞여 "향상 모드가 더 무겁다"처럼 보였다 · [45 §4-2](45-perf-boost-benchmark.md)) ② 향상 모드 A/B에 **입력 지연·프레임 시간**을 붙인다(`NSQL_TRACE_FRAMES`·`win-latency-probe.ps1`) ③ macOS·Linux의 향상 모드 A/B 자동화([71 §7](71-performance-review-process.md) 표의 빈칸) ④ ~~E·F 단계 미실행~~ ✅ 09-22에 A~F 전 단계 실행 · 남은 것은 **G(병목 추적)를 자동으로 트리거**하는 조건(판정선을 넘으면 `[frames]`·`[startup]`을 자동 수집) | 71 · 45 · 39 | ☐ |
| **T-166** | P1 | 대 | **확장 개발자 API · Rainbow Pairs 분리** — [68](68-extension-developer-api.md) E1~E8(API 크레이트 · 편집기 장식 API · in-process 재작성 · WASM `wasmi` 실행기 · 로딩/해제 관리자 · 매니저 wasm 설치·서명·갱신 · SDK·샘플·Rainbow Pairs wasm 게시 · CLI `nsql ext`). **결정 D-87 · D-150~155 답 뒤 착수** | 68 · 50 · 51 · 22 | ⏳ 결정 대기 |
| **T-164** | P1 | 중 | **트랜잭션 실기 — [66](66-transaction-test-scenarios.md)의 DBMS별 대본을 위에서 아래로 복사 실행**(§4 Oracle · §5 SQL Server · §6 PostgreSQL · §7 SQLite · 급하면 §8 3분 점검)(§9 기록표 · 두 번째 세션 도구로 서버 상태 확인 · ★ B6 롤백 실효 = T-146 · B9 PG aborted · B10~11 암묵 커밋 · C1~C11 잠금 방지는 §0-2 단축값으로) · ❌는 결함으로 등록 · 자동화가 덮는 것은 §10 | 66 · 34 · 44 · 56 | ⏳ |
| **T-163** | P2 | 소 | **Linux 실기 확인표**(09-22 91차 · 자동화가 못 보는 것): ① 한글 IME — 기본 백엔드가 X11(XWayland)이라 XIM 경로(`gfx.linux_backend = wayland`면 winit `Ime`) · 편집기·입력 창·설정 검색 ② 창 11곳 열림·그리기(2코어 VM에서 첫 페인트 24~66 ms) ③ 모달(접속·파일·비밀번호) = 메인에 붙은 창(`WM_TRANSIENT_FOR`+MODAL · journal 09-22 §12): 메인 클릭 → 모달이 위에 남는지 · 함께 최소화 · Esc 뒤 메인 복귀 · 분수 배율에서 글자 흐림 여부 ④ 탐색기 우클릭·툴바 드래그·그리드 드래그(마우스 라우팅) ⑤ 화면 캡처 도구(`grim`/`gnome-screenshot`) 정착 · 데스크톱 런처에서 Oracle 직접 지정(`oracle.client_dir`) ⑥ 독·창 전환기의 앱 아이콘(`app_id` = `.desktop` · `scripts/install-desktop-linux.sh`) ⑦ ★ **비밀번호 없는 접속의 비밀번호 창이 뜨지 않음**(사용자 09-22 Linux 실기 · T-132 ②): 시작 인자 `oracle://BISCM@host/BISCM` 경로는 격리 인스턴스에서 창이 뜬다(`xwininfo` 매핑·transient·포커스 확인 · X11 백엔드) → 확인할 것 = 사용자가 쓴 경로(편집기 `CONNECT user@host` / 로그인 창 빈 비밀번호 Connect / 프로필 비밀번호 없음) · 그 시점의 백엔드(X11 전환 전 Wayland 빌드였는지 — Wayland는 새 창 활성화가 안 돼 뒤에 숨을 수 있음) · 창이 생기는데 안 보이는지(`xwininfo -root -tree`) vs 아예 안 열리는지(로그 창 `⟨net⟩`·상태줄) · ~~**Windows 세션**: nexa-ui CI windows-latest `test` 실패 원인 확인~~ ✅ 92차(win) = nexa-font 시험의 `set_text_gdi` 경주(`file_type()` 아님) → 가드 `GdiOn` + 걷기 캐시 3-OS 복귀(journal 09-22 §14 · CI windows-latest ✓ e5433cb) | 61 §3 Linux 열 · journal 09-22 | ⏳ |
| **T-148** | P2 | 소 | **DDL 점검의 잔여**([62 §5-2](62-macos-input-and-present.md)) — ✅ 09-21 87차: `SHOW ERR[ORS] [종류 이름]`(러너 `last_unit` + `ALL_ERRORS` 재조회 · "No errors." · Oracle 실기) · `sqlite:////abs` 절대 경로 유지(`sqlite_path`) · 접속 실패 오류 표기 = 대상 방언(CLI · GUI `error_dialect` MC/DC) · 시작 인자 접속의 탐색기 루트 = 프로필 이름 · `NSQL_TRACE_MEM` 맥 측정(`task_info` `phys_footprint`). 잔여: MySQL `DELIMITER`(드라이버 도입 때) · SQLite 열기 실패의 분류가 "Connection lost"로 나온다(분류표 보강) | 62 | ✅(잔여 2) |
| **T-147** | P1 | 중 | **맥 화면 내보내기**([62 §2-1](62-macos-input-and-present.md) · D-133) — 🚧 09-21 87차: nexa-sys `layer_present`(IOSurface 풀 · sRGB 태그) + `present.rs` 한 곳 + 설정 `gfx.mac_present`(기본 softbuffer) · present 36.2 → **2.9 ms** · 프레임 51 → 16 ms · 유휴 CPU ⅓. 잔여: **기본값 전환(D-133)** · `nexa-gfx::Surface` stride(중간 버퍼 21 MB·복사 제거) · 유휴 때 풀 줄이기 · 크기 조절 중/Apple Silicon 실기 · 큰 파일 적재 피크(T-136 D6) | 62 55 | 🚧 |
| **T-146** | P1 | 소 | ✅ 09-21 87차 — **원인 = 수동 커밋 모드가 PG·SQLite·SQL Server·MySQL에서 실제로는 자동 커밋**(서버가 문장마다 커밋하는데 트랜잭션을 여는 코드가 없었다 · INSERT가 "미커밋"으로 보이지만 이미 커밋 = 데이터 안전 결함 · T-137 L1 결함 아님) → 러너 `manual_begin_sql`(MC/DC) + `tx_open` · SQLite 전 경로 테스트. 확인 = 다음 push의 CI `integration` | 56 | ✅(CI 확인 대기) |
| **T-145** | P2 | 중 | **편집마다 본문 전체를 보는 기능의 줄 단위 갱신**([59 §6-4](59-large-file-handling.md)): 줄 변경 표시(기준선 비교)와 괄호 짝 표가 편집마다 본문을 문자열로 다시 만든다(4만 줄 입력 7.5 ms의 대부분) → `TextBuf` 변경 기록으로 바뀐 줄 범위만 · 정규식 찾기의 줄 단위 경로(질의가 한 줄일 때) · 100만 줄 이상에서 줄 표 "계단" 오프셋 |
| **T-144** | P2 | 소 | **되돌리기 후속**([60 §7](60-undo-redo-redesign.md) · ✅ 09-20 84차 — D-129 기록 파일(`undofile.rs`) · D-130 거대 편집 확인 · D-131 쉬었다 치면 새 묶음 · D-132 재로드 = 최소 줄 편집). ⏳ 실기(사용자): 한글 조합 중 큰 파일 입력 · 거대 선택 위 타이핑 안내 · 재시작 뒤 되돌리기 |
| **T-141** | P1 | 중 | **큰 파일 모드**([59 §5](59-large-file-handling.md) · ✅ 09-20 83차 — L1/L2 기능 축소(끄기) · 저장본 사본 0 · 상태줄 칸 · `file.large_force` · `file.large_*` 4키). 잔여(선택): 미니맵 간격 표본 · 괄호 표 범위 · 기준선 = 줄 해시 · 2만 자 넘는 줄 토큰화 생략 |
| **T-142** | P1 | 대 | **편집 버퍼 교체**([59 §6](59-large-file-handling.md) · ✅ 09-20 84차 — nexa-ctl `TextBuf`(UTF-8 갭 버퍼 + 줄 표 + 변경 기록 · 좌표 = 글자 인덱스 유지) · 그리기 = 보이는 줄만 · 65 MB 상주 348 → 87 MB · 입력 190 → 3 ms). 잔여 = T-145 |
| **T-143** | P2 | 중 | **큰 SQL 스크립트 관리**([59 §5](59-large-file-handling.md) · ✅ 09-20 83차 — 열기 선택 넷 · File ▸ Run SQL File… · 읽기 전용 · 앞부분만 · **탭 격리 비동기 적재 + 진행 막**(`fileload.rs`) · 취소). 잔여: **스트리밍 문장 분할기**(지금은 통째로 읽어 나눔) · N문장 넘으면 페치 끔 · 인코딩 표본 판정 · 읽기 전용 tail · 그 탭 첫 페인트(행 해시·폭 0.15초)도 스레드로 |
| **T-140** | P1 | 중 | **외부 파일 변경 처리**([58 §5](58-external-change-policy.md) · ✅ 09-19 81차 — D-119~124 권장안 · `merge3` · `StatWatch` · `extfile.rs` · 트리거 넷 · 띠 · 저장 2단 확인 · 백업 · 따라가기 · 설정 7키). 남은 것 = 실기(고친 탭 병합 · 겹침 띠 · git checkout 다중 파일 · 네트워크 드라이브) · 좌우 비교 뷰(19) · OS 와처 어댑터(포트만 열림) | — | ✅ |
| **T-139** | P1 | 중 | ✅ 09-20 mac 86차 **macOS 한글 입력**([62 §1](62-macos-input-and-present.md)) — 원인 = winit NSView 경계(첫 키 자모 유출 · 조합 직후 첫 1바이트 키 삼킴) + 팔레트 IME 라우팅 → **앱 조합**(nexa-ctl `TextBox` 조합기 · nexa-sys `input_source` · 설정 `input.hangul_compose` auto/system/app) · 9개 입력란 자동 검증 `가나다1234 가나다!@#$`. 잔여: 앱 조합 중 한자 변환 없음(설정 system으로 회피) · D-134 | 62 | ✅ |
| **T-138** | P1 | 중 | **객체 탐색기 갱신**([57](57-explorer-refresh-after-ddl.md) · ✅ 09-19 80차 — `ddl_target` · 디프 갱신 · T1 실행/커밋 시점 · T3 F5/Shift+F5 · T4 못 찾음 · T2 워터마크 · 설정 `meta.refresh_*` + 마이그레이션 · SQLite 전 경로 테스트). 남은 것 = 실기(Oracle·PG·SQL Server) · "조회 성공인데 트리에 없음" 신호(FROM 분석) · T-57 ② 메타 저장소와 트리거 공유 | — | ✅ |
| **T-137** | P1 | 중 | **수동 커밋 잠금 방지**([56 §9](56-manual-commit-lock-prevention.md) · ✅ 09-19 L1~L4 1차 · 09-19 80차 "차단 중" 띠 ✅ · 39 §3/26 §8 등재 ✅ · ⑤ 접속 유형(개발/시험/운영) ✅ 81차(목록 우클릭 · `?env=` · PROD 칩 · `tx.prod_*` · `run.prod_confirm` — 값·UI는 가정 · 확인 필요) · 남은 것 = 실기): ① L1 읽기 트랜잭션 자동 종료(`tx.read_end`) ② L2 유휴 미커밋 경고·재알림·카운트다운 자동 롤백 ③ L3 막힘 감지(메타 세션 · 4방언) ④ L4 서버 안전망 세션 파라미터 ⑤ (2차) 접속 유형 프리셋 | 56 | 🚧 |
| **T-136** | P1 | 중 | **편집기 입력 지연 2차**([55 §5](55-editor-input-latency.md)): ✅ 깜빡임 위상 입력 기준(09-19 win · §6) · D6 픽셀 버퍼 보존(softbuffer 프레임당 20 MB 할당 제거) → D5 부분 무효화(`Invalidations` 합집합·층 건너뜀) · D12 휠 뒤 60 fps 핀 해제 · D16 자동 반복 합치기 · D7 하이라이터 줄 캐시 · D9/D10 글꼴 경로·아틀라스 · 39 §6 게이트(2 MB 페인트 ≤ 20 ms) | 55 39 | ☐ · **09-19 실측(26 §7-3)**: 3.6 MB·4만 줄에서 `paint_multiline` 66 ms/프레임(캐럿 깜빡임마다 = 유휴 CPU 812 ms/6초) · Private +24 MB — 세대(`edit.rev`)로 묶은 줄 표·표시 문자열 캐시가 1순위 · **✅ 09-19 82차**: 캐시 넷 + 키 경로 복사 제거로 4만 줄 유휴 그리기 78 → 3.3 ms · 입력 209 → 16 ms(59 §1) — 남은 몫은 T-142(버퍼 교체) |
| **T-135** | P3 | 중 | **화면 분할 편집**(사용자 09-19 · 우선순위 낮음): 탭 2개를 세로/가로로 나란히(Sublime `Layout` 2열/2행 · 각 칸 자체 탭 바 · 실행·결과는 활성 칸 기준) → 완성 뒤 **Compare**(두 칸 diff · [19](19-compare-git-and-object-history.md))로 확장 · nexa-ctl `Splitter` 재사용 | 19 | ☐ |
| **T-134** | P2 | 소 | 툴바 도크 후속: 플로팅 창을 도크 위로 끌어 붙이기(지금은 X 닫기 = 되돌리기) · 오른쪽 띠 ↔ 왼쪽 띠 이동 · [54](54-connection-model-and-disconnect.md) D-117 루트 탭 수 표시 | — | ☐ |
| **T-133** | P1 | 중 | ✅ 09-19 로그인 **필수 항목** 표시([22 §10](22-driver-extensions.md)) — `Dialect::login_required()` 원장 · 라벨 `*` · 동작 때 빠진 칸 경고 띠 + 상태줄 + 포커스 · `(optional)` · `missing_fields` MC/DC · nexa-ctl `set_warning` — 잔여: CLI `nsql connect` 인자 검사에 같은 표 · D-115(비밀번호 없는 접속 스위치) | 22 §10 | 🚧 |
| **T-113** | P2 | 중 | **로그 후속**([48 §5](48-logging-architecture.md)): 완전 지연 포맷(`LogRec` 32B · 텍스트는 표시 시점) · ✅ feature `devlog` 컴파일 타임 제거(09-17) · `trace` 수준 생산 지점 · `tx`/`meta` 층 · ✅ 텍스트 보기 변환 시간(09-17) | 48 39 | ☐ |
| **T-114** | P2 | 소 | **Sublime 커서 후속**([29 §7-1](29-editor-syntax-palette-statusbar.md)): ✅ Ctrl+M 괄호 짝 · ✅ Ctrl+Shift+M 괄호 안 확장(09-17) · Ctrl+Shift+Space 스코프 확장 · ✅ Ctrl+↑/↓ 스크롤만(09-17) · mac ⌘↑/↓ · 가운데 버튼 열 선택 | 29 | 🚧 |
| **T-111** | P3 | 소 | 색 설정 알파 통합(09-17): `editor.ruler_alpha`·`editor.whitespace_alpha`를 `#RRGGBBAA`로 흡수(마이그레이션 = 6자리 값 + alpha → 8자리) 뒤 키 제거 · 색 창 키 모드에서 최근 색 공유 확인 | 24 | ☐ |
| **T-109** | P2 | 소 | UI 글꼴 GDI 경로에서 가운뎃점 `·`(U+00B7)이 2×1px·전진 2px로 나옴(설정 창 `·→$`가 `→$`로 보임 · nexa-font 덤프 09-17) — 폴백 face 선택/전진 폭 점검 | nexa-font | ☐ |
| **T-101** | P2 | 소 | ✅ 09-16 48차 확인 — `--overflow` 기본값은 설정에서 읽어 `none`으로 찍힘(이미 수정돼 있었음 · `help.rs` 8행) | — | ✅ |
| **T-171** | P2 | — | ✅ 09-23 최소판(journal §90 ⑥): `VarStore.formulas` + `Action::Replan` · 남음 = 변수 창에 수식 표시 · 재계산 로그 한 줄 | 63 §9 |
| **T-98** | P1 | 중 | ✅ 09-16 48차 — nexa-ui `EditCommand` 14종(줄 복제/삭제/합치기/이동 · 주석 토글 · 들여쓰기 ±·여러 줄 Tab · 줄 선택/나누기 · 캐럿 추가 ↑/↓ · 대소문자) + 키맵 2단 코드(`ctrl+k,ctrl+u`) + macOS `control+…` + `Ctrl+G` · 메뉴/팔레트 등재. 잔여 = `Ctrl+K,Ctrl+D` 건너뛰기 · `Ctrl+U` 소프트 되돌리기 · 캡처 창의 2단 코드 입력 | T-58 [29](29-editor-syntax-palette-statusbar.md) | ✅ |
| **T-97** | P2 | 중 | ✅ 09-16 49차 — nexa-ui `TextBox::set_minimap`(줄당 2px · 토큰 색 · 뷰포트 상자 · 창 한정 캐시 비트맵 · 클릭/드래그 · 테스트 5) + `editor.minimap`(off)/`editor.minimap_width`(80). 한계 = wrap 모드는 소프트 행 기준 | 32차 | ✅ |
| **T-96** | P2 | 소 | ✅ 09-16 48차 — `Ctrl+P`/⌘P Goto Anything(팔레트 재사용 · 열린 탭 ✓/`*`/경로 + 최근 파일 · 퍼지 · `:숫자` 줄 이동 · Tabs ▸ 탭 찾기…). 잔여 = 접속 표시 · (C) 탭바 ▾ 넘침 드롭다운 | 팔레트 | ✅ |
| **T-95** | P2 | 중 | 로그 창 본문 = **읽기 전용 편집기 뷰**(nexa-ctl 편집기 부품 공유) → Sublime **컬럼(블록) 선택**·다중 커서·찾기를 편집기와 같은 부품으로 · 지금의 자체 선택 코드 제거(사용자 09-16 24차) | T-57 [29](29-editor-syntax-palette-statusbar.md) | ☐ |
| **T-94** | P2 | 대 | **데이터 편집기**(DBeaver 결과 하단 행 추가/삭제/복제 · 셀 편집 · Save/Cancel) — 문장 생성 = [41](41-sql-copy-key-rules.md) 키 규칙 · 트랜잭션 [34](34-transaction-ux.md) · 별도 설계 문서 먼저 | T-93 41 34 | ☐ |
| **T-92** | P2 | 중 | DBeaver식 **가상 키**(테이블별 사용자 지정 키 · 프로필 저장 · [41 D-67](41-sql-copy-key-rules.md)) — 앞 3컬럼 경고가 잦으면 | — | ☐ |

