# 2026-09-20 — 84차(win) · ★ 편집 버퍼 교체(T-142) · 되돌리기 후속 넷(D-129~132)

> 83차(되돌리기 재설계 · 큰 파일 모드 · 탭 격리 적재)는 [2026-09-19](2026-09-19.md) 끝에 있다(09-19 밤~09-20).

## 84차 (win) — 사용자 "추천한 대로 진행 · 선행이 필요하다는 의견에도 동의 · T-142 선행" + "끝나면 문서 최신화 · commit · main 병합 · push"

- **T-142 편집 버퍼 교체**([59 §6](../59-large-file-handling.md) · D-126): nexa-ctl `edit/textbuf.rs` `TextBuf` = UTF-8 갭 버퍼 + 줄 시작 표 + 줄 변경 기록 · `EditState`의 `Vec<char>`를 교체(본문을 바꾸는 곳은 83차에 셋으로 모아 두었다 → 그 셋만 고침) · 편집 명령 36곳 · 텍스트 상자 26곳을 새 조회로 이관 · **그리기 = 보이는 줄만**(`Rows`) + 줄별 폭·구문 상태 캐시를 변경 기록으로 갱신(전 행 해시 제거) · 조합 중 글자는 캐럿 줄 하나에만 · `PreparedText` = 스레드에서 만든 `TextBuf` · `TextBox::chars()` 폐지 → `buf()` · 호스트 5곳(캐럿 줄·열 · 선택 요약 · 저장본 비교 · 단계 판정 · 외부 변경 비교) + 찾기 = 줄 단위.
  - 순서: ① `TextBuf` + 난수 모델 대조 테스트(30 시드 × 150 편집 · 한글·이모지·개행 · 한 곳/여러 곳 · 변경 기록으로 줄 수 추적) → ② `EditState` 교체(임시 호환 경로로 기존 290개 테스트 통과 확인 — 되돌리기 속성 테스트 포함) → ③ 자주 도는 경로부터 새 조회로 → ④ 그리기 → ⑤ 호스트.
  - 실측(Release): 70만 줄 입력 157/184 → **3.0/4.1 ms** · 그리기 12.8 → 2.1 ms · 첫 페인트 195 → 55 ms · 65 MB 앱 상주 348 → **87 MB** · 피크 443 → 106 MB · 20 MB 112 → 35 MB · 6 MB 44 → 18 MB · 2 MB(보통) 39 → 28 MB · 유휴 CPU 초당 60 → 0~30 ms · 단어 타이핑 1.66 → 0.07 ms.
  - 실기(기동 명령 · 입력 주입 없음): 65 MB에서 줄 복제 → 주석 → 줄 이동 → 줄 선택 → 대문자 → 되돌리기 4번 = 원문 + "저장됨".
- **되돌리기 후속 넷**([60 §7](../60-undo-redo-redesign.md)): D-131 쉬었다 치면 새 묶음(`editor.undo_group_ms` 1500) · D-132 재로드 = 최소 줄 편집(`merge3::line_edits` — 위아래 한 줄씩 + 가운데 1,000줄 = 기록 100 B 미만) · D-130 거대 편집 확인(`editor.undo_giant_mb` 32 — 지우는 동작을 되풀이해야 진행 · 타이핑은 확인이 아님 · 진행하면 히스토리 비움) · D-129 되돌리기 기록 파일(`undofile.rs` — 저장할 때 쓰고 열 때 본문 해시가 같으면 복원 · `editor.undo_persist` on · 4 MB · 30일).
- 덤: 저장 직후에도 큰 파일은 저장본 사본을 만들지 않는다(83차는 열 때만) · 괄호 짝 표가 상한을 넘는 본문이면 문자열을 만들기 전에 그만둔다 · 벤치 `bench_editor`/`bench_undo` 그대로 동작.
- 검증: 두 저장소 fmt · clippy 0 · 테스트 green(nexa-ui 349 · nexa-sql 322) · nexa-ui 3-OS 검사 통과 · Debug + Release 빌드.
- 🔧 시험 중 실수 하나: 캡처 스크립트를 셸 함수로 감싸면서 `"$SW\\$1"`이 글자 그대로 `$1`로 넘어가 격리 폴더가 `scratchpad$1`이라는 **새 빈 폴더**로 잡혔다("Not connected"로 보인 원인). 사용자의 실제 설정 폴더는 쓰이지 않았다(설정 파일 수정 시각으로 확인) · 떠돌이 폴더 삭제. 같이 드러난 것 = `undofile` 단위 테스트가 실제 설정 폴더에 빈 `undo/`를 만들었다 → 폴더를 인자로 받는 구조(`*_in`)로 고치고 빈 폴더 제거.
- **충돌·조정**: ① 59 §4 "좌표를 바이트로 · 포트 도입 · 3보 이관" ↔ 구현 = **글자 인덱스 유지 · 포트 없음**(호출부·기록·파일 형식의 한글 경계 위험을 지지 않고도 목표 수치를 넘겼다 — 59 §6-3). ② 60 §5 D-130 권장 "L2에서만 확인 창" ↔ 구현 = **바이트 기준으로 어느 탭에나 · 확인 창 대신 앱의 2단 관례(되풀이)** — 그리고 "되풀이"를 **지우는 동작으로 한정**(타이핑 두 번이 확인이 되는 사고를 막으려고 · 설계에 없던 규칙). ③ 60 §5 D-129 "hot exit 때 탭별 파일" ↔ 이 앱엔 hot exit이 없다 → **저장 시점 기록(Vim 방식)**. 기본값 켬은 주류 편집기 관례를 따랐고, 평문으로 남는다는 점을 설정 설명·문서에 적었다. ④ 60 조사안 "닫힌 묶음 varint 직렬화 · 디스크 흘리기" = 하지 않음(기록 파일은 고정 폭 · 거대 연산은 흘리는 대신 막는다). ⑤ 83차 §5-3 "남은 비용(T-142로)" 두 줄(입력 190 ms · 유휴 CPU 60 ms)은 이번에 해소 — 문서는 §6으로 이어 적고 §5는 당시 기록으로 둔다.
- push: nexa-ui `d2aea86`(CI ✅) · nexa-sql `df3e24b`(`ci` ✅ · `integration` ❌ — **이번 변경과 무관한 기존 실패**: `pg_read_only_transaction_ends_in_manual_mode`가 78~79차 push부터 3회 연속 실패 → TODO T-146). Debug GUI 재기동.
