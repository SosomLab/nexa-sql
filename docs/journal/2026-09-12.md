# 2026-09-12 — 착수

## 1차 (mac) — 조사 · 라이브러리 추출 · 엔진 · 보고서

**순서**: 기존 계열 저장소 조사(clip·beep·dir2·memkeeper·tauri-test1 — 규약·라이선스·크레이트 결합도) → 조사 에이전트 5건 병렬 → `nexa-ui` 추출 → `nexa-sql` 골격 → `nsql-script` 구현 → 문서.

**계열 조사에서 확인한 것**: 4개 Rust 앱 모두 프레임워크 0(자체 CPU 래스터 · winit+softbuffer 또는 Win32) · PolyForm NC 동일 · 문서/커밋 규약 `docs/16` 공유 · `nclip-ctl` 의존은 `nclip-gfx` 하나 · DB 크레이트 0 · dir2에 `wasmi` 플러그인 선례 · nexa-dir에 라이선스 정책(13)·활성화 설계(17) 원본. ⚠️ clip/beep/dir2/memkeeper의 LICENSE 헤더가 가리키는 `docs/13-licensing.md`는 nexa-dir에만 있음(각 저장소는 dangling 링크).

**nexa-ui 추출**: 계보 최신본 = clip. `cp` + 식별자 치환 + 주석 앱 이름 일반화 → fmt ✓ · clippy ✓ · 189 green(gfx 14 · ctl 165 · conf 10). dir2 `widgets`(dock·tabbar·menubar·columns·rows 7,648 LOC)는 `DrawCtx` 세대 차로 미이관(nexa-ui U-2).

**nsql-script 설계 판단**:
- 엔진은 **행동(Action)만 낸다** — DB·화면 무의존이라 `cargo test`와 `nsql plan`으로 검증. 호스트가 실행·흡수.
- 사용자 예시(Golden)에서 `EXEC` 홀로 + 다음 줄 SELECT…INTO 블록 관용 발견 → 블록 EXEC 규칙 추가. 선언 없는 `:V` 대입 → 암묵 생성(`Auto`) + 진단.
- MSSQL: `sp_executesql` OUTPUT 1차 · `DECLARE` 프리펜드 2차(배치 첫 문장 DDL·USE·SET) · `SELECT … INTO :X` → `SELECT :X = …`(바인드 표기 유지 후 `@` 치환 — 순서 결함 1건 수정: 먼저 `@`로 바꾸면 파라미터가 비었다).
- 실측: `nsql plan --dialect mssql examples/golden-session-vars.sql` 항목 6 · 변수 9 · 오류 0.

**결함 수정 이력**: 임시값 수명(테스트) · `Option<&str>.copied()` · manual_strip · 꼬리 주석 뒤 `;` 절단(`terminated_by_semicolon`이 위치 반환) · 줄 연속 이중 공백 · 줄번호 기대값 오기(9·21).

**남긴 것**: DP 10건 확인 · M1 드라이버. Artifact(웹 보고서)는 만들지 않음 — 저장소 문서가 SSOT.

## 2차 (mac) — 병행 개발: 드라이버 · CLI · 최소 GUI

**결정 흡수**: 사용자 "서명 별도 · 나머지 한꺼번에 · GUI 최소 병행" → DP-1~9를 DR-10~18로. 추가 요구 7건은 문서 12·14·15·17·18·19에 흡수(구현 순서는 17).

**드라이버 API 확인**: `~/.cargo/registry`의 `oracle-0.6.3`·`tiberius-0.12.3` 소스를 직접 읽어 시그니처 확정(`Statement::bind/bind_value/is_query/implicit_result` · `RefCursor::query` · tiberius `Client::query/execute` · `ColumnData` 변형). Oracle은 InOut을 VARCHAR2(4000)로 바인드해 문자열로 회수 후 `VarType`으로 강제 — 실서버 검증 전이라 M1 T-4에서 실측 필요. MSSQL은 tiberius에 OUTPUT 회수 API가 없어 `DECLARE @v = @Pn` + 트레일러 `SELECT @v AS [v]` 설계(파라미터로 값 전달 → 리터럴 직렬화·플랜 캐시 문제 회피).

**실기(SQLite)**: `nsql run -c sqlite::memory: examples/sqlite-session-vars.sql` → `EXEC :V_MAX := (SELECT MAX(sal) …)`가 `SELECT … AS "V_MAX"`로 실행돼 520이 흡수되고 `PRINT`·후속 조회 바인드까지 왕복. export json/insert/csv ✓.

**결함 4건**: ① 줄 기반 분리기가 한 줄 다중 문장을 잃음 → 오프셋 기반으로 재작성 ② 블록 EXEC 시작을 개행 위치로 넘겨 본문이 빔 ③ `?` 방언 `:V := expr`이 `CALL`로 래핑 ④ OUT 흡수가 이름 목록에 갇혀 별칭 컬럼을 놓침 → 1행 트레일러의 모든 컬럼 흡수.

**GUI**: clip `demo.rs`·`main_win.rs` 이벤트 번역을 참고해 창·워커·그리드 구성. 실행은 워커 스레드(세션은 그 스레드에서 생성 — `Send` 불요), 결과는 채널 + `EventLoopProxy`. 두 `RasterCtx`(UI 본 / 고정폭 본)로 영역별 폰트. 창 실기는 하지 않음(자동 환경) — `--smoke`만.

## 3차 (mac) — 정리 · push

사용자에게 확인 질문 4건(실서버 검증 방식 · 다음 우선순위 · 실기 OS · 캐시 위치)을 대화상자로 물었으나 취소됨 → D-14~D-17로 문서에 남기고 기본안(권장)을 적어 둠. 사용자 지시로 commit → main push. `nexa-sql` 원격은 이미 `SosomLab/nexa-sql`(공개 · 빈 저장소)였고, `nexa-ui`는 원격이 없어 `gh repo create SosomLab/nexa-ui --public`으로 만들고 같은 SSH 별칭(`git@kiros33.github.com`)으로 push.
