# 2026-09-18 — 56차(mac) · ★ 세션 컨텍스트(DR-34): 공유 N · 전용(CONNECT) · 개별 모드 · 실행 통제 단일화 · 로그인 창/텍스트박스 맥 결함

## 56차 (mac) — 사용자 요청 묶음

**요구(요약)**: ① 공유 세션과 탭별 세션을 **병행** 개발 ② 공유면 실행은 세션당 1개 → **모든 실행 진입점을 통합 block**(Disable → 끝나면 Enable) ③ Golden의 *Private Session*처럼 `connect "oracle:…"` 명령으로 붙은 세션은 **그 탭 안에서만** 유일 실행 ④ 프로필 이름·접속 문자열 둘 다 ⑤ 탭 이름 앞 **이미지 버튼 표식** · 우클릭 = 해제 + 공유 복귀 · `disconnect` 타이핑도 ⑥ 개별 모드는 모든 탭에 표식 · 해제하면 그 탭은 실행 불가 ⑦ 접두 문자가 필요한지 조사 ⑧ 유휴 세션 처리의 서버/클라이언트 부하 검토 ⑨ 기본 사상 = **1 인스턴스 · 1 서버 · 1 계정** ⑩ 공유 연결은 **여러 개**(기존 유지 + 추가 · 같은 서버 중복 제외 · Disconnect 드롭다운 · 탭이 선택) ⑪ 탐색기(인텔리센스) 연동 설계 ⑫ 통제 로직 종합 검토 · **MC/DC** ⑬ 맥 로그인 창 상세 확장이 Windows와 다름 · 텍스트박스 가로 스크롤 표시 · 긴 글 앞부분 표시.

### 설계 · 구현 = [52 세션 모드](../52-session-modes.md) · **DR-34**

- **`sessions.rs`(신규)**: `Sess` = 워커(스레드·Runner·DB 세션) + 그 세션의 실행 상태 **전부**(busy · aux · tx_pending/txlog · run_editor/run_tab/… · run_toast · key_cache · status · idle). `App.sess` = 활성 탭의 세션 · `parked` = 나머지 · 활성 탭이 바뀌면 `mem::swap` 1회(`sync_sess`) · 잠든 세션의 이벤트는 `with_sess`로 잠시 앞에 꺼내 **같은 `drain_events`** 로 처리 → main.rs의 세션 상태 참조 328곳을 `self.sess.*`로 기계 변환만 하고 다중 세션이 됐다.
- **통제 단일화**: `Sess::blocked() = busy ∨ aux>0` · 문지기 `gate_open()` · 화면 `sync_gate()`/`gate_view`. 전수 점검(`worker.send` 26곳 · 52 §3-1)에서 **무통제 5건 발견·수정** — 실행 중 추가 페치/전체 조회/건수/키 조회가 큐에 쌓임 · Commit/Rollback이 실행 뒤에 줄 서서 조용히 실행 · Explain이 직전 실행의 결과 탭으로 · 접속 창이 활성 탭 세션의 busy를 봄 · 실행 앞에 끼운 재접속의 `done`이 busy를 일찍 풂(`skip_done`). 그리드는 `session_blocked`면 자동 페치 요청 자체를 만들지 않고 도구줄(⇊·Σ·새로고침)을 흐리게 · ■만 활성.
- **전용 세션**: 실행 직전 `connect_intent`(파싱만) → `placement` → `NewPrivate`(새 워커 · 탭 표식 · 옛 결과 `more=false`) / `Retarget` / `ClosePrivate`(미커밋 확인 → 세션 거둠 → 공유 복귀) / `Run`(공유 탭은 처음 실행한 연결에 묶임). `CONNECT 프로필` · `CONNECT "oracle://u/p@h:1521/svc"` · 짧은 스킴 `oracle:h:1521/svc`(자격 없음 → 같은 대상 프로필이 **하나뿐이면** 자격을 빌림 `worker::fill_credentials`) · `sqlite:경로` · 따옴표. 설정 `session.private_connect`(끄면 종전 = 공유 세션 교체).
- **접두 조사(52 §5)**: 문장 첫머리가 `CONNECT`/`DISCONNECT`인 서버 SQL은 Oracle·MSSQL·PG·MySQL·SQLite에 없고, DB2/Informix/ISO는 뜻이 같다 → **접두 불필요**. 유일한 구멍 = 선택 실행으로 잘린 `CONNECT BY …` 조각 → 가드(`is_connect_by`) · 짝 별칭 `:disconnect` 추가.
- **공유 연결 N개**: `login_plan`(Use 중복 금지 · Busy · Recycle · New · Limit `session.max_shared` 4) · 활성 공유 연결 `default_shared` · `tab_bind` · 공유 연결이 둘 이상이면 공유 탭에도 표식(중립색) · **툴바 Disconnect ▾**(활성화/해제/모두 해제 · 대상 하나면 바로) · 해제 시 탐색기 동반 종료 · 묶인 탭은 끊김 표식 · `reap_shared`.
- **개별 모드** `session.mode=per-editor`: 탭이 **처음 활성화될 때** `default_spec`으로 접속(복원 탭 30개 = 접속 1) · 해제 = 끊김 표식 + 실행 불가 · 표식 메뉴 "다시 접속" · 상한 `session.max_private` 8(넘으면 공유 세션).
- **유휴 닫기**(52 §6): 방식 4종 비교 → ⓒ 닫고 다음 실행 때 재접속 채택(주기 핑은 DBA의 유휴 정책을 무력화 · 26 §8 위반 소지라 배제). DBMS별: Oracle(프로세스+PGA)·PG(백엔드 프로세스)는 이득 큼 · MSSQL 작음 · **SQLite는 닫지 않음**(`:memory:` 소실). `idle_action` 순수 판정 · `session.idle_secs` 1800 · 전용만(`session.idle_shared` off) · 열린 트랜잭션(수동 커밋의 조회 포함) 제외.
- **잃는 순간**: 전용 탭 닫기 = 그 세션 대기 문장 전부 · 다른 탭이면 앞으로 꺼내 묻기 · 종료 = 세션마다 한 번씩 · 닫는 세션이 실행 중이면 취소 먼저.
- **탐색기 연동 설계(52 §2-2 · 구현 T-121)**: 노드 = 서버 키 · 자동 추가 + **노드·캐시는 수동 해제 / 메타 세션은 유휴 자동 회수** + `MetaStore` 캐시를 세션 수명에서 분리(LRU) — 사용자 안 ①②의 절충(D-106).
- **MC/DC(52 §12)**: 분기 판단 10개를 순수 함수로(`route_tab`·`login_plan`·`placement`·`reap_shared`·`badge_kind`·`gate_view`·`idle_action`·`connect_intent`/`is_connect_by`·`ConnectSpec::parse`·`same_target`) · 조건별 독립 영향 쌍 테스트. 호스트에 남은 분기 5개는 명시(T-122).

### 맥 결함 3건

- **로그인 창 상세 확장**: `advance()`가 **기본 크기**(`tuning.window_w/h`)로 `request_inner_size` → 09-17 창 크기 기억 뒤로 사용자가 키운 창이 Details에서 기본 폭+폼으로 줄어 목록이 좁아짐. → `base`(목록 창 논리 크기 · 사용자 Resized를 따라감 · 슬라이드 중 Resized는 무시) 기준으로 **오른쪽으로만** 커짐 · 닫을 때 기억하는 크기도 `base`(폼이 열린 채 닫아도 다음에 넓게 열리지 않음). 플랫폼 중립 수정(Windows도 같은 경로).
- **텍스트박스**(nexa-ui 32차): 포커스 없는 상자는 앞부분부터 · 가로 스크롤 표시는 스크롤하는 동안만(자동 숨김을 안 붙였던 것이 원인).

### 검증

`cargo test --workspace` green(nexa-sql 82 · nsql-script 47 · nsql-run 18 …) · nexa-ctl 248 · clippy 0(nexa-sql·nsql-script·settings·i18n · nexa-ctl) · Debug/Release 빌드 · 앱 기동 스모크(6초 생존 · 패닉 0). **GUI 상호작용은 미실기** → ⏳ **T-125 = 52 §13 ①~⑮** + 로그인 창 확장 + 텍스트박스 2건.

**결정 대기**: D-96~100 · D-102~108(52 §10). D-101 = ✅ 추가(사용자 09-18).
**미푸시**: nexa-sql · nexa-ui 둘 다(사용자 요청 시).

## 56차 후반 (mac) — 결정 D-96~108 개발 · 서버별 탐색기 · 세션 상태 재검토 · 공유 상한 8

**요구**: ① 공유/개별 모드에서 서버 메타를 어떻게 보관·제공할지 — 탐색기가 인텔리센스·툴팁의 **단일 원천**이므로 그 서버에 연결이 **하나라도** 있으면 탐색기가 유지돼야 한다 ② 공유 서버 상한 4 → **8** ③ 세션이 끊기면 세션 설정·Temp 데이터가 무효화되는데 유휴 닫기가 맞는가 · 세션에 묶인 변수 개념이 있는가(Oracle `DECLARE`는?) ④ "결정에 따른 개발도 모두 진행" ⑤ 빌드·재실행.

- **서버별 탐색기**(`explorers.rs` `ExplorerSet` · 52 §2-2): 서버 키(방언·호스트·포트·DB·계정)당 `Explorer` 하나 · **어떤 세션이든** 접속되면 확보(`explorer_attach` — 프로필 이름뿐/자격 없는 스펙은 저장소에서 완성) · `sync_refs`로 참조 수 ≥1이면 유지 · 0이면 **메타 접속만 닫고 트리 유지**(오프라인 · 수동 제거) · 메타 세션 유휴 회수(`Req::Suspend` → 다음 요청 때 메타 스레드가 재개) · 활성 탭의 서버 트리를 앞으로(전환 비용 0) · 서버 ≥2면 머리줄 `이름 i/n ▾` → 목록 팝업 · 라이브 로그는 실행 세션의 서버 탐색기로. 종전 "활성 공유 연결만 따라가는 탐색기 하나" 제거.
- **세션 상태 재검토(52 §6-4)**: Oracle `DECLARE` = **블록 범위**(세션 종속 아님) · 세션에 남는 것 = 패키지 전역 변수·애플리케이션 컨텍스트·`ALTER SESSION`·GTT 데이터 / MSSQL `#temp`·`SET`·`USE`·`SESSION_CONTEXT`(`DECLARE @v`는 배치 범위) / PG `SET`·사용자 GUC·임시 테이블·`PREPARE`·`LISTEN`·권고 잠금 / MySQL **`@v` 사용자 변수**·임시 테이블. → 아무 세션이나 닫는 것은 틀림 · **`alters_session_state`로 상태를 만든 세션은 유휴 닫기 제외**(PL/SQL·프로시저 호출은 보수적으로 전부) · 세션이 바뀌면 1회 안내(`StSessStateLost`). 한계 = 자동 커밋의 GTT INSERT.
- **D-99** `CONNECT` 앞 문장 → 공유 탭은 실행 거부 + 토스트(`Placement::Refuse`) · **D-104** 뒤에서 끝난 실행 = 탭 제목 ✓/✗ · **D-105** `TxLog` 전 세션 합본(`select_session` · 세션별 `open_tx`/`current` · `TxFilter.session` · 창 "세션" 열은 세션 ≥2일 때만) · `session.max_shared` 기본 **8**(최대 32).
- MC/DC 표에 D11 `pane_move` · D12 분류기 · D13 · D14 `TxLog` 격리 추가 · D3에 `preceded` · D7에 `stateful`.
- 검증: `cargo test --workspace` green(nexa-sql 85) · clippy 0 · Debug/Release 빌드 · 앱 재실행. ⏳ 실기 = 52 §13 ①~⑳.
- **Disconnect ▾ 보완(사용자)**: 누르면 늘 연결 목록(공유 + 전용/개별 탭 세션 · 탭 제목 병기) → 줄 클릭 = 그 연결만 해제 · **모두 해제** · 공유 ≥2면 `활성 연결로 ▸`. 종전 "대상이 하나면 바로 해제"·서브메뉴 구조 제거(`disconnect_session`).
- **정리·병합**: README 색인에 44~52 추가 · MILESTONES M2에 DR-34 행 · `cargo fmt` · `check-3os`: nexa-ui 3타깃 ✓ / nexa-sql 호스트 fmt·clippy ✓, 교차 2타깃은 C 의존(ring·odpic·sqlite)의 교차 컴파일러가 이 맥에 없어 실패(T-120과 같은 환경 제약 · CI가 판정). 브랜치 `feat/session-modes`(nexa-sql) · `feat/tab-badge-textbox`(nexa-ui) → main 병합 · push.
- **탭 툴팁이 탐색기 밑에 깔림(사용자 캡처)**: 첫 탭의 카드가 탭 가운데 정렬로 왼쪽으로 나가고, 탐색기가 나중에 그려져 덮었다 → `draw_tooltip_in`으로 가로 클램프를 **편집기 영역 x부터**(결과 도구줄 툴팁 09-16과 같은 처방).

## 57차 (mac) — 로그인 목록 열 조절을 결과 그리드와 같게 · 기본 크기 · 미니맵 기본 켬

- **마지막 열 폭 조절**(사용자 캡처 2장): 목록은 경계를 끌어도 가로 스크롤이 따라오지 않아 마지막 열(Target)을 창 밖으로 키우면 경계가 보이지 않았다 → 결과 그리드와 같은 식(`edge − scroll_x > 뷰포트 폭`이면 그만큼 스크롤)으로 **경계가 커서를 따라 보인다**.
- **자동 맞춤**: 헤더 경계를 400ms 안에 다시 누르면(더블클릭 · 결과 그리드와 같은 규칙) 헤더 이름(+정렬 배지 자리)·목록 전 행의 값 중 가장 넓은 폭 + 여백으로. 폭 측정은 그리기 문맥이 필요해 `autofit` 예약 → 다음 페인트에서 반영. 비밀번호 열 = 체크 상자 폭.
- **기본 크기 = 캡처 기준**: 창 `conn.window_w/h` 640×520 → **748×526** · 열 = 이름 124 · 종류 88 · 사용자 112 · 비밀번호 62(고정) + **대상 = 남는 폭 전부 · 최소 408**(긴 경로가 잘리지 않게 · 창이 좁으면 가로 스크롤). 종전 비율(`COL_FRACS`) 제거. 기억된 `window.login_size`가 있으면 그쪽이 우선.
- **미니맵 기본 켬**: `editor.minimap` off → **on**(향상 모드 `perf.boost`는 종전대로 끔).
- 검증: workspace 테스트 green · clippy 0 · Debug/Release 빌드 · 재실행. ⏳ 실기: 경계 끌기·더블클릭 · 새 기본 폭.
- **탐색기 = 서버 루트를 접속 순으로 쌓기**(사용자 캡처 "SNOP-DB 밑에 Demo가 보이도록 · 선택해서 1개만 보이는 게 아니라"): 56차의 머리줄 선택기(`이름 i/n ▾`) 폐기 → `ExplorerSet`이 서버 칸을 세로로 놓는다. 칸 높이 = 내용 높이 · 넘치면 큰 칸부터 깎는 순수 함수 `stack_heights`(테스트 6) · 누름은 커서 아래 칸 · 이동은 전 칸(hover 해제) · 휠은 마지막 마우스 위치의 칸 · 키는 마지막으로 누른 칸 · 선택은 전체에 하나(`clear_selection`) · 우클릭 메뉴는 전 칸을 그린 뒤 `paint_menu` + 메뉴 영역 = 탐색기 전체(`set_menu_host` — 한 줄짜리 칸에 갇히지 않게) · 오프라인 루트 우클릭 "탐색기에서 제거"(`ExplorerAction::RemoveServer`).
- **SQLite 루트 = 파일 이름만**(전체 경로는 접속 창·탭 툴팁에) · **메인 창 기본 크기** 1100×720 → **1375×945**(캡처 기준 · 기억된 `window.main_size`가 우선).
- `examples/oracle-refcursor-pkg.sql`: REF CURSOR 패키지 `NSQL_DEMO_PKG`(`FIND_OBJECTS(P_NAME, P_RC OUT, P_MAX)` = 이름에 글자가 든 객체 목록 · LIKE 특수문자 이스케이프 · 함수판 `OBJECTS_LIKE`) + `VARIABLE rc REFCURSOR` / `EXEC 패키지.프로시저(:V_NAME, :rc)` / `PRINT rc` 사용 예 — `nsql plan` 11항목 오류 0(서버 실행은 사용자 실기).
- **자동 맞춤이 값을 가림(사용자 캡처 · Name 열 "SNOP-DB-Tes")**: 자동 맞춤은 여백 8px로 쟀는데 행은 `pad` = 10px로 그린다(글자 `cx+pad` 시작 · 클립 `cx+cw−pad` 끝) → 4px 부족. 같은 `pad`를 쓰고 소수 전진 폭(CoreText) 올림 여유 2px를 더했다.
- **탐색기 = 한 트리처럼 이어 붙이기(사용자 캡처 "수평 분할된 패널처럼 보인다 → 1번째 서버 정보 끝에 연결")**: 칸 높이를 깎아 각자 스크롤하던 `stack_heights` 폐기 → 각 서버 트리를 **내용 전체 높이**로 연달아 놓고 `ExplorerSet`이 **공용 스크롤 하나**(`scroll` + 오버레이 `ScrollBars`)를 갖는다. `Explorer::set_clip`(공용 뷰포트)로 그리기·히트 테스트를 보이는 부분만 · 키보드 이동은 `selected_span`으로 공용 스크롤이 따라감 · 휠/스크롤바 끌기는 묶음이 먼저 처리.
- **자동 맞춤을 더 딱 맞게(사용자 "헤더와 값을 포함해서 더 Tight하게 · Pass는 헤더만 보이면 된다")**: 필요한 폭 = `pad` + max(헤더 이름[+ 지금 달린 정렬 배지], 값) + `pad/2` + 1px. 종전엔 정렬 배지 자리(`" ▲9"`)를 늘 더했고 오른쪽 여백도 `pad`였다 → 헤더·행의 오른쪽 클립을 `pad/2`로 같이 줄여 식을 하나로. 비밀번호 열 = 체크 상자 < "Pass"라 헤더 폭이 결정.

## 58차 (mac) — 접속 생존 관리 조사([53](../53-connection-liveness.md) · 사용자 "VPN이 끊어진 상태 … 다른 프로그램은 어떻게")

- **실측(앱 상태)**: 앱 소켓 `192.168.211.62 → 61.81.244.214:1521 ESTABLISHED` 1개(VPN 주소로 맺음) · 기본 경로는 en0 · `nc` 실패(75초) · macOS `always_keepalive=0`·`keepidle` 2시간 → 드라이버가 keepalive를 안 켜 **영원히 ESTABLISHED** · 앱은 `connected=true`·신호등 초록(창 닫힘 = 주기 확인 없음) → F5 = 빠른 판정을 건너뛰고 죽은 소켓에 보내 **수 분 막힘** · ■(OCIBreak)도 같은 소켓 · 탈출 = 접속 해제.
- **타 도구**: DBeaver `InvalidateJob`(네트워크 핸들러 → 컨텍스트 BEFORE/INVALIDATE/AFTER · 전부 실패면 강제 해제 대화상자 · 열린 트랜잭션 롤백 시도) · keep-alive 간격 + idle close 설정 / DataGrip keep-alive 질의 + auto-disconnect + Reconnect 링크 / SSMS·SqlClient 다음 F5 자동 재접속(세션 상태 소실) / SQL Developer 프롬프트 / psql `PQreset` 자동 · 다음 명령부터 / SQL*Plus 없음. 공통: **감지는 다음 동작 때 · 재접속은 사용자 동작에 묶임 · 상태 소실은 알림** · 차이 = 막히는 시간(드라이버 타임아웃/keepalive 유무).
- **드라이버 API**: Oracle `set_call_timeout`·`status()`(0 왕복)·`ping()` / PG `keepalives*`·`tcp_user_timeout`·`is_closed()`·`is_valid()` / MSSQL 자체 소켓(SO_KEEPALIVE·read timeout).
- **제안**: Broken 상태 신설 · 동작 직전 빠른 판정을 "오래된 성공(60초)"에도 · TCP keepalive(빈 패킷 · 서버 유휴 정책 무관 · 질의 keepalive는 채택 안 함) · Oracle 호출 타임아웃은 설정만 · Broken 취소 = 워커 교체 · OS 네트워크 변경 신호(nexa-sys). 우리 소스 고칠 곳 10개 표 · D-109~114 · T-127~130.

## 59차 (mac) — 끊긴 세션 관리 구현([53 §8](../53-connection-liveness.md) · 사용자 "제안 방식으로 구현")

- **워커 `ensure_alive`**: 순수 판정 `sessions::live_plan`(preflight · suspect · dead_hint · stale · allow · auto → probe/reconnect · MC/DC D15) → SYN 1 판정 → 죽음 = `ConnOutcome::Broken` + 오류(막힘 0) · 살아 있고 의심이면 같은 스펙 재접속 · **Run·FetchPage·Count·Keys·Commit/Rollback** 전부 지난다(커밋/롤백은 재접속 없음). `last_ok`는 성공마다. 접속 자체(접속 창·실행 인자·유휴 뒤·메타 Open/재개)도 판정 먼저 — 테스트 `unreachable_server_fails_fast_and_reports_broken`(TEST-NET-1 · 4초).
- **드라이버**: `Session::is_alive()` 포트 · Oracle `status()`·`set_call_timeout`(`session.call_timeout_secs` 기본 0) · PG `keepalives*`·`is_closed()` · MSSQL `socket2` keepalive(어댑터 안) · `nsql_drivers::set_net_options` · `net.keepalive_secs` 60 · `probe.stale_secs` 60.
- **UI**: `Sess.broken` · 탭 표식 끊김 · 플러그 빨강 · Disconnect ▾ "· 끊김" · 상태줄 `[끊김]` · 로그 "접속 끊김/회복" · 접속성 오류도 Broken · **■ on Broken = `abandon_worker`**(워커 교체 · 세션은 Broken+idle_closed → 다음 실행 때 재접속) · 유휴 닫기도 워커 교체(죽은 소켓의 logoff에 갇히지 않게) · `spawn`의 `auto_reconnect` 인자 제거(워커가 설정을 명령마다 읽음).
- 남은 것: T-130(OS 네트워크 변경 신호 · 지금은 60초 stale이 대신) · MSSQL read timeout · 잠든 메타 스레드의 갇힌 logoff.
- 검증: workspace 테스트 green(nexa-sql 87) · clippy 0 · Debug/Release 빌드 · 재실행. ⏳ 실기 = VPN 끊긴 채 접속/F5/■ · 복구 뒤 F5.

## 60차 (mac) — 접속 UX 전체 설계·구현([52 §7-2](../52-session-modes.md) · 사용자 요청 5건)

- **표식 메뉴 개편**: ① 공유 연결 목록(● = 이 탭 · 끊김 표시 · 누르면 그 연결로 — 전용이었으면 **바로 폐기**) ② **미연결** ③ (개별 모드) **이 탭 접속…** ④ 전용이면 구분자 → `🔌 전용 — 설명 · 상태` · 해제 · 다시 접속.
- **탭의 `CONNECT`**: 공유 탭 = 새 전용 연결 · 전용 탭 = 끊고 새로 — **같은 서버·계정**(`same_server`)이면 `connect.reconnect_same`: 끔 = 기존 유지(`strip_first_connect`로 명령만 지우고 나머지 실행 · 로그) · 켬 = 명시적 재접속. 표식/세션 관리자의 "다시 접속"도 같은 설정(`connect_quietly(spec, again)`).
- **세션 관리자**(`open_session_manager`): 서버별로 모은 전체 세션(`● 공유 · 🔌 [탭]` · 끊김/오프라인 표시) · 줄 ▸ 활성 연결로/다시 접속/접속 해제 · 모두 해제 — 툴바 Disconnect **▾**(nexa-ctl `Toolbar`가 ▾ 영역 클릭을 `id#drop`으로 구별 · 33차) · View ▸ Session Manager · 팔레트.
- **툴바 Disconnect 본체 = 모든 연결 해제**(지금 세션 미커밋은 묻고 · 다른 세션 미커밋은 남기고 로그) → 전 탭 미연결. **플러그 색 = 지금 탭의 연결**(초록/빨강/기본).
- **미연결 상태**(`make_unconnected`): 접속 없는 전용 자리(세션 객체만) — 표식 사선 · 실행 = not connected · 메뉴에서 다시 연결 선택. **새 탭**: 공유 모드 = 그때의 활성 공유 연결에 즉시 묶임(`sync_sess`) · 개별 모드 = 미연결 자리 + 접속 창 자동(메뉴 New · 탭 바 [+] 둘 다 `on_new_tab`) · 닫으면 미연결.
- **표식은 항상 보인다**(사용자 "항상 보여지도록" · 재시작 뒤 표식이 없던 것 = 시작 때 `sync_sess_ui`를 안 부른 것 + 공유 하나면 숨기던 규칙) → `badge_kind`에서 `None` 제거 · 시작 직후 동기화 1회.
- 케이스 표 = 52 §7-2(탭 상태 3 × 조작 12 · 남은 틈 2건 명시).
- 검증: workspace green(nexa-sql 88) · clippy 0 · Debug 빌드·재실행. ⏳ 실기.
- **새 탭의 표식 자리(사용자 캡처 Script_5/6)**: 만든 직후 표식이 없다가 다음 이벤트에 생겨 제목이 밀렸다 → `Editors::sync_badges`가 호스트가 아직 알려 주지 않은 탭도 **미연결 사선으로 자리를 잡고** · 호스트는 탭 목록이 바뀐 것을 보면(`badge_tabs`) 즉시 `sync_sess_ui`(페인트마다가 아니라 바뀔 때만 — `sync_tx_ui`의 redraw와 맞물린 재그리기 루프 방지).

## 61차 (mac) — 세션 창(별도 창 · 사용자 "트랜잭션 로그 창처럼")

- `sessions_win.rs` `SessionsWin` — 트랜잭션 로그 창과 같은 골격(모덜리스 · 메인 소유 · `window.sessions_pos/_size` 기억 · Esc 닫기). 표 = **서버 머리줄 + 세션 줄**: 서버 · 세션(● 활성 공유 / 공유 / 🔌 [탭]) · 상태(연결됨 · 작업 중 · 끊김(빨강 띠) · 오프라인) · 유휴 · 대기 · 탭. 행 우클릭 = 활성 연결로 / 탭으로 / 다시 접속 / 접속 해제 · 빈 곳 = 모두 해제 · 더블클릭 = 탭으로(전용) / 활성 연결로(공유). 푸터 = 세션 수 · 서버 수 · 조작 안내.
- 데이터 = 호스트가 그릴 때 `session_rows()`로 `Sess`에서 만든다(창은 보기만 · 상태가 바뀌면 `sync_sess_ui`가 redraw). 열기 = 툴바 Disconnect **▾** · View ▸ Session Manager · 팔레트. 60차의 팝업식 세션 관리자는 제거.
- **표식 메뉴 최종(사용자 확정)**: `No connection` · 연결할 수 있는 공유 연결 목록 · (전용 연결이 있을 때만) 구분자 + 전용 연결 한 줄. 지금 것 앞에만 **✓**(1탭 1연결 · 배타) · 체크 유무와 무관하게 글자 열 정렬(nexa-ctl `CtxItem::with_mark` — 아이콘 칸에 ✓ · 메뉴 아이콘 설정과 무관 · 34차). 머리글·"Sessions by server"·"Disconnect all"·부가 설명 전부 제거. `No connection` 선택 = 전용 연결 해제.
- **표식 메뉴 보완(사용자)**: 체크가 하나도 없어도 글자 열 고정(`with_mark(false)`도 칸 예약 · `Option<bool>`) · 시작 직후·해제 뒤 등 이 탭의 세션이 접속돼 있지 않으면 `No connection`에 ✓.
- **툴바 Disconnect / 세션 목록 분리(사용자 원복 요청)**: Disconnect = 종전대로 **지금 탭의 연결 해제**(드롭다운 없음 · 이 탭에 연결이 있을 때만 활성) · 새 버튼 `conn.sessions`(Material `dns` 아이콘) = 세션 창. "모두 해제"는 세션 창 우클릭에만.
- **활동 막대 선택 표시줄(사용자 캡처)**: 두께 2 → **4** · 위아래 3px씩 짧게.
- **표식 모양(사용자)**: 바탕은 늘 둥근 사각형 · 연결됨 = 연결됨 색(Switch 초록)의 글자 **S**(공유)/**P**(전용) · 미연결/끊김 = 흐린 플러그+사선 그대로(nexa-ui 35차 `draw_badge`).

## 62차 (mac · 09-19 새벽) — 신호등 갱신 정책 · SQLite 파일 판정 · 접속 문자열 열 · 기본 스키마

- **VPN 복구 뒤 신호등이 안 바뀜(사용자)**: 종전 = 실패 간격 60s × 2^(n−1) → 최대 **32분**(`probe.retry_delay` 60 · `max_retries` 5) · 접속 창을 다시 열어도 "예약 유지". → ① 창을 열 때 **대상 전부 즉시 1회**(`poke`) ② 실패 대기 상한 = **`probe.interval`(60s)** · `probe.retry_delay` 기본 **10s**(10 → 20 → 40 → 60 → 60…) ③ **신호등 클릭 = 지금 다시 확인**(`RowBtn::Light`). 부하: SYN 1/프로필 · 창이 열려 있을 때만 · 접속한 적 있는 서버만(26 §8 그대로).
- **SQLite 신호등**(사용자): `probe::Target::File` — 파일을 열 수 있으면 초록 · 없거나 못 열면 빨강 · `:memory:` 초록 · 접속 이력과 무관(로컬 확인). 행 Test/Connect 버튼도 비밀번호 없이 활성(`row_has_password` = 비밀번호 있음 ∨ SQLite).
- **접속 문자열 열**(사용자): "Target" → **Connection string** · 우리 형식 `dialect://user@host:port/db?schema=x`(비밀번호 없음 · `ConnectSpec::connection_string()` · 그대로 `CONNECT`에 쓸 수 있고 비밀번호는 저장소가 채운다) · 우클릭 **접속 문자열 복사**(`ConnWinAction::CopyText`).
- **기본 스키마**(사용자 "기본 접속 Database"): `ConnectSpec.schema` + `?schema=`(파서·`redacted`·저장소 `schema=`) · 접속 창 **Default schema** 칸(Oracle·PostgreSQL만 · SQL Server는 Database가 그 자리 · 파일 방언 없음) · 접속 직후 `Session::set_option("schema")` — Oracle `ALTER SESSION SET CURRENT_SCHEMA` · PG `SET search_path TO "x", public` · MSSQL `USE [db]`(CONNECT 문자열의 `?schema=`로 호출될 때) · 실패는 오류 이벤트로 알리고 접속은 유지. 테스트 +1(`schema_query_and_connection_string`).
- **행 Test ↔ 폼 상태 동기화(사용자)**: `panel_state_for`가 `panel_op`(다른 프로필의 결과·프로브에 덮임)보다 **행 표식**(`is_testing` · `connect_mark`)을 먼저 본다 → 테스트 중 Details를 펼쳐도 늘 "Testing" · SQLite 행 버튼(Test/Connect)은 비밀번호 없이 활성(`row_ready` 한 규칙 = 아이콘·히트·툴팁).
- 포트 칸 `conn.port_w` 58 → **72**(mac에서 "1522" 잘림) · 세션·트랜잭션 로그 창 = **토글**(열려 있으면 닫기 · 로그 창과 같은 규칙) · 툴바 오른쪽 그룹 툴팁이 창 밖으로 잘림 → 도크가 창 폭을 클램프로 준다(nexa-ui 36차 `paint_tooltip_in`).
- **탭 표식 색 결정(사용자 질문)**: S 공유 = 초록(`ON_GREEN` · 연결됨·평상시) · P 전용 = 강조색 파랑(`tab_accent` · 예외적·격리 = "이 탭만 다르다") · 미연결/끊김 = 흐림. 둘 다 초록이던 것을 바꿈(nexa-ui 37차).
- **접속 폼 바뀜 표시 + Save 유도 설계**([22 §9](../22-driver-extensions.md) · T-131): 스냅숏 비교 · 칸 왼쪽 2px 강조 띠 + 라벨 `•` · Save 강조+`Save •` · 목록 `*` · 잃는 순간에만 저장/버림/취소 · 라벨 클릭 = 그 칸 되돌리기.
