# 2026-09-27 — 이어서 할 일(세션 인계 · Linux 101차 후반)

> 이 문서는 **다음 세션 시작점**이다. 앞선 작업 기록은 [journal 2026-09-26 §6~§10](2026-09-26.md), 결정 자료는 [25 §12·§13](../25-license-tiers-and-server.md) · [91](../91-license-root-key-operations.md).

## 0. 지금 상태 — 미커밋(세 저장소 · 아직 commit/push 안 함)

| 저장소 | 상태 | 검증 |
|---|---|---|
| **nexa-sql** | 27개 수정 + 6개 신규(코드 M 17 · 문서/스크립트) | ✅ fmt·clippy 0 · 테스트 264 통과 · Debug exe 01:58 빌드됨 |
| **nexa-ui** | `textbox.rs`(`commit_composition`) + DEVLOG/STATUS | ✅ clippy 0 · 테스트 통과 |
| **nexa-license** | Cargo.toml·lib.rs 수정 + 6개 신규 모듈 + `rust-toolchain.toml` | ✅ **검증됨(§7)** — fmt ✓ · clippy `-D warnings` ✓ · 시험 19/19 |

## 1. 완료 — 코드 반영됨(커밋만 남음)

1. **저장소 최신화·분석** — nexa-ui 82→83차, nexa-sql +128커밋 당김.
2. **Linux 전수 검사** — 기능 78/78 · 4-DBMS DDL/DML/TCL/DCL 62/62 · 성능·회수 R1~R6 · 신규 도구 4종(`scripts/linux-{func-check,dbms-e2e,inventory,mem-reclaim}.sh`). 문서 = 26 §7-13.
3. **🔧 그리드 UPDATE 방언 오염(ORA-00936)** — 결과 그리드에 세션 방언 동봉(`main.rs` 결과 도착 2곳).
4. **🔧 Ctrl+X 데이터 손실** — 클립보드 성공 뒤에만 삭제(편집기·그리드 셀).
5. **★ Linux 클립보드 X11 직접 구현** — `clipboard_x11.rs`(x11rb · INCR · `clipboard.x11_native`). CLI 폴백 유지.
6. **🔧 ROWID 주입이 `--` 주석에 붙던 ORA-00923** — `gridedit_sql.rs` `code_span()`.
7. **IME 한글 단축키 3-OS 전수** — 보조 창 6곳 `shortcut_letter` · `commit_composition`(nexa-ui). 시험 = 명령 121×3OS.
8. **D-225 그리드 재조회 기본 off** — `grid.edit_hidden_keys/rowid` off · 편집 툴바 **⚿ 행 식별**(`grid.edit.identify`) · 사전 검사 비용 실측(journal §8: 100행 6.9→13.3s ×1.9).
9. **🔧 팔레트 한글 입력** — IME 사건을 팔레트로(`route_inner`).
10. **🔧 직접 실행 COMMIT/ROLLBACK 툴바 반영** — `sessions::user_tx_end` MC/DC + `tx_close`.
11. **★ 팔레트 보기 토글 + 레이아웃 초기화** — 상태 기준 1줄(`PalShow/PalHide`) · `view.layout_reset`.

## 2. 라이선스 — 설계 확정됨 · 라이브러리 개발 중

**결정 자료(문서)**: [25 §12](../25-license-tiers-and-server.md) 발급기 CLI 사양 · [25 §13](../25-license-tiers-and-server.md) 무료/Pro/Org 차별점 + 등록 안내/광고 슬롯 설계 · [91](../91-license-root-key-operations.md) 루트 키 운영(생성·보관·이동·회전·유출). 결정 **D-41~D-48**, 발급기 확정 **L-1~L-7**.

**사용자 확정 사항(09-27)**:
- 초기 버전 = **전체 기능 제공 라이선스** 발급(`features=*`). 영역/기능 구분은 체계화 뒤 새 발급분부터(D-48 · 기존 파일 불변 · 큰 개편 = `nxl2` + 무상/유상 재발급).
- 로컬 기준정보(기기 ID) 기반 노드-락 — 이미 `machine.rs`로 구현 방향.

**작성된 `nexa-license` 모듈(미검증)**:
- `date.rs` — YYYY-MM-DD 파싱(외부 crate 0 · 시험 포함)
- `request.rs` — 요청 코드 `NEXAREQ1` encode/decode(시험 포함)
- `machine.rs`(feature `machine-id`) — 3-OS 기기 ID SHA-256[..20](시험 포함)
- `verify.rs` — `verify_license` + `SigVerifier` 포트 + `Verdict`(dalek 시험 포함)
- `ed25519.rs`(feature `ed25519`) — dalek 2.x 검증 어댑터
- `keys.rs` — `ROOT_KEYS`(빈 목록 · 발급 PC에서 keygen 뒤 채움)
- `Cargo.toml` — `ed25519`/`machine-id` feature + dev-deps · `lib.rs` 모듈 등록

## 3. ~~⚠️ 급한 것 — 툴체인~~ → ✅ 해결(§7 · ⓐ 저장소 안 `rust-toolchain.toml`)

`nexa-license`에서 `cargo test`가 **"no default toolchain is configured"**로 실패한다.
- 원인: `nexa-sql`에는 `rust-toolchain.toml`이 있어 툴체인이 고정되나, `nexa-license`에는 없고 rustup 기본값도 미설정.
- 설치된 툴체인: `stable-x86_64-unknown-linux-gnu (active)`.
- **고칠 방법(둘 중 하나 · 사용자 확인 후)**: ⓐ `nexa-license`에 nexa-sql과 같은 `rust-toolchain.toml` 복사 · ⓑ `rustup default stable`. (사용자가 직전 tool 호출을 거부했으므로 다음 세션에서 방식을 확인하고 진행.)
- 고친 뒤: `cd ../nexa-license && cargo test --workspace --all-features` → 6개 모듈 시험 통과 확인. **MSRV 주의**: nexa-license Cargo.toml은 `rust-version = "1.82"`인데 ed25519-dalek 2.x가 더 높은 MSRV를 요구할 수 있음 → 실패 시 `rust-version` 상향 또는 dalek 버전 조정.

## 4. 대기 — 접수만 하고 미착수

- **디지털 자산 보호 설계**(사용자 09-27): 로컬 기준정보 기반 생성은 진행 중. 클라이언트 측 분석 지연 기법은 **효과가 제한적**(docs/23 §2-3: 로컬 검증은 바이너리 패치로 우회 가능 — 모든 로컬 인증의 공통 한계)이므로, 강한 보호는 서버 검증(25 §4)에 두는 것이 설계 원칙. → **설계 문서로 정리**(구현 아님) 예정. 사용자와 범위 합의 필요.
- **등록 안내 카드 + 광고 슬롯**: 25 §13-5 설계만 있음. 구현은 D-47 확정 뒤. 광고는 **테스트 전용**(Release `none` 고정 · 소재 컴파일 제외).
- **발급기 CLI `nexa-license-tool`**(T-241): 비공개 저장소 `SosomLab/nexa-license-server` 필요 → **L-7: 사용자가 GitHub에서 생성**해야 착수(내가 못 만듦).
- **상한식 게이트**(D-46): 동시 연결 2/2 · 편집 탭 5 · 편집 용량 2MB 읽기전용 · 결과 탭 2 · 창 1 — D-41 확정 뒤.

## 5. 다음 세션 순서(권장)

1. 툴체인 고치기(§3) → `nexa-license` 검증 통과.
2. 세 저장소 **commit·main 병합·push**(nexa-ui 먼저 · CLAUDE.md §3 규약) — 단, 사용자 명시 요청 시에만. 라이선스 미검증분은 검증 뒤 커밋.
3. 라이선스: L-1~L-7·D-41~D-48 확정 → `keys.rs` 채우기(발급 PC keygen) → 발급기 저장소(L-7) → 발급기 CLI(T-241) → 앱 층 `nsql-license`(T-32').
4. 남은 실기(T-163 Linux ⑦~⑫) · docs 디지털 자산 보호 설계.

## 6. 기억할 규칙(이번 세션에서 확인)

- 기능 점검·성능은 **Release로**(Debug filterbar 1.5s vs Release 0.1s).
- 자동 시험에 **사용자 클립보드를 덮어쓰는 명령 금지**(09-27 위반 사고 · journal 09-26 §6-3).
- 앱 재시작 = 편집 중일 수 있으니 **내가 띄운 PID만** kill 후 새로 띄움.

## 7. 이어진 세션(09-27 · Linux 101차 후반 2) — "별도 확인이 필요 없는 대상 전체 진행"

사용자 지시 = "진행해야 할 대상을 분석하고 순서대로 · 별도 확인이 필요 없는 대상 전체 진행 · 관련 저장소 전체 push".

### 7-1. 툴체인 → nexa-license 검증 ✅

- §3의 두 안 중 **ⓐ**를 택했다 — `nexa-license/rust-toolchain.toml`(stable · rustfmt/clippy · 교차 타깃 없음)은 **저장소 내부 파일**이라 확인 대상이 아니고, ⓑ `rustup default stable`은 사용자 전역 rustup 설정이라 뺐다.
- `cargo test --workspace --all-features` = **19/19**(base32 4 · date 1 · format 6 · machine 2 · request 1 · types 2 · verify 3). 의존 MSRV 확인: ed25519-dalek 2.2.0 = 1.81 · curve25519-dalek 4.1.3 = 1.60 → `rust-version = "1.82"` 그대로 충분.
- CI 게이트(`clippy --all-features --all-targets -D warnings`)에 걸릴 것 둘을 미리 잡았다: `cargo fmt` 차이(date.rs 시험) · 워크스페이스 lint `unwrap_used` 5건(date/machine/request 시험) → 다른 시험 모듈과 같게 `expect(...)`.
- README 모듈 상태표 갱신(verify·ed25519·machine·request·date ✅ · keys 🚧 빈 목록).

### 7-2. 문서 사이 어긋남 하나(결정 대기 · 사용자에게 보고)

`nexa-license`의 서명 코드 위치가 문서마다 다르다 — **25 §11-1(09-14)·README·lib.rs** = "공개 라이브러리는 검증 전용 · `issuer` feature 삭제 · 서명은 비공개 저장소만" vs **25 §12-1·§12-8(09-27 확정용 제안)** = "`nexa-license` `features = ["issuer"]` 뒤에 sign". L-1~L-7 답과 함께 **L-8**로 정해야 발급기 착수 때 헤매지 않는다(권장 = §11-1 유지 · sign은 비공개 `nexa-license-tool` 안에 · 라이브러리는 `canonical`·`base32`·`Doc`만 제공 — 지금 코드가 이미 그 형태).

### 7-3. push(사용자 명시 요청) — 순서 nexa-ui → nexa-license → nexa-sql

- 게이트: nexa-ui fmt ✓ · clippy `-D warnings` ✓ · hangul 시험 16 ✓ / nexa-sql fmt ✓ · clippy ✓(테스트 605·통합 13/13은 §0·09-26 §4-1) / nexa-license 위 §7-1. `check-3os.sh`는 이 VM에서 교차 컴파일 한계(ring · 09-26 §4-1)라 호스트 검사만 — 3-OS는 CI가 본다.
- 결과·CI 기록은 아래 §7-4.

### 7-4. 확인 없이 진행하지 않은 것(사용자 몫 · 그대로 남김)

- D-41~D-47 · L-1~L-7(+L-8) 확정 → 상한식 게이트(D-46) · 등록 안내 카드/광고 슬롯(D-47) · 발급기 T-241 · `keys.rs` 채우기(발급 PC keygen · docs/91 §1) · `nexa-license-server` 저장소 생성(L-7).
- T-163 Linux 실기 ⑦~⑫(키·눈이 있어야 보이는 것).

## 8. 102차(win · 09-27) — "저장소 최신화 및 변경사항 분석, license와 ui 저장소 포함"

세 저장소 `--ff-only` 당김: nexa-ui 3165e24→5b2b049(84차 `commit_composition`) · nexa-license 8588bc7→a6f7ce8(T-45 2차 · 6 모듈) · nexa-sql d1a68b1→4ecbd88(101차 Linux 일괄). Windows에서 처음 도는 코드는 nexa-sql의 IME 단축키 전수(`shortcut_letter_of` · 보조 창 6곳) · Ctrl+X 순서 · 그리드 방언 동봉 · `user_tx_end` · 팔레트 토글 — Linux 전용은 `clipboard_x11.rs`(cfg) 하나.

### 8-1. CI — nexa-sql 4ecbd88만 빨강(ci macOS·Windows · integration)

| 워크플로 | 원인 | 고침 |
|---|---|---|
| `ci` check(macos·windows) | `main.rs` `settings_clip_native()`가 Linux `clipboard.rs` X11 경로에서만 불려 다른 OS에서 dead_code → `-D warnings` | `#[cfg_attr(not(all(unix, not(target_os = "macos"))), allow(dead_code))]`(규칙 = OS별 cfg 함수는 allow · [61 §3](../61-core-design-and-working-rules.md)) · Windows clippy `--all-targets -D warnings` ✓ |
| `integration` it | d1a68b1(문서 커밋 · `examples/`는 paths 밖이라 그때는 안 돌았다)이 `it-mssql.sql`에 `SELECT :Top from dual;`를 넣음 → SQL Server에 `dual` 없음(Msg 208) | `SELECT :Top AS top_val;`(FROM 없이 · 사용자 의도 "치환 변수 확인" 유지). 참고 = 출력에 "`:TOP` undeclared → NULL" — `:setvar`는 치환 변수라 바인드 `:Top`은 NULL이 맞다 |

nexa-ui 5b2b049 · nexa-license a6f7ce8 = ci 초록. 수정 둘은 사용자 요청("리눅스에서 테스트 중 · 꼭 필요한 대상만 commit·push")으로 **main에 직접 5265dcc**(두 파일만 · 문서는 뒤에).

### 8-2. Windows 로컬 검증

- nexa-license `cargo test --workspace --all-features` = **19/19**(Windows 기기 ID `reg query MachineGuid` 경로 포함 — `this_machine_yields_an_id_or_none`).
- nexa-sql `cargo test --workspace` = 610 통과 · 실패 0(1 ignored) · Release GUI·CLI 빌드 ✓.

### 8-3. 읽으며 본 것(고치지 않음 · 보고)

- **nexa-license `machine.rs` Windows** = `std::process::Command("reg")` — GUI 앱(windows subsystem)에서 부르면 **콘솔 창이 잠깐 뜬다**(`CREATE_NO_WINDOW` 없음). 앱 층 `nsql-license`(T-32') 배선 때 `creation_flags(0x0800_0000)` 또는 레지스트리 직접 읽기(advapi32 · 외부 crate 0 유지)로 바꿔야 한다. 같은 이유로 macOS `ioreg`는 문제없다(콘솔 없음).
- L-8(서명 코드 위치)은 §7-2 그대로 결정 대기.
- 다음 = journal §5 순서(D-41~D-47 · L-1~L-8 → `keys.rs` → 발급기) · T-163 Linux 실기 ⑦~⑫.

## 9. 구현 — 디지털 자산 보호 설계(92)의 코드 몫 + 라이선스 앱 층 T-32'·T-33(사용자 09-27 "바로 구현도 진행")

설계 [92](../92-digital-asset-protection.md)의 결론은 "분석 지연 기법은 하지 않는다(D-227) · 저비용 보강 넷 R1~R4(D-228)"이고, R1~R4는 전부 **앱 층**에 산다. 그래서 앱 층 최소판을 이번에 만들었다(게이트 배선·GUI 배지는 D-41·D-44 확정 뒤 — 코드는 준비돼 있고 부르는 곳만 없다).

### 9-1. nexa-license `fs`(feature) — R4 "파일 자리 고정 + 원자적 쓰기"

`fs.rs` `Store`(폴더 목록 주입 · 라이브러리는 OS 폴더 규칙을 모른다) — `locate`(첫 존재 파일) · `read` → `Found{path,text,stamp}` · `install_text/install_from`(첫 폴더 · `write_atomic` = 같은 폴더 임시 파일 + `rename` · 유닉스 0600) · `remove`(첫 폴더만) · `stamp`(mtime+길이 = 변경 서명). 시험 4(폴더 없음 = None · 순서/삭제/권한/임시 파일 0 · 원본 보존·서명 변화 · **동시 설치 300회 읽기에 찢긴 파일 0** = 23 §5). 라이브러리 시험 23/23.

### 9-2. nsql-license(신규 크레이트 · T-32' 앱 층)

- `PRODUCT`(`build.rs`가 `NSQL_BUILD_DATE` 박음 · 우선순위 env → `SOURCE_DATE_EPOCH` → 지금 · Hinnant civil · 외부 crate 0) = **R2 빌드일 기준 시각**.
- `Feature` 20종 열거(25 §13-3 표 · Pro 11 · 상한 4 · Org 5) · `as_str` 매핑 한 곳 · 모르는 이름 무시 · `Tier` 표시용.
- `LicenseState`(Free·Licensed·Invalid·Outdated·Expired) · `entitlement(state, feature)` **순수 함수** · `Licensing::check` = 유일한 판정 입구(R1).
- `Licensing` = `open_default`(사용자 `<설정>/license` → 기기 공용 `/etc/nexa/nexa-sql` · `%ProgramData%\nexa\nexa-sql` · `/Library/Application Support/nexa/nexa-sql`) · `refresh`(경로·mtime·길이 서명 재판정) · `install`(**Licensed만 쓴다** · 원본 보존) · `remove` · `machine_code` · `request_code`(메타 os·app·d·n·e).
- 루트 키 = `nexa_license::keys::ROOT_KEYS`(지금 빈 목록 → 모든 파일 `Invalid(NoRootKey)` = Free). 시험은 `with_roots`로 키 주입.
- 시험 8(23 §5 전부): 설치→check Allowed/Denied(NotIncluded) · `*` = Org 포함 전부 · 변조 = Signature·**쓰지 않음** · 순서 교란+CRLF+주석 = 통과 · 다른 루트 키 거부 · 다른 기기 · Outdated(features ∅·표시 Free) · Expired · 다른 제품 · 형식 · 빈 루트 키 · refresh 변경 감지 · 사용자 폴더 > 기기 폴더 · 요청 코드 왕복.
- 로그 규칙(92 §4): 크레이트는 `licensee`·이메일·기기 원문을 어디에도 쓰지 않는다.

### 9-3. CLI `nsql license`(T-33)

`status | request [name [email]] | install <file> | remove | path` · help 항목 + i18n `Msg` 7(영/한). 격리 `NSQL_HOME` 실동작: free 상태 표 · 요청 코드 한 줄 · 쓰레기 파일 `invalid(format)` 종료 1 · 형식만 맞는 파일 `invalid(malformed)` 종료 1 · **거부 = 폴더조차 만들지 않음** · 실제 설정 폴더 무영향. 종료 코드 4(게이트)는 각 명령 입구 몫(D-41 뒤).

### 9-4. CI 두 결함(4ecbd88 빨강) — 102차 win(5265dcc)과 같은 결론

- mac·win clippy: 102차 win이 먼저 `cfg_attr(allow(dead_code))`로 고쳐 push(5265dcc) → rebase에서 그쪽을 따랐다(내 `#[cfg]` 판은 버림 · 결과 동일).
- integration: `examples/it-mssql.sql` 15행 `SELECT :Top from dual;`(d1a68b1 · SQL Server에 dual 없음 · `examples/**`는 integration 트리거 밖이라 이번에 처음 돌았다) → `SELECT :Top AS top_val;`(사용자 09-27 "from dual 제거" · 102차 win과 같은 줄이라 그쪽 판).
- 워크플로 3개(ci · integration · release)에 **형제 저장소 `nexa-license` 체크아웃** 추가(nsql-license path 의존 · 공개 저장소라 토큰 불필요).

### 9-5. 남은 것(결정 뒤)

GUI 라이선스 탭·상태줄 배지(T-34 · D-44 문구) · 게이트 배선(T-36 · D-41 표) · 상한식 게이트(D-46) · 등록 안내 카드(D-47) · `keys.rs` 채우기(발급 PC keygen · 91 §1) · 발급기(T-241 · L-7).

### 9-6. push 경위 · 102차 win과의 합류 · machine.rs

- nexa-license `fs` = e1f1dc7(CI ✓) → nexa-sql push가 **거부**(원격에 102차 win 5265dcc·697bd85) → `rebase origin/main`: 코드 둘(`main.rs` · `it-mssql.sql`)은 **원격 판**(같은 결론 · Windows에서 검증됨) · 문서(CLAUDE.md 현 단계 · DEVLOG · journal)는 양쪽 유지 · 내 §8 → **§9**. 결과 = 018014f.
- 102차 win §8-3 지적(`machine.rs` Windows `reg` = GUI에서 콘솔 창 깜빡임) → nexa-license **8c556e8** `creation_flags(CREATE_NO_WINDOW)`(std `CommandExt` · 외부 crate 0). Linux에서 `--target x86_64-pc-windows-msvc` check·clippy로 확인(nexa-license는 C 의존이 없어 교차 검사가 된다).
- 사용자 "Windows PC 수정분 반영" = fetch 결과 세 저장소 로컬 = 원격(102차 커밋은 rebase로 이미 포함). 그쪽에 미push 변경이 있으면 push 뒤 다시 당긴다.
- **CI 전부 초록**: nexa-sql 018014f `ci`(macOS·Windows·Ubuntu) ✓ · `integration` ✓(it-mssql 통과) · nexa-license e1f1dc7 ✓ · 8c556e8 ✓ · nexa-ui 5b2b049 ✓.
